
# Quality Rules

These rules should be enforced by the local harness where practical.

## Planning

- Every implementation task starts from `docs/exec_plans/active/plan-NNN-<task>.md`.
- Completed plans move to `docs/exec_plans/completed/`.
- Completed plans require review mirrors in `docs/reviews/`.
- Plan filenames use `plan-NNN-<task>.md`.
- `docs/plan` is prohibited.
- Update the Decision Log when scope or approach changes.

## Git Flow

- Do not implement, commit, or push feature work directly on `main`.
- Use `codex/plan-NNN-<task>` branches for implementation work.
- Use `.worktree/plan-NNN-<task>` checkouts for git repository work.
- After QA and review, merge the task branch to `main`, push `main`, delete the merged branch with `git branch -d`, and remove the worktree.

## QA And Review

- QA and review are separate loops.
- Review starts only after QA completes.
- Failed validation must be reported and fixed or explicitly documented.
- Completion reviews go in `docs/reviews/` and should summarize QA, findings, residual risk, and follow-ups.
- Strict quality scans must ignore local `.worktree/` checkouts while still checking committed docs and completed plan/review artifacts.

## Documentation Hygiene

- Root Markdown files are limited to `README.md` and `AGENTS.md`.
- Durable documentation belongs under `docs/`.
- `AGENTS.md` should stay a concise repository map.
- `README.md` is the current public entry point and must stay aligned with code behavior.

## Safety

- Sensitive real user, customer, startup, credential, and production data must not appear in samples, tests, docs, or screenshots.
- Broad permissions, remote AI calls, trackers, ads, payment flows, and destructive automations require explicit project rationale.
- Real user audio, copyrighted sample packs, unreleased beats, credentials, and private project files must not be committed.

## Commands

Current commands:

```sh
python3 harness/scripts/run_qa.py
python3 harness/scripts/run_quality_gate.py
npm run harness:smoke
npm run typecheck
npm run build
npm run qa
npm run verify
```

Desktop app manual check:

```sh
npm run desktop
```

## Product QA Gates

P0 scheduler work must prove stable play/stop/loop behavior, BPM changes, current-step feedback, and separation between UI timing and audio timing.

Runtime smoke work must execute the real local TypeScript domain, render, and MIDI modules through `npm run harness:smoke`; construct sample-free 8-bar beats from every supported `styleProfiles` entry and every Beat Blueprint using existing project data; verify non-silent full-mix plus drum, 808, synth, and chord stem analysis for each case; verify WAV RIFF/WAVE headers, WAV/stem file names, Standard MIDI header, and MIDI file name; avoid writing media artifacts; and avoid browser, Electron, network, imported audio, sampling, remote AI, accounts, analytics, or cloud sync dependencies.

Production build hygiene work must preserve `outDir: "dist"` and `sourcemap: true`, use Vite 8 / Rolldown `build.rolldownOptions.output.codeSplitting.groups` for real chunk separation, and must not hide large-chunk warnings by setting `chunkSizeWarningLimit`. UI chunk work may split pure model data, helper utilities, shared analysis helpers, read-only derivation modules, and render-only panels out of `src/ui/App.tsx`, including render-only Guided/Studio workflow panels and Snapshot Compare derivation code, but must not alter app UI behavior, project schema, playback, audio rendering, WAV/stem/MIDI export, Handoff behavior, local draft behavior, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Brief correction and sampling-audit work must prove the product can no longer be summarized as a sampling app, sample-chopping tool, sample-pack workflow, or sampler setup surface. Incoming drafts that place `AudioClipEvent`, `audio`, or `sampler` in core event, clip, track, device, or default Instrument Panel examples must be rewritten before implementation so the MVP event union stays `NoteEvent | DrumHitEvent | AutomationEvent`, core clips stay pattern/MIDI/automation containers, MVP tracks stay built-in beat-production tracks, and sampler/audio-clip concepts remain only in a clearly labeled optional sampling extension.

Attached-brief intake checks must reject combined MVP examples that include `audio`, `sampler`, `AudioClip`, `Sampler`, waveform editing, chop pads, sample import, or one-shot mapping in the core track, clip, device, audio-engine, first-run, or roadmap lists. The accepted rewrite keeps the direct beat-workstation core sample-free and moves those entries to a later optional sampling module.

Korean concept-brief checks must treat "비트(모든 장르)를 만드는" and "샘플링은 부가 기능" as binding product constraints. Any sample import, chopping, loop-stretching, sampler, `AudioClipEvent`, `audio`, or `sampler` example from that brief must be moved to optional extension scope before code, docs, roadmap, UI plans, or QA expectations accept it.

Latest concept-verdict checks must reject summaries, plans, UI drafts, architecture proposals, and roadmap notes that describe GrooveForge as "sample import -> chop -> arrange -> mix/master" or as a sampler with composition extras. The accepted summary is "directly compose/program beats across genres, design sounds, arrange, mix/master, and export"; sampling can appear only as a later opt-in material source inside that workstation.

Attached Korean brief rewrite checks must accept the corrected beat-production mini DAW definition while rejecting verbatim promotion of its mixed optional-sampling examples into the MVP. `AudioClipEvent`, `AudioClip`, `audio`, `sampler`, waveform editing, sample import, chop/slice, loop stretch, pitch/stretch, one-shot mapping, and sampler tracks must be optional extension material; MVP docs, plans, schemas, first-run UI, and QA expectations must keep the core path sample-free and event-first.

First-read framing checks must keep README Product Spine and product Product Definition sections centered on direct beat composition, sound design, arrangement, mixing, mastering, and export. Detailed sampling, `AudioClipEvent`, `sampler`, and attached-brief correction rules belong in Draft Intake Guardrails or optional-extension sections, not in the first-read concept summary.

Beat-workstation reframe work must include an acceptance test that a first session can choose BPM/key/style, program drums and 808/bass, add melody/chords, shape sounds, arrange sections, balance the mix, set the master posture, and export WAV/stems without importing audio.

Producer/beginner workflow checks must preserve both routes: a guided setup -> compose -> arrange -> mix -> deliver path for first-time beat makers and direct editable access to events, tone, arrangement, mix/master, and handoff posture for working producers.

Tap Tempo and Quick Actions work must keep tap history UI-local and out of saved project schema, route button clicks and command pulses only through the existing tap handler and delayed commit path, update only the project BPM through explicit user clicks/commands and existing undoable project history, clamp calculated BPM to the supported transport range, preserve manual BPM entry, playback, metronome, save/load, WAV/stem/MIDI export semantics, and avoid audio input, recording, beat detection, tempo automation, command chains, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Tempo Nudge Pad and Quick Actions work must derive -1, +1, half-time, and double-time BPM changes only from the current project BPM, clamp results to the supported transport range, reset UI-local Tap Tempo state, route every pad click and command run through the existing explicit undoable tempo nudge handler, preserve manual BPM entry, Tap Tempo, playback, metronome, save/load, WAV/stem/MIDI export semantics, and avoid hidden tempo automation, command chains, audio input, recording, beat detection, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Swing Feel Pad and Quick Actions work must derive Straight, Tight, Laid, Loose, and current-style targets only from local Swing Feel definitions, `project.swing`, and the selected local style profile; route every visible pad click and command run only through the existing undoable project swing update path; show before/after Swing Feel Result feedback as UI-local state; preserve the manual Swing slider, style selection, Style Inspector, Groove Compass, Pattern A/B/C event data, playback, save/load, snapshots, undo/redo, WAV/stem/MIDI export, and project schema beyond the existing swing field; and avoid changing drum microtiming, note timing, arrangement, mixer, master, hidden generation, command chains, autoplay, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Transport Position Readout work must derive Bar/Beat/Step, section, Pattern A/B/C, and Song/Block/Turn/Pattern loop-scope labels only from local playback snapshots, selected arrangement block state, focused transition state, selected pattern state, and current transport loop scope; stay UI-local and out of saved project schema; preserve playback scheduling, loop selection, metronome, Tap Tempo, Tempo Nudge Pads, save/load, undo/redo, WAV/stem/MIDI export semantics; and avoid count-in, marker editing, hidden arrangement mutation, audio input, recording, beat detection, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Metronome and Quick Actions work must keep click playback realtime-only, accent beat 1 distinctly from beats 2-4, persist the transport toggle through save/load migration, route the transport button and command run only through the existing metronome toggle handler, follow live BPM changes while playing, preserve Stop/Space behavior, and keep WAV/stem exports free of metronome audio.

Transport Command Reference work must mark Transport Position Readout, Loop Scope, and Metronome Desktop rows as readout-backed transport entries, reflect only the existing local Bar/Beat/Step context, Song/Block/Pattern loop-scope controls, realtime metronome toggle, and local result feedback; preserve Transport Position derivation, playback scheduling, loop-scope routing, metronome toggle behavior, metronome persistence, realtime click synthesis, result copy, disabled-state behavior, Tap Tempo, Tempo Nudge, Swing Feel, Key Retarget, Pattern Cue/Switch/Use, Arrangement Block Cue/Jump, Section Locator, Transition Loop, Hook Loop, Topline Loop, Pattern A/B/C events, arrangement, mixer, master, project data, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid count-in, marker editing, audio input, beat detection, hidden arrangement mutation, autoplay, tutorials, macros, command chains, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Pattern Cue Quick Actions work must expose only Pattern A/B/C audition-prep commands, route each command through the existing Pattern Compare Cue handler, set only selected-pattern preview state plus Pattern loop scope, reset selected note/drum/chord focus the same way Cue buttons do, show local Pattern Compare Result feedback for target Pattern, event posture, audition cue, and next check only after explicit command runs, avoid autoplay and undo history, preserve Pattern A/B/C event data, selected-block Pattern assignment, arrangement blocks, playback start/stop, save/load, mixer/master, WAV/stem/MIDI export semantics, and avoid hidden generation, command chains, auto-arrangement, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Pattern Switch Quick Actions work must expose only Pattern A/B/C edit-focus commands, route each command through the existing Pattern tab selection handler, reset selected note/drum/chord focus the same way tab clicks do, show local Quick Action Result feedback for before/after Edit Pattern posture, event count, arrangement usage, audition cue, and next check only after explicit command runs, preserve Pattern A/B/C event data, arrangement blocks, playback scheduling, loop scope, save/load, mixer/master, WAV/stem/MIDI export semantics, and avoid hidden generation, command chains, autoplay, auto-arrangement, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Pattern Cue/Switch result metric work must derive the explicit cue or edit-focus action, target Pattern A/B/C, target event count, drum/music posture, arrangement usage, selected-block placement, and current edit Pattern only from explicit `pattern-cue-*` or `pattern-switch-*` command ids plus local Pattern and arrangement state after existing Cue/Switch handlers; preserve Pattern Cue and Pattern Switch routing, selected Pattern alignment, selected-event reset behavior, Pattern Compare Result feedback, Pattern A/B/C musical events, selected-block assignment, arrangement blocks, playback, save/load, WAV/stem/MIDI export, Handoff, command search, scope filters, Pinned Commands, and Recent Commands; and avoid auto-arrangement, command chains, macros, autoplay, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Pattern Compare Decision result metric work must derive the explicit Cue or Use recommendation, target Pattern A/B/C, target event count, drum/music posture, selected-block placement, arrangement usage, and current edit Pattern only from the current `pattern-compare-decision` command title/detail plus local Pattern and arrangement state after the existing Pattern Compare Decision action handler; preserve recommendation derivation, visible readout routing, direct Pattern Cue/Switch/Use commands, selected Pattern alignment, undo semantics, Pattern Compare Result feedback, Pattern A/B/C musical events, arrangement length, section, bars, energy, muted tracks, playback, save/load, WAV/stem/MIDI export, Handoff, command search, scope filters, Pinned Commands, and Recent Commands; and avoid auto-arrangement, command chains, macros, autoplay, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Pattern Use Quick Actions work must expose only selected-arrangement-block Pattern A/B/C assignment commands, route each command through the existing Pattern Compare Use handler and undoable arrangement block update path, disable no-op commands when the selected block already uses the target Pattern, update only the selected block's Pattern assignment plus selected edit Pattern, show local Pattern Compare Result feedback for selected-block placement, event posture, audition cue, and next check only after explicit command runs, preserve Pattern A/B/C event data, arrangement length, section, energy, muted tracks, playback scheduling, loop scope, save/load, mixer/master, WAV/stem/MIDI export semantics, and avoid hidden generation, command chains, autoplay, auto-arrangement, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Pattern Use result metric work must derive the explicit selected-block assignment action, target Pattern A/B/C, selected block section, bar range, target event count, drum/music posture, arrangement usage, and current edit Pattern only from explicit `pattern-use-*` command ids plus local Pattern and arrangement state after the existing Pattern Use handler; preserve Pattern Use routing, disabled no-op behavior, selected Pattern alignment, undo semantics, Pattern Compare Result feedback, Pattern A/B/C musical events, arrangement length, section, bars, energy, muted tracks, playback, save/load, WAV/stem/MIDI export, Handoff, command search, scope filters, Pinned Commands, and Recent Commands; and avoid auto-arrangement, command chains, macros, autoplay, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Drum, bass, melody, and arrangement work must preserve editable project state without losing event timing, pitch, velocity, glide, track routing, or mixer state.

Drum dynamics work must migrate old patterns, preserve per-step velocity and hat repeat data across Pattern A/B/C copy, save/load, undo/redo, realtime playback, WAV export, and stem export.

Drum probability work must migrate old patterns to 100% chance, preserve per-step probability data across Pattern A/B/C copy, save/load, undo/redo, realtime playback, WAV export, and stem export, and use deterministic probability gates so exported audio is reproducible from saved project data.

808, melody, and chord chance work must migrate older musical events to 100% chance, preserve probability data across Pattern A/B/C copy, save/load, undo/redo, realtime playback, WAV export, and stem export, and use deterministic probability gates so exported audio is reproducible from saved project data.

Chance badge work must keep badges read-only, show below-100% drum, 808, melody, and chord chance without changing playback or export behavior, and keep the default 100% editing grid visually clean.

Drum microtiming work must migrate old patterns, preserve per-step timing offsets across Pattern A/B/C copy, save/load, undo/redo, realtime playback, WAV export, and stem export, and keep offsets as local musical event data rather than imported audio.

Groove humanize work must be deterministic, operate on selected Pattern A/B/C velocity and microtiming event data, preserve pattern independence, keep results manually editable, and avoid remote AI or sampling dependencies.

Style groove work must apply key-aware editable Pattern A/B/C event data, BPM, swing, and built-in sound preset changes without introducing imported audio or bypassing undo/redo.

Style expansion work must add new genres as first-class `StyleId` entries with key-aware Pattern A/B/C event blueprints, BPM/swing defaults, and local sound preset mapping; every new style must remain editable, undoable through the existing style-selection path, sample-free, and free of imported audio, hidden assets, remote AI, plugin hosting, accounts, analytics, or cloud sync.

Style Inspector, Style Goal Progress Cards, Style Goal cue controls, Style Goal Cue Result feedback, Style Goal action controls, direct Quick Actions Style Goal Cue commands, direct Quick Actions Style Goal Action commands, Style Goal Action Result feedback, Style Quick Picks, and Quick Actions Style Quick Pick work must derive read-only BPM range, active/default swing, bass role, melody role, sound preset, direct-composition goals, selected-Pattern/arrangement progress, command labels, focus labels, goal-cue buttons, goal-cue command labels, goal-cue result labels, goal-cue result action labels, goal-action buttons, goal-action command labels, goal-action result labels, quick-pick result labels, quick-pick result metrics, and Pattern A/B/C event density from local style profile, existing composer-action style goals/cues/actions/results, selected Pattern counts, arrangement bars, loop-scope state, and project data; keep Style Goal Progress Cards read-only except for explicit cue buttons and explicit action buttons; route Style Goal Cue clicks and commands only through existing Pattern/Song loop-scope behavior while playback is stopped, without autoplay, undo history, musical event mutation, arrangement mutation, mixer/master mutation, save/load changes, or export changes; show Style Goal Cue Result only after explicit cue actions and keep it UI-local and out of saved project schema; route Style Goal Cue Result action clicks and Style Goal Action clicks/commands only through existing Composer Actions, keep cue-result action controls disabled when no matching action exists, and never auto-run actions after cueing; keep Style Goal Action Result UI-local and out of saved project schema; keep style selection accessible/testable; route quick-pick button clicks and command runs through the existing undoable style-selection path; preserve existing style application, Composer Action semantics, undo/redo, save/load, playback, WAV/stem/MIDI export semantics; and avoid sampling, imported audio, hidden generation, remote AI, plugin hosting, accounts, analytics, or cloud sync.

Style Inspector Focus work must derive focus targets, the Quick Actions Style Inspector focus command, any direct Style Inspector focus Quick Actions, Focus Result labels, style metrics, audition cues, and next-check feedback only from existing Style Inspector metric ids, Style Goal Progress focus ids, existing Style Inspector metrics, Style Goal Progress cards, Pattern A/B/C density rows, local style/profile posture, the visible summary, and the explicitly clicked or command-run item; show Focus Result only after explicit visible or command focus; keep focused item state and result feedback UI-local and out of saved project schema and undo history; route clicks and command runs only to existing Transport, Compose, Arrange, or Sound panels through the existing focus handler; preserve focus derivation, metric order, goal card order, density row order, visible focus buttons, Style Quick Picks, style selection, current-style starter preview/apply behavior, undo/redo, save/load, playback, WAV/stem/MIDI export, Handoff Pack, Handoff Sheet, and project data semantics; avoid changing style profile definitions, style selection, Quick Pick behavior, generated Pattern A/B/C events, arrangement, mixer, master, exports, playback, or direct editing semantics; and avoid auto-applying styles, autoplay, command chains, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Style Inspector result metric work must derive the explicit priority or direct style-inspector focus action, destination panel, focused genre-fit lane, style name, BPM range/current BPM, swing/default swing, bass role, melody role, sound preset, direct-composition goal posture, Pattern A/B/C density, selected Pattern, editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, audition cue, and next style-inspector check only from current Style Inspector command id/title/detail plus local style profile, Pattern, arrangement, selected-block, and export state after the existing focus path; preserve Style Inspector derivation, focus item ordering, focus routing, Style Quick Pick behavior, style profile definitions, selected style application, Pattern A/B/C musical events, arrangement data, playback, save/load, WAV/stem/MIDI export, Handoff, command search, scope filters, Pinned Commands, and Recent Commands; and avoid auto-applying styles, auto-writing, auto-arrangement, command chains, macros, autoplay, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Style Inspector Command Reference work must mark the Create row as `Quick Actions / Readout`, reflect only the existing local genre-fit diagnostics, BPM/swing/bass/melody/sound posture, Style Goal Progress, Pattern density, Style Inspector focus command, direct lane focus commands, and local Focus Result feedback; preserve Style Inspector derivation, focus derivation, metric order, goal card order, density row order, focus routing, Quick Actions execution, Style Quick Picks, style selection, current-style starter preview/apply behavior, style profile definitions, Pattern A/B/C events, arrangement, mixer, master, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid auto-applying styles, hidden generation, automatic writing, tutorials, macros, command chains, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Key Retarget and Quick Actions Key Retarget work must derive key commands only from existing local key options, route dropdown changes and command runs only through the existing undoable `retargetProjectKey` path, retarget Pattern A/B/C 808/bass, melody, and chord-root musical events without changing scale definitions or retargeting algorithms, show local Quick Action Result feedback for before/after key, Pattern A/B/C event count, edit Pattern, audition cue, and next check only after explicit command runs, preserve Key Compass focus behavior, selected-note and selected-chord editing, Keyboard Capture, MIDI Input, save/load, snapshots, realtime playback, WAV/stem/MIDI export, and Handoff semantics, and avoid auto-retargeting, hidden generation, command chains, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Setup Command Reference work must mark Tap Tempo, Tempo Nudge, Swing Feel, Key Retarget, and Style Quick Picks Create rows as `Quick Actions / Readout`, reflect only the existing local BPM pulse, BPM nudge, groove feel, key retarget, style direction, and local result feedback; preserve Tap Tempo history, delayed BPM commit behavior, Tempo Nudge calculation, Swing Feel derivation, Key Retarget logic, Style Quick Pick routing, result copy, disabled-state behavior, manual BPM entry, manual Swing slider, style profiles, key options, scale definitions, Style Inspector, Key Compass, Groove Compass, Beat Blueprints, Pattern A/B/C events, arrangement, mixer, master, project data, playback, metronome, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid automatic tempo detection, audio input analysis, auto-retargeting, auto-applying styles, hidden generation, tutorials, macros, command chains, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Key Compass work must derive scale notes, chord motion, cadence resolution, 808/bass posture, melody posture, and selected-note or selected-chord focus only from local key, selected Pattern A/B/C musical event data, and current editor selection state; remain read-only; avoid mutating project state, changing recommendations, applying chord or note edits, triggering playback, or triggering exports; preserve key retargeting, Pattern A/B/C independence, note/chord editing, save/load, undo/redo, realtime playback, WAV/stem/MIDI export, Beat Readiness, Beat Map, Production Snapshot, Next Move, and Handoff semantics; and avoid sampling, imported audio, remote AI, remote analysis, plugin hosting, music-theory guarantees, genre-authenticity claims, accounts, analytics, or cloud sync.

Key Compass Focus work must derive focus targets, the default readout card, the Quick Actions Key Compass focus command, any direct Key Compass card Quick Actions, Focus Result labels, key metrics, audition cues, and next-check feedback only from existing Key Compass cards, existing Key Compass card ids, the current highest-priority key card, the visible summary, and the explicitly clicked or command-run card, show Focus Result only after explicit visible or command focus, keep focused card state, readout state, and result feedback UI-local and out of saved project schema and undo history, route clicks and command runs only to the existing Compose panel through the existing focus handler, preserve Key Compass card derivation, card order, card scoring, visible focus buttons, key retargeting, scale definitions, selected note/chord state, Pattern A/B/C musical events, direct note/chord editing, recommendations, playback, exports, and Handoff state, and avoid auto-writing, autoplay, command chains, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Key Compass result metric work must derive the explicit harmony focus action, active or direct harmony lane, destination panel, lane context, current project key, selected Pattern, editable event count, and song length only from current Key Compass command title/detail plus local project state after the existing Key Compass focus handler; preserve Key Compass card derivation, card order, card scoring, focus-card selection, visible focus behavior, direct card command definitions, key retargeting, scale definitions, selected note/chord state, Pattern A/B/C musical events, direct note/chord editing, Composer Guide, Groove Compass, Guide Quick Start, First Beat Path, Beat Spine, Mode Focus, Session Pass, Quick Actions filtering, command search, Pinned Commands, Recent Commands, project data, playback, save/load, WAV/stem/MIDI export, Handoff Sheet, Handoff Pack, and local draft behavior; avoid onboarding overlays, tutorials, macros, command chains, hidden generation, auto-run, autoplay, automatic key rewriting, auto-save, auto-export, sampling, imported audio, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Key Compass Command Reference work must mark the Guide row as `Quick Actions / Readout`, reflect only the existing local scale, cadence, chord, 808/bass, melody, selected-note, and selected-chord diagnostics, Focus readout, Key Compass focus command, direct card commands, and local Focus Result feedback; preserve Key Compass card derivation, card order, Focus readout derivation, focus routing, Quick Actions execution, card scoring, recommendations, key retargeting, scale definitions, selected note/chord state, Pattern A/B/C musical events, direct note/chord editing, project data, playback, render/export, project schema, save/load, privacy boundaries, snapshots, Composer Actions, and Handoff state; and avoid auto-writing, autoplay, tutorials, macros, command chains, hidden generation, automatic key rewriting, automatic note correction, media uploads, reference-track import or analysis, sampling, imported audio, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Groove Compass work must derive selected-pattern drum density, kick/clap anchor, hat motion, timing feel, chance posture, pocket balance, and selected drum focus only from local Pattern A/B/C drum pattern, velocity, microtiming, probability, and hat repeat data; remain read-only; avoid mutating project state, changing recommendations, applying drum edits, triggering playback, or triggering exports; preserve Pattern A/B/C independence, drum editing, save/load, undo/redo, realtime playback, WAV/stem/MIDI export, Beat Readiness, Beat Map, Production Snapshot, Key Compass, Next Move, and Handoff semantics; and avoid sampling, imported audio, remote AI, remote analysis, plugin hosting, groove correctness guarantees, accounts, analytics, or cloud sync.

Groove Compass Focus work must derive focus targets, the default readout card, the Quick Actions Groove Compass focus command, any direct Groove Compass card Quick Actions, Focus Result labels, groove metrics, audition cues, and next-check feedback only from existing Groove Compass cards, existing Groove Compass card ids, the current highest-priority groove card, the visible summary, and the explicitly clicked or command-run card, show Focus Result only after explicit visible or command focus, keep focused card state, readout state, and result feedback UI-local and out of saved project schema and undo history, route clicks and command runs only to the existing Compose panel through the existing focus handler, preserve Groove Compass card derivation, card order, card scoring, visible focus buttons, selected drum state, Pattern A/B/C drum data, direct drum editing, recommendations, playback, exports, Groove Compass Cue behavior, and Handoff state, and avoid auto-writing, autoplay, command chains, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Groove Compass result metric work must derive the explicit pocket focus action, active or direct groove lane, destination panel, lane context, selected Pattern, editable event count, drum hit count, and song length only from current Groove Compass command title/detail plus local project state after the existing Groove Compass focus handler; preserve Groove Compass card derivation, card order, card scoring, focus-card selection, visible focus behavior, direct card command definitions, selected drum state, Pattern A/B/C drum data, direct drum editing, Groove Compass Cue behavior, cue routing, Key Compass, Composer Guide, Guide Quick Start, First Beat Path, Beat Spine, Mode Focus, Session Pass, Quick Actions filtering, command search, Pinned Commands, Recent Commands, project data, playback, save/load, WAV/stem/MIDI export, Handoff Sheet, Handoff Pack, and local draft behavior; avoid onboarding overlays, tutorials, macros, command chains, hidden generation, auto-run, autoplay, automatic drum edits, auto-save, auto-export, sampling, imported audio, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Groove Compass Cue work must derive the visible Cue control, Quick Actions Groove Compass Cue command, result metric, audition cue, and next-check text only from selected Pattern, existing Groove Compass summary, existing transport loop scope, and local before/after project state; route visible Cue clicks and command runs only through existing selected Pattern plus Pattern loop transport state; keep cue state out of saved project schema and undo history; preserve Groove Compass derivation/card order/focus behavior, selected drum state, Pattern A/B/C event data, arrangement data, playback scheduling, save/load, WAV/stem/MIDI export, and Handoff semantics; and avoid autoplay, auto-fixing groove, hidden generation, command chains, sampling, imported audio, sampler devices, remote AI, accounts, analytics, or cloud sync.

Groove Compass Command Reference work must mark the Guide row as `Quick Actions / Readout`, reflect only the existing local density, anchors, hat motion, timing, chance, pocket balance, selected-drum diagnostics, Focus readout, Groove Compass focus command, direct card commands, Groove Compass cue command, and local Focus/Cue Result feedback; preserve Groove Compass card derivation, card order, Focus readout derivation, focus routing, cue routing, Quick Actions execution, card scoring, recommendations, selected drum state, Pattern A/B/C drum data, arrangement data, direct drum editing, project data, playback scheduling, render/export, project schema, save/load, privacy boundaries, snapshots, Composer Actions, and Handoff state; and avoid auto-writing, autoplay, tutorials, macros, command chains, hidden generation, auto-fixing groove, automatic drum edits, media uploads, reference-track import or analysis, sampling, imported audio, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Composer Guide work must derive drums, 808/bass, harmony, melody, arrangement, and finish posture only from local selected Pattern A/B/C event data, arrangement blocks, selected Delivery Target, Beat Readiness, deterministic export analysis, and deterministic stem analysis; remain read-only; avoid mutating project state, changing recommendations, applying musical edits, triggering playback, or triggering exports; preserve Pattern A/B/C independence, editing, save/load, undo/redo, realtime playback, WAV/stem/MIDI export, Beat Readiness, Beat Map, Production Snapshot, Key Compass, Groove Compass, Next Move, and Handoff semantics; and avoid sampling, imported audio, remote AI, remote analysis, plugin hosting, songwriting guarantees, accounts, analytics, or cloud sync.

Composer Guide Focus work must derive focus targets, the visible Focus Readout action target, Focus Readout destination, metric, audition, next-check metadata, matching Focus Readout action title/aria context, matching card Focus action title/aria context, the Quick Actions Composer Guide focus command detail, any direct Composer Guide card Quick Actions detail, Focus Result labels, guide metrics, audition cues, and next-check feedback only from existing Composer Guide card ids, existing Composer Guide cards, and the current highest-priority guide card; keep focused card state, visible Focus Readout action and metadata state, readout/card/command action label state, and Focus Result feedback UI-local and out of saved project schema and undo history; route Focus Readout actions, card clicks, and command runs only to existing Compose, Arrange, or Master panels through the existing focus handler; show Focus Result only after explicit visible or command focus; preserve Composer Guide card derivation, card order, visible focus buttons, Focus Readout metric/audition metadata plus readout/card/command action context, guide scoring, recommendations, project musical data, arrangement, mixer, master, exports, playback, save/load, undo/redo, Composer Actions, and Handoff state; and avoid auto-writing, autoplay, macros, command chains, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Composer Guide result metric work must derive the explicit guide focus action, active or direct writing lane, destination panel, command detail context, guide metric, audition cue, next-check feedback, current mode, selected Pattern, editable event count, and song length only from current Composer Guide command title/detail plus local project state after the existing Composer Guide focus handler; preserve Composer Guide card derivation, card order, focus-card selection, visible focus behavior, direct card command definitions and detail context, focus routing, Guide Quick Start, First Beat Path, Beat Spine, Mode Focus, Session Pass, Composer Actions, Quick Actions filtering, command search, Pinned Commands, Recent Commands, project data, playback, save/load, WAV/stem/MIDI export, Handoff Sheet, Handoff Pack, and local draft behavior; avoid onboarding overlays, tutorials, macros, command chains, hidden generation, auto-run, autoplay, auto-save, auto-export, sampling, imported audio, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Composer Guide Command Reference work must mark the Guide row as `Quick Actions / Readout`, reflect only the existing local drums, 808/bass, harmony, melody, arrangement, and finish posture cards, Focus readout metric/audition metadata/action context, card Focus action context, Composer Guide focus command detail context, direct card command detail context, and local Focus Result feedback; preserve Composer Guide card derivation, card order, Focus readout metric/audition metadata plus readout/card/command action context derivation, focus routing, Quick Actions execution, guide scoring, recommendations, project musical data, arrangement, mixer, master, playback, render/export, project schema, save/load, privacy boundaries, snapshots, Composer Actions, and Handoff state; and avoid auto-writing, autoplay, tutorials, macros, command chains, hidden generation, media uploads, reference-track import or analysis, sampling, imported audio, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Mode Focus work must derive Guided/Studio orientation, active card labels, read-only Decision Readout labels, and the visible Decision Readout action target only from local project state and existing Composer Guide, Beat Map, Review Queue, Finish Checklist, and Mode Focus card summaries; keep Decision Readout state and visible Decision Readout action state UI-local and out of saved project schema; remain project-read-only except explicit jump navigation through the documented Mode Focus jump path; avoid mutating project state, changing recommendations, changing Mode Focus card order or scoring, applying edits, triggering playback, save, or export; preserve mode save/load semantics, all existing controls, realtime playback, WAV/stem/MIDI export, and Handoff semantics; and avoid sampling, imported audio, remote AI, remote analysis, plugin hosting, accounts, analytics, or cloud sync.

Mode Focus Jump work must derive jump targets, the visible Decision Readout action, the Quick Actions Mode Focus jump command, any direct Mode Focus card Quick Actions, Jump Result labels, mode metrics, audition cues, and next-check feedback only from existing Mode Focus cards, Beat Map stage ids, Composer Guide focus targets, Finish Checklist focus targets, Review Queue focus targets, and the current Guided or Studio mode; route visible Decision Readout action clicks, card clicks, and command runs only to existing Compose, Arrange, Mix, Master, or Deliver panels through the existing jump handler; show Jump Result only after explicit visible or command jumps; keep jump state, visible Decision Readout action state, and Jump Result state UI-local and out of saved project schema; preserve Mode Focus scoring, Composer Guide scoring, Beat Map scoring, Finish Checklist scoring, Review Queue priority, Workflow Navigator behavior, project data, playback, save/load, undo/redo, render/export, Quick Actions, and Handoff semantics; and avoid onboarding overlays, tutorials, macros, command chains, auto-run, autoplay, auto-save, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Mode Focus result metric work must derive the explicit orientation action, active or direct orientation card, destination panel, card context, current Guided/Studio mode, selected Pattern, editable event count, and song length only from the current `mode-focus-jump` or `mode-focus-card-*` command title/detail plus local project state after the existing Mode Focus jump handler; preserve Mode Focus card derivation, scoring, card order, active-card selection, Decision Readout behavior, visible jump behavior, direct card command definitions, jump routing, Composer Guide, Beat Map, Review Queue, Finish Checklist, Workflow Navigator, Session Pass, First Beat Path, Quick Actions filtering, command search, Pinned Commands, Recent Commands, project data, playback, save/load, WAV/stem/MIDI export, Handoff Sheet, Handoff Pack, and local draft behavior; and avoid changing orientation recommendations, auto-running commands, macros, command chains, onboarding overlays, tutorials, autoplay, hidden generation, auto-save, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Mode Focus Command Reference work must mark Mode Focus as `Quick Actions / Readout` only when it reflects existing Mode Focus orientation, Decision Readout action, Quick Actions Mode Focus jump routing, direct Mode Focus card commands, and local Jump Result feedback; it must not change Mode Focus card derivation, scoring, card order, Decision Readout derivation, jump routing, Quick Actions execution, project data, playback scheduling, render/export, project schema, save/load, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Mode Switch Result work must route Guided and Studio top-button clicks plus direct Quick Actions `mode-switch-guided` and `mode-switch-studio` commands only through the shared project mode switch handler; mutate only `project.mode`; derive UI-local result title, status, before/after mode metric, audition cue, and next-check text only from before/after project mode plus existing Mode Focus, Session Pass, and First Beat Path summaries; keep result state out of saved project schema and undo history beyond the explicit mode change; preserve Mode Focus scoring, Session Pass scoring, First Beat Path scoring, Workflow Navigator behavior, musical events, arrangement, mixer/master, playback, save/load, WAV/stem/MIDI export, Handoff behavior, and all sampling boundaries; and avoid onboarding overlays, tutorials, macros, command chains, auto-run, autoplay, auto-save, auto-export, imported audio, sampler setup, remote AI, accounts, analytics, or cloud sync.

Composer Actions work must derive action buttons, direct Composer Actions Quick Actions, style priorities, thresholds, copy, scope previews, impact previews, undo posture, post-click result metrics, audition cues, and next checks only from existing Composer Actions, local selected Pattern A/B/C event data, Composer Guide posture, selected style profile, Beat Readiness, arrangement blocks, selected Delivery Target, deterministic export analysis, deterministic stem analysis, current master state, and explicit local preset definitions; run only after explicit user clicks or command selections; keep previews, result strips, audition cues, and next checks informational rather than modal confirmations, autoplay, or export triggers; keep result strips UI-only and out of saved project data; route mutating buttons and direct Composer Actions Quick Actions only through the existing Composer Actions run handler and existing undoable Drum Foundation, 808 Bassline, Chord Progression, Melody Motif, Pattern Fill, Pattern Chain, arrangement template, Beat Blueprint, and Master Finish handlers; preserve read-only guide surfaces, Pattern A/B/C independence, save/load, undo/redo, realtime playback, WAV/stem/MIDI export, Beat Readiness, Beat Map, Production Snapshot, Key Compass, Groove Compass, Next Move, and Handoff semantics; and avoid sampling, imported audio, remote AI, remote analysis, hidden generation, plugin hosting, songwriting guarantees, genre-authenticity claims, accounts, analytics, or cloud sync.

Quick Actions Composer Actions result metric work must derive the explicit writing move, writing area, route, scope, impact, undo posture, selected Pattern, editable event count, Pattern A/B/C usage, drum hit count, 808/bass note count, harmony chord count, melody note count, selected arrangement block, arrangement block count, song length, export readiness, style-goal posture, audition cue, and next composer-action check only from the current Composer Action command id/title/detail plus local Composer Actions, selected style profile, selected Pattern, Pattern A/B/C event data, arrangement, selected-block, and export state after the existing Composer Actions handler; preserve Composer Actions derivation, style priority scoring, action ordering, direct command generation, Composer Actions run handling, Drum Foundation, 808 Bassline, Chord Progression, Melody Motif, Pattern Fill, Pattern Chain, arrangement template, Beat Blueprint, Master Finish apply paths, Pattern A/B/C independence, project data, playback scheduling, save/load, undo/redo, render/export, privacy boundaries, and sampling boundaries; and avoid auto-writing, command chains, hidden generation, autoplay, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Composer Actions Command Reference work must mark the Create row as `Quick Actions / Readout`, reflect only the existing style-aware writing moves, inline scope/impact/undo previews, direct Composer Action commands, and local Composer Action Result feedback; preserve Composer Actions derivation, style priority scoring, thresholds, action copy, scope previews, impact previews, undo posture, direct command generation, result metrics, audition cues, next-check text, Composer Actions run handling, Drum Foundation, 808 Bassline, Chord Progression, Melody Motif, Pattern Fill, Pattern Chain, arrangement template, Beat Blueprint, Master Finish apply paths, Pattern A/B/C independence, Composer Guide, Beat Readiness, Beat Map, Production Snapshot, Key Compass, Groove Compass, Next Move, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid sampling, imported audio, remote AI, remote analysis, hidden generation, plugin hosting, songwriting guarantees, genre-authenticity claims, accounts, analytics, cloud sync, macros, command chains, autoplay, or auto-export.

Beat Blueprint work must apply deterministic sample-free project starts from local style, key, BPM, Pattern A/B/C event data, arrangement templates, sound presets, mixer balance, and master presets; keep the catalog covering every supported `styleProfiles` entry with at least one dedicated Blueprint; keep the result fully editable and undoable; preserve title, mode, metronome, save/load, playback, WAV/stem/MIDI export semantics; and avoid remote AI, imported audio, sampling, plugin hosting, hidden randomness, or hidden assets.

Beat Blueprint Preview work must derive preview/readout text, any Preview Decision Readout, visible Preview Decision action targets, any Preview Listening Cue, and visible Preview Listening Cue action targets only from existing local Beat Blueprint metadata, current-style match summary, visible preview metrics, current transport loop state, and current project state; keep preview selection, decision labels, visible Preview Decision action state, listening-cue labels, and visible Preview Listening Cue action state UI-local and out of saved project schema; route visible Preview Decision apply actions only through the existing Beat Blueprint apply path, visible Preview Decision compare actions only through existing Beat Blueprint preview state, and visible Preview Listening Cue actions only through existing transport loop selection while playback is stopped; avoid mutating project data until an explicit Apply path runs; preserve Quick Actions, Next Move, undo/redo, save/load, playback, WAV/stem/MIDI export semantics, and avoid modal confirmations, hidden generation, autoplay, auto-save, remote AI, imported audio, sampling, plugin hosting, accounts, analytics, or cloud sync.

Beat Blueprint Style Match work must derive the current-style starter only from local `project.styleId`, `styleProfiles`, `beatBlueprints`, and existing Blueprint preview summary logic; keep the match strip UI-local and out of saved project schema and undo history; route Preview only through the existing preview selection state and Apply only through the existing undoable Beat Blueprint apply path; preserve the full Blueprint list, Preview, Result, Next Move, Quick Actions, save/load, playback, WAV/stem/MIDI export semantics; and avoid auto-applying on style change, filtering away other styles, sampling, imported audio, hidden generation, remote AI, accounts, analytics, or cloud sync.

Beat Blueprint Result work must update result labels only after explicit Beat Blueprint Apply clicks, derive applied blueprint, full-project starter scope, before/after style, key, BPM, arrangement, sound, and master posture, changed posture impact, audition cue, and next-check text only from local before/after project state and existing Beat Blueprint metadata; Quick Actions Blueprint result metrics must derive apply feedback only from existing Blueprint metadata plus local before/after project state, and preview feedback only from existing Blueprint metadata without implying project edits; keep result state UI-local and out of saved project schema and undo history; preserve Beat Blueprint Preview, Beat Blueprint definitions and apply behavior, style templates, musical event generation, arrangement templates, sound presets, mixer/master algorithms, Quick Actions, Next Move, undo/redo, save/load, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid modal confirmations, autoplay, auto-save, auto-export, hidden generation, remote AI, imported audio, sampling, audio clips, plugin hosting, accounts, analytics, or cloud sync.

Quick Actions Beat Blueprint Preview result metric work must identify only explicit Preview, Preview Decision, and Preview Listening Cue command context, blueprint metadata, Song/Pattern cue posture, style/key/BPM/arrangement/sound/master starter posture, selected Pattern, editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, and next preview/apply check from local command metadata plus current project state; preserve Beat Blueprint preview/apply routing, generated Pattern A/B/C events, arrangement templates, sound presets, mixer/master algorithms, project schema, playback, render/export, Handoff, privacy boundaries, and sampling boundaries; and avoid auto-apply, command chains, hidden generation, autoplay, imported audio, remote AI, accounts, analytics, or cloud sync.

Delivery Target work must store the selected fixed or custom target in local project state, migrate older files to a safe default target and bounded default custom target, keep target selection and custom target editing separate from target alignment, route custom edits and alignment changes through explicit undoable project update paths, preserve Pattern A/B/C event data, save/load, snapshots, realtime playback, WAV/stem/MIDI export, Beat Readiness, Beat Map, Next Move, Handoff Sheet, and Mix Coach semantics, and avoid platform compliance, LUFS/true-peak claims, professional mastering guarantees, sampling, imported audio, plugin hosting, remote AI, remote analysis, hidden automation, accounts, analytics, or cloud sync.

Quick Actions Delivery Target select work must derive commands only from local fixed `deliveryTargets`, the current bounded custom target, and existing Delivery Target metadata; route command runs only through the existing target-selection handler, not the alignment handler; disable only the currently selected target; derive result metrics and follow-up text from the selected local Delivery Target and before/after project state; preserve custom target editing, Delivery Target Alignment Preview/Result, Quick Actions Delivery Target Align, Beat Map, Session Brief, Handoff Pack, Export Preflight, save/load, snapshots, undo/redo, playback, WAV/stem/MIDI export, and saved project schema beyond the existing selected target field; and avoid auto-align, auto-export, autoplay, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Delivery Target select result metric work must identify only the explicit target-select action, selected target, command target, target focus, target length, preferred arrangement template, master posture, mix posture, stem expectation, selected Pattern, editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, Session Brief context, package readiness, and next target-alignment check from current Delivery Target select command title/detail plus local project state; preserve target selection behavior, custom target editing, Delivery Target Alignment Preview/Result, Quick Actions Delivery Target Align, alignment execution, arrangement templates, mixer/master update paths, direct export command ids, export handlers, render bytes, MIDI bytes, Handoff Sheet contents, file names, download behavior, Handoff Pack, Handoff Package Check, Export Preflight, project schema, privacy boundaries, and sampling boundaries.

Delivery Target Alignment Preview work must derive preview labels and any Quick Actions Delivery Target Align command target only from local project state and the selected active Delivery Target; stay UI-local and out of saved project schema; show target fit, bar length, master preset, mix posture, and stem expectation before Align; route Align clicks and any command runs only through the existing Delivery Target alignment handler; preserve target selection, custom editing, alignment behavior, arrangement templates, mixer/master changes, save/load, snapshots, undo/redo, playback, WAV/stem/MIDI export, Beat Readiness, Beat Map, Next Move, Handoff Sheet, Handoff Pack, and Mix Coach semantics; and avoid modal confirmations, auto-align, auto-export, sampling, imported audio, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync.

Delivery Target Alignment Result work must update result labels only after explicit Align clicks or Quick Actions Delivery Target Align command runs, derive target, editable arrangement/master/mix scope, before/after target, length, master, mix, and stem posture, changed-alignment impact, audition cue, and next-check text only from local before/after project state, selected active Delivery Target, and existing target definitions; keep result state UI-local and out of saved project schema and undo history; preserve Delivery Target Alignment Preview, target selection, custom editing, alignment behavior, arrangement templates, mixer/master changes, save/load, snapshots, undo/redo, playback, WAV/stem/MIDI export, Beat Readiness, Beat Map, Next Move, Export Preflight, Handoff Sheet, Handoff Pack, and Mix Coach semantics; and avoid modal confirmations, auto-align, autoplay, auto-export, sampling, imported audio, plugin hosting, remote AI, remote analysis, accounts, analytics, platform compliance, publishing/licensing claims, LUFS/true-peak guarantees, or cloud sync.

Quick Actions Delivery Target Align result metric work must identify only the explicit alignment action, Deliver destination, selected target, target-fit posture, arrangement length, master posture, mix posture, stem expectation, selected Pattern, editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, Session Brief context, handoff/package readiness, and next delivery check from current Delivery Target Align command title/detail plus local project state; preserve target selection, custom target editing, preview derivation, alignment result labels, alignment execution, arrangement templates, mixer/master update paths, direct export command ids, export handlers, render bytes, MIDI bytes, Handoff Sheet contents, file names, download behavior, Handoff Pack, Handoff Package Check, Export Preflight, project schema, privacy boundaries, and sampling boundaries.

Delivery Target Alignment Command Reference work must mark the Deliver row as `Quick Actions / Readout`, reflect only the existing target-fit preview, explicit Delivery Target Align command, post-align Result metrics, audition cue, and Export Preflight/Handoff Pack follow-up; preserve target selection, custom target editing, preview derivation, alignment result labels, alignment execution, arrangement templates, mixer/master update paths, save/load, snapshots, undo/redo, playback, render/export, project schema, privacy boundaries, and sampling boundaries; and avoid modal confirmations, auto-align, auto-export, autoplay, publishing/licensing claims, LUFS/true-peak guarantees, sampling, imported audio, sampler devices, remote AI, accounts, analytics, or cloud sync.

Session Brief work must store bounded artist, vibe, reference, and notes text in local project state, migrate older files to empty defaults, preserve save/load, snapshots, undo/redo, realtime playback, WAV/stem/MIDI export, Beat Readiness, Beat Map, Next Move, Mix Coach, and Delivery Target semantics, and avoid media uploads, copyrighted reference audio, remote AI, remote analysis, collaboration, accounts, analytics, cloud sync, sampling, imported audio, plugin hosting, hidden automation, or compliance claims.

Session Brief role readout work must derive only from local artist, vibe, reference, and notes fields; stay UI-local and out of saved project schema; preserve Session Brief editing, clear behavior, save/load, snapshots, undo/redo, Handoff Sheet, Handoff Pack, realtime playback, WAV/stem/MIDI export, Beat Readiness, Beat Map, Next Move, Mix Coach, and Delivery Target semantics; and avoid media uploads, copyrighted reference audio handling, remote AI, remote analysis, collaboration services, accounts, analytics, cloud sync, sampling, imported audio, plugin hosting, hidden automation, or compliance claims.

Session Brief Compass work must derive direction, reference, artist/vocal context, and handoff readiness cards only from local Session Brief text, selected Delivery Target, style, key, BPM, arrangement length, deterministic export analysis, and deterministic stem analysis; stay read-only and UI-local except for existing manual Session Brief edits and starter pad clicks; preserve Session Brief editing, clear behavior, Starter Pads, Handoff Sheet, Handoff Pack, Export Preflight, save/load, snapshots, undo/redo, realtime playback, WAV/stem/MIDI export, Beat Readiness, Beat Map, Next Move, Mix Coach, and Delivery Target semantics; and avoid media uploads, reference-track import or analysis, copyrighted reference audio handling, remote AI, remote analysis, collaboration services, accounts, analytics, cloud sync, sampling, imported audio, plugin hosting, hidden automation, or compliance claims.

Session Brief Compass Focus work must derive focused card state, field/Handoff focus targets, the Quick Actions Brief Compass focus command, direct Brief Compass card Quick Actions, Focus Result labels, brief metrics, audition cues, and next-check feedback only from existing Session Brief Compass cards, local Session Brief fields, existing field refs, the visible Session Brief Compass summary, the explicitly clicked or command-run card, the current highest-priority brief compass card, and the existing Handoff/Deliver panel ref; show Focus Result only after explicit visible or command focus; keep focused card state and Focus Result feedback UI-local and out of saved project schema and undo history; route button clicks and command runs only to the matching brief input or Handoff area without mutating project data; preserve Session Brief Compass derivation, manual editing, clear behavior, Starter Pads, Handoff Sheet, Handoff Pack, Export Preflight, save/load, snapshots, undo/redo, realtime playback, WAV/stem/MIDI export, Beat Readiness, Beat Map, Next Move, Mix Coach, and Delivery Target semantics; and avoid auto-writing brief fields, media uploads, reference-track import or analysis, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Session Brief Compass Command Reference work must mark the Guide row as `Quick Actions / Readout`, reflect only the existing local direction, reference, artist/vocal context, handoff readiness cards, Brief Compass focus command, direct card commands, and local Focus Result feedback; preserve brief derivation, manual editing, clear behavior, Starter Pads, focus routing, Quick Actions execution, project data, playback, render/export, project schema, save/load, privacy boundaries, and snapshots; and avoid auto-writing brief fields, media uploads, reference-track import or analysis, copyrighted reference audio handling, sampling, imported audio, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Quick Actions Session Brief Compass result metric work must identify only the explicit compass focus action, active or direct brief lane, destination field or Handoff area, lane status/context, current brief field count, artist/vibe/reference/notes posture, selected Delivery Target, target focus, selected Pattern, editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, package readiness, and next brief/handoff check from current Brief Compass command title/detail plus local project state; preserve Brief Compass card derivation, card order, focus-target selection, focus routing, Focus Result behavior, manual Session Brief editing, clear behavior, Session Brief Starter, Handoff Sheet contents, Handoff Pack, Handoff Package Check, Export Preflight, direct export command ids, export handlers, render bytes, MIDI bytes, file names, download behavior, project schema, privacy boundaries, and sampling boundaries.

Reference Alignment work must derive reference, direction, form, mix, listen cue, handoff cards, Focus Result labels, alignment metrics, audition cues, and next-check feedback only from existing Session Brief, Delivery Target, arrangement, Beat Readiness, deterministic export analysis, deterministic stem analysis, the visible Reference Alignment summary, and the explicitly clicked or command-run card; show Focus Result only after explicit visible or command focus; keep focused card state and Focus Result feedback UI-local and out of saved project schema and undo history; route visible Focus buttons, the Quick Actions Reference Alignment focus command, and direct Reference Alignment card commands only to existing brief fields, Arrange, Master, or Deliver panels; preserve Session Brief editing, Brief Compass, Listening Pass, Production Snapshot, Handoff Pack, Handoff Sheet, save/load, snapshots, undo/redo, realtime playback, WAV/stem/MIDI export, and project schema; and avoid reference audio import, waveform matching, audio analysis, autoplay, sampling, imported audio, sampler devices, remote AI, accounts, analytics, or cloud sync.

Reference Alignment Command Reference work must mark the Guide row as `Quick Actions / Readout`, reflect only the existing local written-reference fit, direction, form, mix posture, listen cue, handoff readiness cards, Reference Alignment focus command, direct card commands, and local Focus Result feedback; preserve Reference Alignment card derivation, card order, focus derivation, focus routing, Quick Actions execution, result labels, Session Brief editing, Delivery Target, arrangement, Beat Readiness, deterministic export/stem analysis, Listening Pass, Production Snapshot, Handoff Pack, Handoff Sheet, save/load, snapshots, undo/redo, realtime playback, render/export, project schema, privacy boundaries, and sampling boundaries; and avoid reference audio import, waveform matching, audio analysis, autoplay, tutorials, macros, command chains, sampling, imported audio, sampler devices, remote AI, accounts, analytics, or cloud sync.

Quick Actions Reference Alignment result metric work must identify only the explicit reference focus action, active or direct alignment lane, destination panel, lane status/context, current brief field count, artist/vibe/reference/notes posture, selected Delivery Target, target focus, selected Pattern, editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, stem readiness, package readiness, and next listening/handoff check from current Reference Alignment command title/detail plus local project state; preserve Reference Alignment card derivation, card order, focus-target selection, focus routing, Focus Result behavior, manual Session Brief editing, Brief Compass, Session Brief Starter, Listening Pass, Handoff Sheet contents, Handoff Pack, Handoff Package Check, Export Preflight, direct export command ids, export handlers, render bytes, MIDI bytes, file names, download behavior, project schema, privacy boundaries, and sampling boundaries; and avoid reference-track upload, waveform matching, audio analysis, sampling, imported audio, sampler devices, remote AI, accounts, analytics, or cloud sync.

Session Brief Starter Pad work must derive starter text only from local project title, style, key, BPM, arrangement length, Delivery Target, stem goal, and mix posture; fill only blank artist, vibe, reference, and notes fields after explicit user clicks through undoable project history; keep result state UI-local and out of saved project schema; preserve manual Session Brief editing, clear behavior, Handoff Sheet, Handoff Pack, Export Preflight, save/load, snapshots, undo/redo, realtime playback, WAV/stem/MIDI export, Beat Readiness, Beat Map, Next Move, Mix Coach, and Delivery Target semantics; and avoid remote AI, remote analysis, media uploads, reference-track import, copyrighted reference audio handling, collaboration services, accounts, analytics, cloud sync, sampling, imported audio, plugin hosting, hidden automation, or compliance claims.

Quick Actions Session Brief Starter work must expose every `sessionBriefStarterPadDefinitions` pad as an explicit command-palette action routed only through the existing `applySessionBriefStarterPad` handler, preserve Session Brief Starter Result, Brief Compass, manual editing, clear behavior, bounded text, blank-field-only writes, save/load, snapshots, undo/redo, Handoff Sheet, Handoff Pack, Export Preflight, realtime playback, WAV/stem/MIDI export, and avoid new brief fields, starter presets, macros, command chains, autoplay, auto-export, media uploads, reference-track import or analysis, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Session Brief Starter result metric work must identify only the explicit starter action, starter pad, changed or already-covered blank-field posture, current brief field count, artist/vibe/reference/notes posture, selected Delivery Target, target focus, selected Pattern, editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, package readiness, and next handoff check from current Session Brief Starter command title/detail plus local project state; preserve starter pad definitions, starter text generation, blank-field-only writes, manual Session Brief editing, clear behavior, Brief Compass, Handoff Sheet contents, Handoff Pack, Handoff Package Check, Export Preflight, direct export command ids, export handlers, render bytes, MIDI bytes, file names, download behavior, project schema, privacy boundaries, and sampling boundaries.

Key retarget work must update Pattern A/B/C 808/bass note pitches, melody note pitches, and chord roots by scale degree, preserve event timing, length, velocity, glide, and chance fields, remain undoable, and avoid imported audio or sampling dependencies.

Chord progression work must preserve Pattern A/B/C independence, keep chord roots and progression presets scale-aware by default, migrate older project files without chord events, keep add/delete operations undoable, preserve at least one chord event in guided editing, and make chord events audible in both realtime playback and WAV export.

Chord Pad work must derive pad roots and qualities from the current key, update only the selected chord event after an explicit click through existing undoable chord update paths, preserve step/length/velocity/chance unless directly edited, keep chord events editable local musical data, and avoid sampling, imported audio, hidden generation, remote AI, accounts, analytics, or cloud sync.

Chord Rhythm Pad work must derive chord length, velocity, and chance changes from explicit local rhythm presets, update only the selected Pattern A/B/C chord event data through undoable project history, keep chord counts, steps, roots, qualities, and inversions stable, keep results manually editable through the Chord Editor, preserve realtime playback plus WAV/stem/MIDI export semantics, and avoid sampling, imported audio, hidden generation, remote AI, accounts, analytics, or cloud sync.

Chord Voicing Pad work must derive chord quality, inversion, length, velocity, and chance changes from explicit local voicing presets, update only the selected Pattern A/B/C chord event through existing undoable chord update paths, preserve chord step/root and chord event counts, keep results manually editable through the Chord Editor, preserve realtime playback plus WAV/stem/MIDI export semantics, and avoid sampling, imported audio, hidden generation, remote AI, accounts, analytics, or cloud sync.

Chord Move Preview work must derive selected chord labels, harmonic role, suggested pad/rhythm/voicing targets, any Quick Actions Chord Move command target, and pre-click move counts only from current local key, selected Pattern A/B/C chord event data, and existing Chord Pad/Rhythm/Voicing option targets, keep the preview UI-local and out of saved project schema and undo history, route pad clicks and any command runs only through existing Chord Pad, Chord Rhythm, or Chord Voicing apply behavior, preserve Chord Pad, Chord Rhythm, and Chord Voicing definitions and apply behavior, progression presets, selected-chord edit tools, Pattern A/B/C independence, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics, and avoid automatic chord writing, hidden generation, sampling, imported audio, remote AI, plugin hosting, accounts, analytics, or cloud sync.

Chord Move Result work must update result labels only after explicit Chord Pad, Chord Rhythm Pad, or Chord Voicing Pad clicks or Quick Actions Chord Move command runs, derive applied move, selected Pattern A/B/C scope, before/after chord count, root/quality, inversion, rhythm, velocity, and chance posture, changed-move impact, audition cue, and next-check text only from local before/after selected Pattern A/B/C chord event data and existing Chord Pad/Rhythm/Voicing definitions, keep result state UI-local and out of saved project schema and undo history, preserve Chord Move Preview, Chord Pad, Chord Rhythm, and Chord Voicing definitions and apply behavior, progression presets, selected-chord edit tools, Pattern A/B/C independence, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics, and avoid modal confirmations, autoplay, automatic chord writing, hidden generation, sampling, imported audio, remote AI, plugin hosting, accounts, analytics, or cloud sync.

Chord Move Command Reference work must mark the Create row as `Quick Actions / Readout`, reflect only the existing local Chord Move Preview, current Chord Pads/Rhythm/Voicing target, direct Chord Move command, and local Chord Move Result feedback; preserve preview derivation, target selection, Chord Pad/Rhythm/Voicing definitions and apply behavior, result metric derivation, Pattern A/B/C event data outside explicit Chord Move actions, selected-chord edit tools, arrangement, mixer, master, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid new chord algorithms, tutorials, macros, command chains, hidden generation, automatic arrangement, automatic export, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Chord velocity meter work must derive visible chord slot velocity meters and labels only from existing `ChordEvent.velocity`, keep chord chance badges, inversion labels, selected-chord state, playing state, chord controls, clipboard, and editor audition unchanged, stay UI-only with no project schema, save/load, playback, render, stem export, MIDI export, Handoff, sampling, recording, remote AI, or cloud scope changes, and keep text contained inside compact chord slots.

Chord inversion work must migrate older chord events to root position, preserve per-chord inversion data across Pattern A/B/C copy, save/load, undo/redo, realtime playback, WAV export, and stem export, and keep inversions as editable musical event data rather than imported audio.

Selected chord edit tool work must keep chord move, copy, paste, duplicate, beat duplicate, previous beat duplicate, delete, step reset, root reset, quality reset, inversion reset, inversion, length reset, length, velocity, chance, audition, and any Quick Actions selected-chord step-reset/duplicate-beat/duplicate-previous-beat/root/root-reset/quality-reset/quality/inversion-reset/length-reset/length/velocity/chance/delete/edit/audition commands scoped to the selected Pattern A/B/C slot, derive selected-chord harmonic readouts only from local key and chord root/quality/inversion fields, route Quick Actions selected-chord edit commands only through existing selected-chord move, copy, paste, duplicate, beat duplicate, previous beat duplicate, delete, step, root, quality, inversion reset, inversion, length, velocity, and chance handlers, with step reset reusing the existing chord event update path, beat duplicate and previous beat duplicate reusing the existing undoable selected-pattern chord insertion path, root reset reusing the existing selected-chord root update path, quality reset reusing the existing selected-chord quality update path, and length reset reusing the existing selected-chord length update path, route selected-chord audition only through UI-local one-shot built-in synthesis, keep chord clipboard, audition, and harmonic readout state UI-local and out of saved project schema, keep selected-chord-copy UI-local until an explicit paste or duplicate writes project data, paste copied chords only into the next empty chord step, preserve root/quality/inversion/length/velocity/chance fields unless directly edited, avoid overlapping pasted, duplicate, beat-duplicate, previous-beat-duplicate, moved, or step-reset chord starts at the same step, preserve at least one chord event, keep paste/duplicate/beat duplicate/previous beat duplicate/move/delete/step reset/root reset/quality reset/inversion reset/length reset/length/velocity/chance actions undoable, and keep realtime playback, WAV/stem export, and MIDI export driven by editable chord event data rather than imported audio, sampling, remote AI, or hidden audio assets.

Selected chord step reset work must keep the selected-chord step reset command scoped to the selected Pattern A/B/C slot; derive the target only from the selected chord step, selected chord length, and existing 16-step grid; snap only to the nearest 4-step anchor that can preserve the selected chord length inside the grid; disable reset when no active selected chord exists, the selected chord already starts on the target anchor, or another chord already starts at the target step; route reset only through the existing chord event update path; set only the chord step; preserve root, quality, inversion, length, velocity, chance, copy/paste/duplicate/delete, playback, WAV/stem/MIDI export, and manual chord controls; keep the chord selected and undoable; and avoid reharmonization, progression replacement, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Selected chord beat duplicate work must keep the selected-chord beat duplicate command scoped to the selected Pattern A/B/C slot; derive the target only from the selected chord step, selected chord length, selected Pattern A/B/C chord events, and existing 16-step grid; duplicate only to the next later 4-step anchor that can preserve the selected chord length inside the grid and has no chord at the target step; enforce that 4-step anchor rule inside the handler as well as the Quick Actions target derivation; disable beat duplicate when no active selected chord exists or no target anchor is available; route the duplicate only through the existing undoable selected-pattern chord insertion path; preserve root, quality, inversion, length, velocity, chance, chord clipboard, playback, WAV/stem/MIDI export, and manual chord controls; select the duplicated chord after a successful edit; and avoid reharmonization, progression replacement, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Visible selected-chord next beat duplicate button work must keep the button scoped to the selected Pattern A/B/C slot, derive the same next later 4-step chord-grid target as the selected-chord beat duplicate command, disable when no valid target exists, and route only through the existing undoable selected-pattern chord insertion path without changing chord clipboard, playback, export, sampling scope, or project schema.

Selected chord previous beat duplicate work must keep the selected-chord previous beat duplicate command and visible selected-chord previous-beat button scoped to the selected Pattern A/B/C slot; derive the target only from the selected chord step, selected chord length, selected Pattern A/B/C chord events, and existing 16-step grid; duplicate only to the nearest earlier 4-step anchor that can preserve the selected chord length inside the grid and has no chord at the target step; enforce that 4-step anchor rule inside the handler as well as the Quick Actions and visible-button target derivation; disable previous beat duplicate when no active selected chord exists or no earlier target anchor is available; route the duplicate only through the existing undoable selected-pattern chord insertion path; preserve root, quality, inversion, length, velocity, chance, chord clipboard, playback, WAV/stem/MIDI export, and manual chord controls; select the duplicated chord after a successful edit; and avoid reharmonization, progression replacement, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Selected chord root reset work must keep the selected-chord root reset command scoped to the selected Pattern A/B/C slot; derive the target only from the current key's scale roots and use the first scale root as the tonic reset target; disable reset when no active selected chord exists, no key tonic root is available, or the selected chord already matches the tonic root; route reset only through the existing selected-chord root update path; set only the chord root; preserve quality, inversion, step, length, velocity, chance, copy/paste/duplicate/delete, playback, WAV/stem/MIDI export, and manual chord controls; keep the chord selected and undoable; and avoid reharmonization, progression replacement, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Selected chord quality reset work must keep the selected-chord quality reset command scoped to the selected Pattern A/B/C slot; derive the target only from the current key, the selected chord root, and the existing key-aware chord quality helper; disable reset when no active selected chord exists, the selected chord root is outside the current key scale roots, or the selected chord already matches the default quality; route reset only through the existing selected-chord quality update path; set only the chord quality; preserve root, inversion, step, length, velocity, chance, copy/paste/duplicate/delete, playback, WAV/stem/MIDI export, and manual chord controls; keep the chord selected and undoable; and avoid reharmonization, progression replacement, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Selected chord length reset work must keep the selected-chord length reset command scoped to the selected Pattern A/B/C slot; derive the target only from the core 4-step chord length capped by the remaining 16-step grid from the selected chord step; route reset only through the existing selected-chord length update path; disable reset when no active selected chord exists or the selected chord already matches the target length; set only the chord length; preserve root, quality, inversion, step, velocity, chance, copy/paste/duplicate/delete, playback, WAV/stem/MIDI export, and manual chord controls; keep the chord selected and undoable; and avoid reharmonization, progression replacement, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Chord Playhead Highlighting work must derive the currently playing chord slot only from local realtime playback snapshots and editable Pattern A/B/C chord step/length data; render it as UI-only state in the Chord Editor; keep selected chord editing independent; avoid auto-selecting, auto-scrolling, saving, undo history, or mutating chord events; preserve playback scheduling, Pattern A/B/C independence, save/load, WAV/stem/MIDI export semantics; and avoid sampling, imported audio, remote AI, remote analysis, accounts, analytics, or cloud sync.

Pattern-aware editor playhead work must derive drum, 808, Synth, and Chord editor playhead highlights only from local realtime playback snapshots and the selected Pattern A/B/C; show editor playheads only when the playback snapshot Pattern matches the selected editing Pattern; keep Transport Position Readout, Arrangement Playhead Highlighting, Section Locator Pads, and Song Form Overview tied to the actually playing Pattern; avoid auto-selecting, auto-scrolling, saving, undo history, or mutating musical events or arrangement data; preserve playback scheduling, Pattern A/B/C independence, save/load, WAV/stem/MIDI export semantics; and avoid sampling, imported audio, remote AI, remote analysis, accounts, analytics, or cloud sync.

Playing Pattern Tab work must derive the audible Pattern A/B/C marker only from local realtime playback snapshots; keep selected Pattern tab state independent from playing Pattern tab state; avoid auto-selecting, auto-scrolling, saving, undo history, or mutating Pattern A/B/C musical events or arrangement data; preserve Pattern-aware editor playheads, Transport Position Readout, Arrangement Playhead Highlighting, Section Locator Pads, Song Form Overview, playback scheduling, Pattern A/B/C independence, save/load, WAV/stem/MIDI export semantics; and avoid sampling, imported audio, remote AI, remote analysis, accounts, analytics, or cloud sync.

Pattern Playback Readout work must derive edit Pattern, audible Pattern, tone, and event-count labels only from local realtime playback snapshots, selected Pattern A/B/C state, and editable Pattern A/B/C event data; keep the readout UI-local and out of saved project schema; avoid auto-selecting, auto-scrolling, saving, undo history, or mutating Pattern A/B/C musical events or arrangement data; preserve Playing Pattern Tabs, Pattern-aware editor playheads, Transport Position Readout, Arrangement Playhead Highlighting, Section Locator Pads, Song Form Overview, playback scheduling, Pattern A/B/C independence, save/load, WAV/stem/MIDI export semantics; and avoid sampling, imported audio, remote AI, remote analysis, accounts, analytics, or cloud sync.

Pattern Playback Readout Command Reference work must mark the Create row as `Quick Actions / Readout`, reflect only the existing edit-vs-heard Pattern readout, selected editing Pattern, audible Pattern, local event-count context, visible Audible Pattern Follow action, and Quick Actions Audible Pattern Follow command; preserve readout derivation, realtime playback snapshots, selected Pattern behavior, audible Pattern tracking, Audible Pattern Follow routing, follow result metrics, Pattern A/B/C event data, arrangement assignments, loop scope, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid automatic follow mode, auto-selecting during playback without a click, autoplay, tutorials, macros, command chains, hidden generation, automatic arrangement, automatic export, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Audible Pattern Follow work must derive the visible follow control, Quick Actions audible Pattern follow command, result metric, audition cue, and next-check text only from local realtime playback snapshots, selected Pattern A/B/C state, editable Pattern event counts, and the existing selected Pattern view-update handler; route visible clicks and command runs only to an explicit one-shot selected Pattern focus change when the audible Pattern differs from the editing Pattern; keep follow state out of saved project schema and undo history; preserve Pattern Playback Readout derivation, Playing Pattern Tabs, Pattern-aware editor playheads, Pattern A/B/C event data, arrangement assignments, playback scheduling, loop scope, save/load, WAV/stem/MIDI export, and Handoff semantics; and avoid automatic follow mode, auto-selecting during playback without a click, autoplay, command chains, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Audible Pattern Follow result metric work must derive the explicit follow action, audible target Pattern A/B/C, before/current edit Pattern, target event count, drum/music posture, arrangement usage, and Pattern Playback Readout context only from the current `pattern-follow-audible` command title/detail plus local playback snapshot, Pattern, and arrangement state after the existing Audible Pattern Follow handler; preserve follow routing, realtime playback snapshot derivation, selected Pattern view-update handling, Pattern Playback Readout, Playing Pattern Tabs, selected-event reset semantics, Pattern A/B/C musical events, arrangement assignments, playback, save/load, WAV/stem/MIDI export, Handoff, command search, scope filters, Pinned Commands, and Recent Commands; and avoid auto-follow mode, autoplay, hidden selection changes, undo history, command chains, macros, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Audible Pattern Follow Command Reference work must mark the Create row as `Quick Actions / Readout`, reflect only the existing Pattern Playback Readout context, heard Pattern target, explicit visible follow action, Quick Actions Audible Pattern Follow command, and local follow result feedback; preserve follow derivation, visible follow-control availability, Quick Actions routing, selected Pattern view-update handling, result metrics, audition cues, next-check text, Pattern Playback Readout derivation, realtime playback snapshots, Pattern A/B/C event data, arrangement assignments, loop scope, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid automatic follow mode, auto-selecting during playback without a click, autoplay, tutorials, macros, command chains, hidden generation, automatic arrangement, automatic export, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Arrangement Playback Readout work must derive selected arrangement block, audible arrangement block, tone, Pattern A/B/C labels, and bar labels only from local realtime playback snapshots, selected arrangement block state, and editable arrangement block data; keep the readout UI-local and out of saved project schema; avoid auto-selecting, auto-scrolling, saving, undo history, or mutating arrangement blocks or Pattern A/B/C musical events; preserve selected-block role readouts, Arrangement Playhead Highlighting, Playing Pattern Tabs, Pattern-aware editor playheads, Transport Position Readout, Section Locator Pads, Song Form Overview, playback scheduling, Pattern A/B/C independence, save/load, WAV/stem/MIDI export semantics; and avoid sampling, imported audio, remote AI, remote analysis, accounts, analytics, or cloud sync.

Arrangement Playback Readout Command Reference work must mark the Arrange row as `Quick Actions / Readout`, reflect only the existing edit-vs-heard block readout, selected arrangement block, audible arrangement block, Pattern A/B/C labels, bar context, visible Audible Arrangement Follow action, and Quick Actions Audible Arrangement Follow command; preserve readout derivation, realtime playback snapshots, selected arrangement block behavior, audible block tracking, Audible Arrangement Follow routing, follow result metrics, arrangement block data, Pattern A/B/C event data, loop scope, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid automatic follow mode, auto-selecting during playback without a click, autoplay, tutorials, macros, command chains, hidden generation, automatic arrangement, automatic export, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Audible Arrangement Follow work must derive the visible follow control, Quick Actions audible arrangement follow command, result metric, audition cue, and next-check text only from local realtime playback snapshots, selected arrangement block state, arrangement block Pattern assignments, and the existing selected arrangement block view-update handler; route visible clicks and command runs only to an explicit one-shot selected arrangement block plus selected Pattern focus change when the audible block differs from the editing block; keep follow state out of saved project schema and undo history; preserve Arrangement Playback Readout derivation, Arrangement Playhead Highlighting, Playing Pattern Tabs, Pattern-aware editor playheads, arrangement block data, Pattern A/B/C event data, playback scheduling, loop scope, save/load, WAV/stem/MIDI export, and Handoff semantics; and avoid automatic follow mode, auto-selecting during playback without a click, autoplay, command chains, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Audible Arrangement Follow result metric work must derive the explicit follow action, audible target block, before/current edit block, Pattern A/B/C assignment, bar range, block event count, song block count, and total song bars only from the current `arrangement-follow-audible` command title/detail plus local playback snapshot, arrangement, and Pattern state after the existing Audible Arrangement Follow handler; preserve follow routing, realtime playback snapshot derivation, selected arrangement block view-update handling, selected Pattern alignment, Arrangement Playback Readout, Arrangement Playhead Highlighting, Pattern A/B/C musical events, arrangement blocks, playback, save/load, WAV/stem/MIDI export, Handoff, command search, scope filters, Pinned Commands, and Recent Commands; and avoid auto-follow mode, autoplay, hidden selection changes, undo history, command chains, macros, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Audible Arrangement Follow Command Reference work must mark Audible Arrangement Follow as `Quick Actions / Readout` only when it reflects existing Arrangement Playback Readout context, Quick Actions audible arrangement follow command, and local follow result feedback; it must not change audible-block derivation, follow routing, Quick Actions execution, playback scheduling, loop scope, render/export, project schema, save/load, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Sound design work must keep tone parameters in project state, migrate older project files without sound data, and make preset or Studio tone changes audible in both realtime playback and WAV export.

Sound Preset Preview work must derive selected preset labels, target tone posture, Preview Decision labels, visible Preview Decision action targets, pre-click sound move counts, any Quick Actions Sound Preset Decision/current/direct command targets, and any direct sound preset Quick Actions only from current local `SoundDesign` state, `soundPresetDesign`, existing `soundPresetIds`, existing Sound Preset Preview data, and explicit preview selection; keep preview/decision/result state UI-local and out of saved project schema and undo history; route the Apply button, visible Preview Decision action, the Quick Actions Sound Preset Decision command, the current-target Quick Actions Sound Preset command, and any direct Sound Preset Quick Actions only through the existing undoable sound preset update path; disable the visible Preview Decision action, Sound Preset Decision command, and direct sound preset commands when the preset already matches current sound; preserve sound preset definitions, Sound Focus Pads, Drum Kit Pads, manual Sound Designer controls, musical events, arrangement, mixer/master, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid auto-apply, autoplay, sampling, imported audio, sample browsing, hidden generation, remote AI, plugin hosting, accounts, analytics, or cloud sync.

Sound Preset Result work must update result labels only after explicit Sound Preset Apply clicks or Sound Preset Decision/current/direct Quick Action command runs, derive applied preset, full built-in sound-design scope, before/after preset, drums, 808, duck, synth, and chord tone posture, changed-parameter impact, audition cue, and next-check text only from local before/after `SoundDesign` data and existing sound preset definitions, keep result state UI-local and out of saved project schema and undo history, preserve Sound Preset Preview, Sound Focus Preview and Result, Drum Kit Preview and Result, manual Sound Designer controls, musical events, arrangement, mixer/master, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics, and avoid modal confirmations, auto-apply, autoplay, sampling, imported audio, sample browsing, hidden generation, remote AI, plugin hosting, accounts, analytics, or cloud sync.

Sound Preset Decision Command Reference work must mark the Sound row as `Quick Actions / Readout`, reflect only the existing Sound Preset Preview Decision Readout, suggested full-tone preset, current/direct Sound Preset commands, before/after tone posture, result metrics, and follow-up labels, preserve preset definitions, preview derivation, disabled-state rules, undoable SoundDesign update paths, playback, render/export, project schema, and preview/result UI-local storage, and avoid changing command execution, automatic apply, autoplay, tone auto-correction, audio analysis, imported audio, sampling, sample browsing, sampler tracks, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync.

Sound Preset Command Reference work must mark the Sound row as `Quick Actions / Readout`, reflect only the existing Sound Preset Preview, explicit Apply path, current/direct Sound Preset commands, before/after drums, 808, duck, Synth, and Chords posture, result metrics, audition cue, and follow-up labels, preserve preset definitions, preview derivation, disabled-state rules, undoable SoundDesign update paths, playback, render/export, project schema, and preview/result UI-local storage, and avoid changing command execution, automatic apply, autoplay, tone auto-correction, audio analysis, imported audio, sampling, sample browsing, sampler tracks, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync.

Sound Focus Pad work must derive kick, drum, 808, sidechain, synth, and chord tone changes from explicit local tone-posture presets, update only editable `SoundDesign` state through undoable project history, mark the result as custom when needed, keep manual Sound Designer controls editable, preserve musical events, arrangement, mixer/master, project files, snapshots, realtime playback, WAV/stem/MIDI export, and Handoff Sheet semantics, and avoid sampling, imported audio, hidden generation, remote AI, plugin hosting, accounts, analytics, or cloud sync.

Sound Focus Preview work must derive suggested tone posture, focus labels, target values, Preview Decision labels, visible Preview Decision action targets, pre-click move counts, any Quick Actions Sound Focus Decision/current/direct command targets, and any direct Sound Focus pad Quick Actions only from current local `SoundDesign` state and existing Sound Focus Pad targets, keep the preview/decision UI-local and out of saved project schema and undo history, route the visible Preview Decision action, the Quick Actions Sound Focus Decision command, the current-target Quick Actions Sound Focus command, and direct pad commands only through the existing undoable Sound Focus pad apply path, disable the visible Preview Decision action, Sound Focus Decision command, current-target command, and direct focus pad commands when the pad already matches current sound, preserve Sound Focus Pad definitions, apply behavior, manual Sound Designer controls, Drum Kit Pads, musical events, arrangement, mixer/master, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics, and avoid sampling, imported audio, sample browsing, hidden generation, remote AI, plugin hosting, accounts, analytics, or cloud sync.

Sound Focus Result work must update result labels only after explicit Sound Focus Pad clicks, visible Preview Decision action clicks, or Sound Focus Decision/current/direct Quick Action command runs, derive applied focus, built-in tone scope, before/after preset, drums, 808, duck, synth, and chord tone posture, changed-parameter impact, audition cue, and next-check text only from local before/after `SoundDesign` data and existing Sound Focus Pad definitions, keep result state UI-local and out of saved project schema and undo history, preserve Sound Focus Preview, Sound Focus Pad definitions, apply behavior, manual Sound Designer controls, Drum Kit Pads, musical events, arrangement, mixer/master, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics, and avoid modal confirmations, autoplay, sampling, imported audio, sample browsing, hidden generation, remote AI, plugin hosting, accounts, analytics, or cloud sync.

Sound Focus Decision Command Reference work must mark the Sound row as `Quick Actions / Readout`, reflect only the existing Sound Focus Preview Decision Readout, suggested 808/Synth/Chords tone-focus target, current/direct Sound Focus commands, before/after tone posture, result metrics, audition cue, and follow-up labels, preserve focus definitions, preview derivation, disabled-state rules, undoable SoundDesign update paths, playback, render/export, project schema, and preview/result UI-local storage, and avoid changing command execution, automatic apply, autoplay, tone auto-correction, audio analysis, imported audio, sampling, sample browsing, sampler tracks, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync.

Sound Focus Command Reference work must mark the Sound row as `Quick Actions / Readout`, reflect only the existing Sound Focus Preview, explicit pad Apply path, current/direct Sound Focus commands, before/after 808/Synth/Chords tone posture, result metrics, audition cue, and follow-up labels, preserve focus definitions, preview derivation, disabled-state rules, undoable SoundDesign update paths, playback, render/export, project schema, and preview/result UI-local storage, and avoid changing command execution, automatic apply, autoplay, tone auto-correction, audio analysis, imported audio, sampling, sample browsing, sampler tracks, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync.

Sound Timbre Check work must derive drums, 808, air, width, warmth, balance spread, status, detail, Quick Actions Timbre Check command labels, result metric, audition cue, and next-check labels only from existing local `SoundDesign` values; route the Quick Actions Timbre Check command only to existing Sound panel focus behavior and UI-local result feedback without mutating project data or sound settings; any Timbre Check focus suggestion must derive only from the existing Sound Focus preview and route Apply Focus only through the existing explicit Sound Focus pad handler; Studio tone baseline/delta/reset work must keep remembered baseline and captured-baseline result state UI-local, refresh that baseline only from named sound preset changes, the initial custom tone, or explicit Capture Baseline clicks, derive per-control baselines from that remembered baseline source, derive Studio Tone Drift Summary labels and the Reset Largest Drift target only from current `SoundDesign`, `studioToneControls`, and the remembered baseline, route Reset Largest Drift only after explicit clicks through the existing Sound Designer update path for the single largest changed SoundDesign parameter, update only the clicked SoundDesign parameter through the existing Sound Designer update path, disable Reset when that parameter already matches the remembered baseline, disable Reset Largest Drift when no drift exists, show Studio Tone Baseline Result feedback only after explicit Capture Baseline clicks using current `SoundDesign` posture and next-check text, and show Studio Tone Reset Result feedback only after explicit Reset clicks or Reset Largest Drift clicks using the affected control's before value, remembered baseline, baseline source, delta-to-zero outcome, and next-check text; preserve Sound Preset Preview/Result, Sound Focus Preview/Result, Drum Kit Preview/Result, manual Sound Designer controls, project schema, musical events, arrangement, mixer/master, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid tone auto-correction, audio analysis, imported audio, sampling, sample browsing, sampler tracks, hidden generation, remote AI, plugin hosting, accounts, analytics, or cloud sync.

Quick Actions Sound Decision and Timbre Check result metric work must identify only the explicit Sound Preset, Drum Kit, Sound Focus, or Timbre Check action, preview/check context, target preset/kit/focus/check posture, current built-in `SoundDesign` posture, selected Pattern, editable drum/808/Synth/chord counts, total editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, and next listening/apply check from local command metadata plus current project state; preserve sound preset definitions, drum kit pad definitions, sound focus pad definitions, timbre scoring, preview selection, decision target derivation, sound apply handlers, generated Pattern A/B/C events, arrangement data, mixer/master algorithms, project schema, playback, render/export, Handoff, privacy boundaries, and sampling boundaries; and avoid auto-apply, command chains, hidden generation, autoplay, imported audio, remote AI, accounts, analytics, or cloud sync.

Sound Snapshot A/B work must derive captured slots, visible readout action targets, tone comparison metrics, Quick Actions Sound Snapshot Decision and capture/recall/clear targets, result metrics, and follow-up labels only from UI-local Sound Snapshot slot state, the current local `SoundDesign`, existing Timbre Check derivation, and explicit command ids; route capture and clear panel buttons, visible readout capture action clicks, Quick Actions Sound Snapshot Decision capture runs, and Quick Actions capture/clear commands only through UI-local Sound Snapshot handlers; route recall panel buttons, visible readout recall action clicks, Quick Actions Sound Snapshot Decision recall runs, and Quick Actions recall commands only through the existing undoable project update path that replaces `project.sound` from the selected slot; keep Sound Snapshot slots and visible readout action state out of saved project schema and localStorage; preserve Sound Preset Preview/Result, Sound Focus Preview/Result, Drum Kit Preview/Result, Timbre Check, manual Sound Designer controls, musical events, arrangement, mixer/master, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid automatic recall, automatic capture, tone auto-correction, audio analysis, imported audio, sampling, sample browsing, sampler tracks, hidden generation, remote AI, plugin hosting, accounts, analytics, or cloud sync.

Sound Snapshot A/B Decision Command Reference work must mark the Sound row as `Quick Actions / Readout`, reflect only the existing visible Sound Snapshot A/B readout action, capture/recall recommendation, A/B slot state, capture/recall/clear commands, tone-pass comparison metrics, result metrics, and follow-up labels, preserve slot derivation, capture/clear handlers, undoable SoundDesign recall paths, playback, render/export, project schema, and A/B slot UI-local storage, and avoid changing command execution, automatic recall, automatic capture, tone auto-correction, audio analysis, imported audio, sampling, sample browsing, sampler tracks, hidden generation, remote AI, plugin hosting, accounts, analytics, or cloud sync.

Sound Snapshot A/B Command Reference work must mark the Sound row as `Quick Actions / Readout`, reflect only the existing Sound Snapshot A/B comparison readout, A/B slot state, capture/recall/clear commands, visible guidance, preset/drum/808/Synth/Chords comparison metrics, result metrics, and follow-up labels, preserve slot derivation, capture/clear handlers, undoable SoundDesign recall paths, playback, render/export, project schema, and A/B slot UI-local storage, and avoid changing command execution, automatic recall, automatic capture, tone auto-correction, audio analysis, imported audio, sampling, sample browsing, sampler tracks, hidden generation, remote AI, plugin hosting, accounts, analytics, or cloud sync.

Drum Kit Pad work must derive built-in kick, clap, and hat tone posture plus `drum_rack` channel mix from explicit local kit presets, update only editable `SoundDesign` drum parameters and the `drum_rack` mixer channel through undoable project history, keep Sound Designer and Mixer controls manually editable, preserve Pattern A/B/C musical events, arrangement, non-drum mixer channels, master state, Delivery Target, Beat Readiness, Beat Map, Next Move, project files, snapshots, realtime playback, WAV/stem/MIDI export, and Handoff Sheet semantics, and avoid sampling, imported audio, sample packs, sampler mapping, hidden generation, remote AI, plugin hosting, accounts, analytics, or cloud sync.

Drum Kit Preview work must derive suggested kit labels, kick/clap/hat target posture, `drum_rack` mixer posture, Preview Decision labels, visible Preview Decision action targets, pre-click kit move counts, any Quick Actions Drum Kit Decision/current/direct command targets, and any direct Drum Kit pad Quick Actions only from current local project state, existing Drum Kit Pad definitions, and existing Drum Kit Preview data; keep preview/decision state UI-local and out of saved project schema and undo history; route the visible Preview Decision action, the Quick Actions Drum Kit Decision command, the current-target Quick Actions Drum Kit command, and direct kit pad commands only through the existing undoable Drum Kit pad apply path; disable the visible Preview Decision action, Drum Kit Decision command, and direct kit pad commands when the kit already matches current sound and drum rack posture; preserve Drum Kit Pad definitions and apply behavior, Drum Kit Result, Sound Focus Preview and Result, manual Sound Designer and Mixer controls, Pattern A/B/C musical events, arrangement, non-drum mixer channels, master state, Delivery Target, Beat Readiness, Beat Map, Next Move, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid modal confirmations, auto-apply, autoplay, sampling, imported audio, sample packs, sampler mapping, hidden generation, remote AI, plugin hosting, accounts, analytics, platform compliance, or cloud sync.

Drum Kit Result work must update result labels only after explicit Drum Kit Pad clicks, visible Preview Decision action clicks, or Drum Kit Decision/current/direct Quick Action command runs, derive applied kit, kick, clap, hat, `drum_rack` mixer posture, changed-kit impact, audition cue, and next-check text only from local before/after project state and existing Drum Kit Pad definitions; keep result state UI-local and out of saved project schema and undo history; preserve Drum Kit Pad definitions and apply behavior, Sound Focus Preview and Result, manual Sound Designer and Mixer controls, Pattern A/B/C musical events, arrangement, non-drum mixer channels, master state, Delivery Target, Beat Readiness, Beat Map, Next Move, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid modal confirmations, autoplay, sampling, imported audio, sample packs, sampler mapping, hidden generation, remote AI, plugin hosting, accounts, analytics, platform compliance, or cloud sync.

Drum Kit Decision Command Reference work must mark the Sound row as `Quick Actions / Readout`, reflect only the existing Drum Kit Preview Decision Readout, suggested built-in kick/clap/hat kit, current/direct Drum Kit commands, before/after drum tone and `drum_rack` posture, result metrics, audition cue, and follow-up labels, preserve kit definitions, preview derivation, disabled-state rules, undoable Drum Kit update paths, playback, render/export, project schema, and preview/result UI-local storage, and avoid changing command execution, automatic apply, autoplay, tone auto-correction, audio analysis, imported audio, sampling, sample browsing, sample packs, sampler tracks, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync.

Drum Kit Command Reference work must mark the Sound row as `Quick Actions / Readout`, reflect only the existing Drum Kit Preview, explicit pad Apply path, current/direct Drum Kit commands, before/after built-in kick/clap/hat tone and `drum_rack` posture, result metrics, audition cue, and follow-up labels, preserve kit definitions, preview derivation, disabled-state rules, undoable Drum Kit update paths, playback, render/export, project schema, and preview/result UI-local storage, and avoid changing command execution, automatic apply, autoplay, tone auto-correction, audio analysis, imported audio, sampling, sample browsing, sample packs, sampler tracks, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync.

Sidechain work must keep kick-to-808 ducking as editable local sound-design state, migrate older project files without sidechain data, and apply the same deterministic gain rule to realtime playback, full-mix WAV export, and 808 stem export.

Project file work must reject invalid imports without overwriting the current project, preserve edited drum, bass, chord, melody, sound design, mixer, master, key, BPM, style, and arrangement data on save/load, and keep file identity plus unsaved-edit readouts UI-local and out of saved project schema.

Local draft recovery work must write only bounded versioned GrooveForge project JSON to renderer localStorage after project edits, validate drafts through the normal project parser before showing Restore Draft, keep Restore and Clear as explicit user-clicked actions, preserve `.grooveforge.json` Save/Open as the durable workflow, keep project safety readouts UI-local and derived only from local draft recovery state, local draft timestamp, current file identity, unsaved-edit state, and project status, keep clearing a draft from deleting or mutating the current project, avoid file downloads, exports, media blobs, imported audio, sampling, accounts, analytics, cloud sync, remote AI, or hidden background storage, and keep restore undoable through project history.

Project Snapshot work must store snapshots as local project-file data, migrate older projects to an empty snapshot list, keep snapshot payloads free of nested snapshots, preserve save/load and undo/redo semantics, allow save/rename/restore/delete without cloud sync or accounts, keep snapshot names bounded and normalized, and avoid sampling, remote AI, hidden audio assets, destructive filesystem versioning, or analytics.

Snapshot slot role readout work must derive only from local `project.snapshots` and `maxProjectSnapshots`; stay UI-local and out of saved project schema; preserve Project Snapshot save, rename, restore, delete, Snapshot Compare, save/load, undo/redo, realtime playback, WAV/stem/MIDI export, Beat Map, Next Move, and Handoff semantics; and avoid auto-save, auto-restore, background filesystem versioning, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Snapshot Compare work must derive current-vs-saved snapshot comparison only from local project state, saved Project Snapshot payloads, deterministic export analysis, deterministic stem analysis, Beat Readiness, selected Delivery Target, and master state; remain read-only; avoid restoring, deleting, renaming, saving, mutating snapshots, triggering exports, or changing recommendations; preserve Project Snapshots, save/load, undo/redo, realtime playback, WAV/stem/MIDI export, Beat Passport, Finish Checklist, Review Queue, Beat Map, Next Move, Mix Coach, Master Finish, and Handoff semantics; and avoid sampling, imported audio, plugin hosting, remote AI, remote analysis, platform compliance, professional mastering, accounts, analytics, or cloud sync claims.

Snapshot Compare Focus work must derive focus items, focus readout, the Quick Actions Snapshot Compare focus command, direct Snapshot Compare metric commands, Focus Result labels, comparison metrics, audition cues, and next-check feedback only from existing Snapshot Compare cards and metrics, the visible summary, and the explicitly clicked or command-run metric; show Focus Result only after explicit visible or command focus; keep focused item state and Focus Result feedback UI-local and out of saved project schema and undo history; route visible Focus controls and command runs only to existing Compose, Arrange, Master, or Deliver panels through the same focus handler; preserve Snapshot Compare derivation, Project Snapshot save/rename/restore/delete behavior, save/load, undo/redo, realtime playback, WAV/stem/MIDI export, Beat Passport, Finish Checklist, Review Queue, Beat Map, Next Move, Mix Coach, Master Finish, and Handoff semantics; and avoid snapshot mutation, restore/delete Quick Actions, auto-save, auto-restore, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Pattern work must keep Pattern A/B/C as independent editable event data. Copy and clear tools must operate on the selected pattern slot without deleting the slot itself. Pattern Compare work must derive event, track, and arrangement-use summaries from local Pattern A/B/C and arrangement state; Cue must switch only selected-pattern preview state without autoplay or undo history; Use must route selected-block pattern changes through existing undoable arrangement updates. Pattern preview playback should loop the selected pattern, while arrangement playback and WAV export should follow arrangement pattern assignments.

Pattern Compare Decision Readout work must derive its Cue/Use recommendation only from existing local Pattern Compare summaries, the selected Pattern A/B/C, and the selected arrangement block pattern; any readout action button must run only after an explicit user click and route through the existing Pattern Compare Cue or Use handlers; keep the readout UI-local and out of saved project schema and undo history; preserve visible Pattern Compare card ordering, Cue/Use handlers, Pattern Compare Result behavior, playback, save/load, WAV/stem/MIDI export, and Handoff semantics; and avoid auto-cueing, auto-arranging, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Pattern Compare Decision work must expose only the current local Pattern Compare Decision recommendation, derive title/detail/keywords/disabled state from the existing decision summary and selected arrangement block, route command runs only through the same explicit Pattern Compare Decision action handler as the visible readout, preserve direct Pattern Cue/Switch/Use commands, Pattern Compare Result behavior, Pattern A/B/C event data, playback, save/load, WAV/stem/MIDI export, and Handoff semantics, and avoid command chains, auto-cueing, auto-arranging, autoplay, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Pattern Compare Decision Result work must derive the command result status, metric label, before/after values, audition cue, and next check only from the current `pattern-compare-decision` Quick Action title/detail plus before/after local Pattern A/B/C and arrangement state; show Cue recommendations as Pattern-loop cue feedback and Use recommendations as arrangement placement feedback; keep the shared Quick Action Result UI-local and out of saved project schema and undo history; preserve visible Pattern Compare Decision, Pattern Compare Result Strip, direct Pattern Cue/Switch/Use commands, Pattern A/B/C event data, playback, save/load, WAV/stem/MIDI export, and Handoff semantics; and avoid new commands, command chains, auto-cueing, auto-arranging, autoplay, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Pattern Compare Decision Command Reference work must mark the Create row as `Quick Actions / Readout`, reflect only the existing current Cue/Use recommendation, selected Pattern A/B/C context, selected-block placement context, visible readout action, Quick Actions Pattern Compare Decision command, and local Pattern Compare Result feedback; preserve recommendation derivation, visible readout action behavior, Quick Actions routing, direct Pattern Cue/Switch/Use commands, disabled-state handling, result metrics, audition cues, next-check text, Pattern Compare card ordering, Pattern A/B/C event data, selected arrangement block assignment behavior, loop scope, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid automatic cueing, automatic arrangement, auto-selecting during playback without a click, autoplay, tutorials, macros, command chains, hidden generation, automatic export, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Pattern Compare Command Reference work must mark the Create row as `Quick Actions / Readout`, reflect only the existing Pattern A/B/C cue/use cards, selected/cued Pattern context, selected-block placement context, direct Pattern Cue/Switch/Use commands, and local Pattern Compare Result feedback; preserve Pattern Compare summary derivation, card ordering, Cue handlers, Use handlers, Switch handlers, selected/cued Pattern state handling, selected-block assignment behavior, result metrics, audition cues, next-check text, Pattern Compare Decision behavior, Pattern A/B/C musical event data, arrangement block data outside explicit Use actions, loop scope, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid automatic cueing, automatic arrangement, auto-selecting during playback without a click, autoplay, tutorials, macros, command chains, hidden generation, automatic export, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Pattern Compare Result work must update result labels only after explicit visible Pattern Compare Cue or Use clicks, derive action, Pattern A/B/C target, selected-block placement, event posture, audition cue, and next check only from local before/after Pattern A/B/C and arrangement state around the existing `cuePattern` and `usePatternInSelectedBlock` handlers, keep result state UI-local and out of saved project schema and undo history, preserve Quick Actions Pattern Cue/Use result behavior, Pattern Compare summary derivation, Pattern A/B/C event data, playback, save/load, WAV/stem/MIDI export, and Handoff semantics, and avoid hidden generation, autoplay, auto-arrangement, auto-export, command chains, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Pattern Edit Result work must update result labels only after explicit Pattern A/B/C Copy or Clear clicks or explicit Quick Actions Pattern Copy/Clear command runs, derive action, source/target Pattern, before/after target event posture, changed drum/music event counts, audition cue, and next check only from local before/after Pattern A/B/C data around the existing `copySelectedPattern` and `clearSelectedPattern` handlers, keep result state UI-local and out of saved project schema and undo history, preserve existing copy/clear undo semantics, Pattern Compare, Pattern Clone, Pattern Variation, Pattern Fill, Pattern Stack, playback, save/load, WAV/stem/MIDI export, and Handoff semantics, and avoid hidden generation, autoplay, auto-arrangement, auto-export, command chains, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Pattern Edit work must expose only Pattern A/B/C Copy to other slots and Clear selected Pattern commands, derive commands only from the selected Pattern and `patternSlots`, route command runs only through the existing `copySelectedPattern` and `clearSelectedPattern` handlers, keep Pattern Edit Result UI-local and shared with visible copy/clear clicks, preserve undo semantics, Pattern A/B/C event data except explicit target overwrite/clear, selected note/drum/chord reset behavior, playback, save/load, WAV/stem/MIDI export, Handoff, command search, scope filters, Spotlight, Pinned Commands, and Recent Commands, and avoid command chains, macros, autoplay, auto-arrangement, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Pattern Copy/Clear result metric work must derive copy versus clear, source/target Pattern labels, target drum/music counts, and target event count only from the explicit `pattern-copy-*` or `pattern-clear` command id, command title, and local selected/target Pattern A/B/C data after the existing `copySelectedPattern` and `clearSelectedPattern` handlers; preserve command availability, handler behavior, undo semantics, playback, save/load, WAV/stem/MIDI export, Handoff, command search, scope filters, Pinned Commands, and Recent Commands; and avoid command chains, macros, autoplay, auto-arrangement, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Pattern Copy/Clear Command Reference work must mark the Create row as `Quick Actions / Readout`, reflect only the existing direct selected-Pattern copy-to-slot commands, selected-Pattern clear, and local Pattern Edit Result feedback; preserve command derivation, `copySelectedPattern` and `clearSelectedPattern` handler behavior, result metric derivation, undo semantics, selected Pattern focus resets, Pattern A/B/C event data outside explicit Copy/Clear actions, arrangement, mixer, master, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid new copy/clear logic, tutorials, macros, command chains, hidden generation, automatic arrangement, automatic export, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Pattern DNA work must derive selected Pattern A/B/C layers, density, dynamics, variation signals, and arrangement use only from local Pattern A/B/C drum hits, 808/bass notes, chord events, melody notes, probability, timing, velocity, hat-repeat, glide, and arrangement-block data; dynamics cards must summarize average velocity, velocity spread, and layer velocity posture without mutating events; remain read-only; avoid mutating Pattern A/B/C, arrangement, mixer, sound, master, snapshots, playback, save, or export state; preserve Pattern Compare, Groove Compass, Composer Guide, Beat Map, Next Move, realtime playback, WAV/stem/MIDI export, and Handoff semantics; and avoid sampling, imported audio, remote AI, remote analysis, hidden generation, accounts, analytics, or cloud sync.

Pattern DNA Focus work must derive focus targets, the Quick Actions Pattern DNA focus command, any direct Pattern DNA card Quick Actions, Focus Result labels, pattern metrics, audition cues, and next-check feedback only from existing Pattern DNA card ids, existing Pattern DNA cards, the current highest-priority loop card, the visible summary, and the explicitly clicked or command-run card, show Focus Result only after explicit visible or command focus, keep focused card state and result feedback UI-local and out of saved project schema and undo history, route clicks and command runs only to existing Compose or Arrange panels through the existing focus handler, preserve Pattern DNA derivation, card order, visible focus buttons, Pattern A/B/C events, arrangement, mixer, master, exports, playback, Pattern Compare, direct editing semantics, and Handoff state, and avoid auto-editing, autoplay, command chains, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Pattern DNA result metric work must derive the explicit loop-posture focus action, active or direct Pattern DNA lane, destination panel, lane context, selected Pattern, editable event count, drum/music layer counts, arrangement use, and song length only from current Pattern DNA command title/detail plus local project state after the existing Pattern DNA focus handler; preserve Pattern DNA derivation, card order, focus-card selection, visible focus behavior, direct card command definitions, Pattern A/B/C event data, arrangement data, Pattern Variation, Pattern Fill, Pattern Clone, Pattern Stack, Pattern Compare, Composer Actions, Layer Starter, Groove Compass, Key Compass, Composer Guide, Quick Actions filtering, command search, Pinned Commands, Recent Commands, project data, playback, save/load, WAV/stem/MIDI export, Handoff Sheet, Handoff Pack, and local draft behavior; avoid onboarding overlays, tutorials, macros, command chains, hidden generation, auto-run, autoplay, automatic pattern edits, auto-save, auto-export, sampling, imported audio, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Pattern DNA Command Reference work must add the Create row as `Quick Actions / Readout`, reflect only the existing local selected-Pattern layer, density, dynamics, variation, and arrangement-use diagnostics, Focus readout, Pattern DNA focus command, direct card commands, and local Focus Result feedback; preserve Pattern DNA derivation, card order, focus routing, Quick Actions execution, Pattern A/B/C events, arrangement, mixer, master, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid tutorials, macros, command chains, hidden generation, automatic fixes, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Layer Starter Pad work must derive Drums, 808, Chords, and Synth readiness, Priority Readout labels, visible priority action targets, the Quick Actions Layer Starter command, any direct Layer Starter Quick Actions, and Quick Action result metrics only from selected Pattern event counts, existing Layer Starter options, and local style action goals/cues; route visible Priority action clicks, pad clicks, and command runs only through existing undoable Drum Foundation, 808 Bassline, Chord Progression, and Melody Motif handlers; keep Layer Starter state, priority readout state, and visible priority action state UI-local and out of saved project schema and undo history; show Layer Starter Result feedback only as UI-local started-layer, before/after layer-count, changed-event, audition-cue, and next-check labels derived from before/after selected Pattern A/B/C data; show Quick Action result metrics only as UI-local selected Pattern, started-layer count/status, and selected-Pattern event-count labels derived from the explicitly clicked or command-run layer starter and local selected Pattern data; disable the visible Priority action and current-target Quick Actions Layer Starter command when every layer is ready and disable any direct Layer Starter command when that target layer is ready; preserve Pattern DNA, Pattern Compare, Pattern Clone Pads, Pattern Stack Pads, Composer Actions, Next Move, Beat Blueprint, direct event editing, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid sampling, imported audio, audio clips, sampler devices, hidden generation, remote AI, remote analysis, plugin hosting, autoplay, auto-arrangement, auto-export, command chains, accounts, analytics, or cloud sync.

Quick Actions Layer Starter result metric work must derive the explicit starter action, active priority or direct starter lane, selected Pattern, target layer action/context, before/current loop event posture, drum/music layer counts, and song length only from current Layer Starter command title/detail plus local project state after the existing Layer Starter handlers; preserve Layer Starter priority derivation, option derivation, direct starter command definitions, visible Priority Readout behavior, Layer Starter Result behavior, Drum Foundation, 808 Bassline, Chord Progression, Melody Motif, Pattern DNA, Pattern Stack, Pattern Compare, Pattern A/B/C event semantics, arrangement data, Quick Actions filtering, command search, Pinned Commands, Recent Commands, project data, playback, save/load, WAV/stem/MIDI export, Handoff Sheet, Handoff Pack, and local draft behavior; avoid onboarding overlays, tutorials, macros, command chains, hidden generation, auto-run, autoplay, automatic extra layer starts, auto-save, auto-export, sampling, imported audio, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Layer Starter Command Reference work must mark the Create row as `Quick Actions / Readout`, reflect only the existing selected Pattern Drums/808/Chords/Synth readiness, highest-priority missing/thin layer, visible Priority action, direct Layer Starter commands, Quick Actions Layer Starter command, and local Layer Starter Result feedback; preserve readiness derivation, priority scoring, visible Priority action behavior, Quick Actions routing, direct layer-start commands, disabled-state handling, result metrics, audition cues, next-check text, Drum Foundation, 808 Bassline, Chord Progression, Melody Motif, Pattern DNA, Pattern Compare, Pattern Clone, Pattern Stack, Composer Actions, Next Move, Beat Blueprint, direct event editing, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid hidden generation, automatic layer starts, command chains, automatic arrangement, autoplay, automatic export, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Listening Pass work must derive composition, arrangement, mix, and delivery audition checkpoints, Focus Readout labels, the visible readout action, the Quick Actions Listening Pass focus command, any direct Listening Pass checkpoint Quick Actions, Focus Result labels, listening metrics, audition cues, and next-check feedback only from selected Pattern event counts, Beat Readiness, Structure Lens, deterministic export analysis, deterministic stem analysis, Delivery Target, Session Brief state, existing Listening Pass checkpoint ids, existing Listening Pass items, the visible summary, the explicitly clicked or command-run checkpoint, and the current highest-priority checkpoint; show Focus Result only after explicit visible or command focus; keep Listening Pass state, readout state, visible readout action state, and result feedback UI-local and out of saved project schema and undo history; route the visible readout action, visible Focus buttons, and command runs only to existing Compose, Arrange, Mix, Master, or Deliver panels through the existing focus handler; preserve checkpoint derivation, checkpoint order, visible focus buttons, Beat Readiness, Pattern DNA, Layer Starter Pads, Workflow Navigator, Composer Actions, Review Queue, Mix Coach, Handoff Pack, direct editing, project files, snapshots, realtime playback, WAV/stem/MIDI export, and Handoff Sheet semantics; and avoid sampling, imported audio, reference-track upload, audio analysis, audio clips, sampler devices, hidden generation, remote AI, remote analysis, plugin hosting, autoplay, auto-fixing, auto-render, auto-export, macros, command chains, accounts, analytics, or cloud sync.

Quick Actions Listening Pass result metric work must derive the explicit listening focus action, current priority or direct audition checkpoint, destination panel, checkpoint status/context, selected Pattern, editable event count, readiness summary, arrangement block count, and song length only from current Listening Pass command title/detail plus local project state after the existing Listening Pass focus path; preserve Listening Pass checkpoint derivation, checkpoint order, focus-target selection, direct checkpoint command definitions, visible Focus Readout behavior, focus routing, Focus Result behavior, Beat Readiness, Structure Lens, Delivery Target, Session Brief, Beat Passport, Production Snapshot, Review Queue, Export Preflight, Mix Coach, Pattern A/B/C event data, arrangement data, Quick Actions filtering, command search, Pinned Commands, Recent Commands, project data, playback, save/load, WAV/stem/MIDI export, Handoff Sheet, Handoff Pack, and local draft behavior; avoid onboarding overlays, tutorials, macros, command chains, hidden generation, auto-run, autoplay, automatic arrangement writing, auto-save, auto-export, sampling, imported audio, reference-track upload, audio analysis, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Listening Pass Command Reference work must mark the Guide row as `Quick Actions / Readout`, reflect only the existing local composition, arrangement, mix, and delivery audition checkpoints, Focus Readout, visible readout action, Listening Pass focus command, direct checkpoint commands, and local Focus Result feedback; preserve Listening Pass checkpoint derivation, checkpoint order, Focus Readout derivation, visible readout action routing, focus routing, Quick Actions execution, selected Pattern state, Beat Readiness, Structure Lens, deterministic export/stem analysis, Delivery Target, Session Brief state, project data, playback scheduling, render/export, project schema, save/load, privacy boundaries, snapshots, direct editing, Handoff Pack, and Handoff Sheet state; and avoid auto-writing, autoplay, tutorials, macros, command chains, hidden generation, automatic fixes, audio analysis, media uploads, reference-track import or analysis, sampling, imported audio, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Hook Readiness Meter work must derive hook section, motif density, arrangement contrast, mix support, handoff context, Priority Readout labels, visible priority action targets, the Quick Actions Hook Readiness focus command, any direct Hook Readiness card Quick Actions, Focus Result labels, hook metrics, audition cues, next-check feedback, any Hook Loop cue commands, and any Hook Fix commands only from local arrangement Hook blocks, Pattern A/B/C event counts, Beat Readiness checks, deterministic export analysis, deterministic stem analysis, selected Delivery Target, Session Brief state, existing Hook Readiness card ids, the visible Hook Readiness summary, the explicitly clicked or command-run card, the current highest-priority hook card, and the current first Hook block; show Focus Result only after explicit visible or command focus; keep Priority Readout state, visible priority action state, focused card state, Focus Result feedback, cue state, Hook Fix result state, and result feedback UI-local and out of saved project schema and undo history; route visible Priority action clicks, focus clicks, and command runs only to existing Compose, Arrange, Mix, Master, or Deliver panels through the existing focus handler; route Hook Loop cue clicks and commands only through existing arrangement selection and Block loop transport state without autoplay; route Hook Fix clicks and commands through exactly one existing explicit handler per card, limited to 8 Bar Pattern Chain, Hook Pattern Variation, Hook Lift Arrangement Move, Headroom Mix Fix, or Vocal Session Brief Starter, with UI-local before/after Hook Fix result feedback; preserve Structure Lens, Song Form Overview, Beat Readiness, Listening Pass, Review Queue, Mix Coach, Handoff Pack, direct editing, project files, snapshots, realtime playback, WAV/stem/MIDI export, and Handoff Sheet semantics; and avoid hook auto-writing, audio analysis, reference-track upload, sampling, imported audio, sampler devices, hidden generation, remote AI, remote analysis, plugin hosting, autoplay, auto-arrangement, auto-mixing, auto-mastering, auto-export, command chains, accounts, analytics, or cloud sync.

Topline Space work must derive rhythm pocket, lead density, vocal window, mix headroom, artist context, Priority Readout labels, visible priority action targets, the Quick Actions Topline Space focus command, any direct Topline Space card Quick Actions, Focus Result labels, topline metrics, audition cues, next-check feedback, any Topline Loop cue commands, and any Topline Fix commands only from local arrangement Hook blocks or the selected Pattern, Pattern A/B/C drum/808/Synth/chord event counts, Beat Readiness checks, deterministic export analysis, deterministic stem analysis, selected Delivery Target, Session Brief state, existing Topline Space card ids, the visible Topline Space summary, the explicitly clicked or command-run card, the current highest-priority topline card, the current first Hook block, and selected Pattern state; show Focus Result only after explicit visible or command focus; keep Priority Readout state, visible priority action state, focused card state, Focus Result feedback, cue state, Topline Fix result state, and result feedback UI-local and out of saved project schema and undo history; route visible Priority action clicks, focus clicks, and command runs only to existing Compose, Arrange, Mix, Master, or Deliver panels through the existing focus handler; route Topline Loop cue clicks and commands only through existing arrangement selection, Pattern selection, and Block or Pattern loop transport state without autoplay; route Topline Fix clicks and commands through exactly one existing explicit handler per card, limited to Pocket Groove Feel, Clear Tail Pattern Fill, 8 Bar Pattern Chain, Headroom Mix Fix, or Vocal Session Brief Starter, with UI-local before/after Topline Fix result feedback; preserve Hook Readiness, Hook Loop cue, Structure Lens, Song Form Overview, Beat Readiness, Listening Pass, Review Queue, Mix Coach, Handoff Pack, direct editing, project files, snapshots, realtime playback, WAV/stem/MIDI export, and Handoff Sheet semantics; and avoid vocal recording, reference-track upload, audio analysis, stem separation, lyric generation, topline auto-writing, sampling, imported audio, sampler devices, hidden generation, remote AI, remote analysis, plugin hosting, autoplay, auto-arrangement, auto-mixing, auto-mastering, auto-export, macros, command chains, accounts, analytics, or cloud sync.

Hook Readiness and Topline Space Command Reference work must mark both Guide rows as `Quick Actions / Readout`, reflect only existing hook-quality checks, vocal/topline pocket checks, Priority Readouts, focus/cue/fix commands, and local Focus/Fix Result feedback; preserve Hook Readiness derivation, Topline Space derivation, card order, priority scoring, visible priority action behavior, focus routing, loop cue routing, fix routing, Quick Actions execution, result metrics, audition cues, next-check text, arrangement, Pattern A/B/C musical events, Beat Readiness, Listening Pass, Review Queue, Mix Coach, Handoff Pack, project schema, save/load, snapshots, undo/redo history, realtime playback, render/export, MIDI export, Handoff Sheet, Handoff Pack, privacy boundaries, and sampling boundaries; and avoid vocal recording, lyrics, reference-track upload, audio analysis, stem separation, sampling, imported audio, sampler devices, hidden generation, remote AI, remote analysis, plugin hosting, autoplay, auto-arrangement, auto-mixing, auto-mastering, auto-export, macros, command chains, accounts, analytics, or cloud sync.

Export Preflight work must derive delivery-risk status, Priority Readout labels, and visible priority action targets only from local Beat Readiness, deterministic export analysis, deterministic stem analysis, selected Delivery Target, arrangement length, Master Automation posture, Session Brief data, and existing Export Preflight cards/focus labels; keep Priority Readout state and visible priority action state UI-local and out of saved project schema and undo history; route visible Priority action clicks only to existing Compose, Master, or Deliver panels through the existing focus handler; avoid changing WAV, stem, MIDI, or Handoff Sheet export contents or mutating project musical events, arrangement, mixer, master, automation, targets, Session Brief, snapshots, playback, save/load, or export state; preserve Handoff Pack, Finish Checklist, Review Queue, Beat Map, Mix Coach, Export Meter, realtime playback, WAV/stem/MIDI export, and Handoff semantics; and avoid sampling, imported audio, remote AI, remote analysis, hidden generation, accounts, analytics, or cloud sync.

Export Preflight Focus work must derive focus targets, the Quick Actions Export Preflight focus command, any direct Export Preflight card Quick Actions, Focus Result labels, preflight metrics, audition cues, and next-check feedback only from existing Export Preflight card ids, existing Export Preflight cards, the visible summary, the explicitly clicked or command-run card, and the current highest-priority preflight card; show Focus Result only after explicit visible or command focus; keep focused card state and result feedback UI-local and out of saved project schema and undo history; route clicks and command runs only to existing Compose, Master, or Deliver panels through the existing focus handler; preserve preflight card derivation, card order, visible focus buttons, preflight scoring, Handoff Pack actions, WAV/stem/MIDI/Handoff Sheet file contents, render/download handlers, project musical data, arrangement, mixer, master, targets, Session Brief, snapshots, playback, save/load, undo/redo, and export state; and avoid auto-fixing, auto-rendering, auto-export, autoplay, macros, command chains, sampling, imported audio, remote AI, accounts, analytics, cloud sync, platform compliance, licensing, or mastering claims.

Quick Actions Export Preflight result metric work must derive the explicit delivery-risk action, current priority or direct preflight card, destination panel, preflight status/context, selected Delivery Target, WAV/headroom posture, audible stems against target stem goal, MIDI/song length posture, Session Brief and Handoff Sheet context, Master Automation posture, selected Pattern, editable event count, Pattern A/B/C usage, arrangement block count, song length, and next Export Preflight check only from current Export Preflight command title/detail plus local project, export, stem, and brief state after the existing Export Preflight focus path; preserve Export Preflight card derivation, card order, scoring, focus-target selection, direct card command definitions, visible Priority action behavior, focus routing, Focus Result behavior, Beat Passport, Beat Readiness, Listening Pass, Production Snapshot, Finish Checklist, Review Queue, Mix Coach, Delivery Target, Session Brief, Pattern A/B/C event data, arrangement data, Quick Actions filtering, command search, Pinned Commands, Recent Commands, project data, playback, save/load, WAV/stem/MIDI export, render/download handlers, Handoff Sheet, Handoff Pack, and local draft behavior; avoid onboarding overlays, tutorials, macros, command chains, hidden generation, auto-run, autoplay, auto-rendering, auto-export, automatic arrangement writing, auto-save, sampling, imported audio, audio analysis, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Export Preflight Command Reference work must mark the Deliver row as `Quick Actions / Readout`, reflect only the existing local readiness, mix/master, Master Automation, WAV/stem/MIDI deliverable, and handoff brief checks, Priority Readout, Export Preflight focus command, direct card commands, and local Focus Result feedback; preserve preflight card derivation, card order, Priority Readout derivation, visible priority action behavior, focus routing, Quick Actions execution, direct card command routing, result labels, disabled-state behavior, Handoff Pack actions, WAV/stem/MIDI/Handoff Sheet file contents, render/download handlers, project musical data, arrangement, mixer, master, Delivery Target, Session Brief, playback, render/export, project schema, save/load, snapshots, privacy boundaries, and sampling boundaries; and avoid auto-fixing, auto-rendering, auto-export, batch export, ZIP/archive creation, uploads, tutorials, macros, command chains, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Workflow Navigator work must derive Compose, Arrange, Mix, Deliver, Jump Result labels, readiness metrics, audition cues, and next-check feedback only from local Beat Map, Export Preflight, selected Pattern A/B/C, arrangement length, export status, visible Workflow Navigator items, and the explicitly clicked navigator item; remain UI-only; perform explicit scroll navigation without mutating project musical events, arrangement, mixer, master, targets, Session Brief, snapshots, playback, save/load, undo history, or export state; show Jump Result only after explicit visible, direct Quick Actions, or Workflow Spotlight jumps; avoid hiding controls by mode or replacing Mode Focus, Composer Guide, Beat Map, Next Move, Handoff Pack, or panel controls; and avoid sampling, imported audio, remote AI, remote analysis, hidden generation, accounts, analytics, or cloud sync.

Workflow Navigator Quick Actions work must expose only direct Compose, Arrange, Mix, and Deliver zone jump commands derived from existing Workflow Navigator items, route command runs only through the existing item-based Workflow Navigator jump handler, show the same UI-local Jump Result feedback as visible Workflow Navigator clicks, keep result feedback UI-local and out of saved project schema and undo history, preserve Workflow Navigator item derivation, item order, visible navigator cards, Workflow Spotlight, Mode Focus, First Beat Path, Beat Map scoring, Export Preflight scoring, Composer Guide, Review Queue, Finish Checklist, Next Move, project data, playback, save/load, undo/redo, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics, and avoid onboarding overlays, tutorials, macros, command chains, hidden generation, auto-run, autoplay, auto-save, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Workflow Navigator result metric work must identify only the explicit workflow jump, Guide destination, target zone, zone status/context, selected Delivery Target, ready/review/blocker workflow counts, selected Pattern, editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, stem readiness, audition cue, and next workflow check from current Workflow Navigator command title/detail plus local project, Beat Map, Export Preflight, export, stem, and navigator item state; preserve Workflow Navigator item derivation, item order, jump routing, Workflow Spotlight, project data, playback, render/export, project schema, privacy boundaries, and sampling boundaries.

Workflow Navigator Command Reference work must mark the Guide row as `Quick Actions / Readout`, reflect only the existing local Compose, Arrange, Mix, and Deliver stage jumps, direct zone commands, readiness metrics, audition cues, next-check feedback, and local Jump Result feedback; preserve Workflow Navigator item derivation, item order, visible navigator cards, Workflow Spotlight derivation, jump routing, Quick Actions execution, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid tutorials, macros, command chains, hidden generation, automatic fixes, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Workflow Spotlight work must derive spotlight status, selected zone, jump target text, ready/review/blocker counts, read-only Decision Readout labels, and the visible Decision Readout action target only from visible Workflow Navigator items; keep spotlight state, Decision Readout state, visible Decision Readout action state, and jump-result feedback UI-local and out of saved project schema; route visible Decision Readout action clicks, spotlight clicks, and any Quick Actions Workflow Spotlight focus command only through the existing item-based Workflow Navigator jump path for the derived zone, show the same Workflow Navigator Jump Result feedback as direct navigator jumps, and keep no-zone state disabled; preserve Workflow Navigator item derivation, item order, jump behavior, Mode Focus, Mode Focus Jump, Beat Map scoring, Export Preflight scoring, Composer Guide, Review Queue, Finish Checklist, Next Move recommendations, project data, playback, save/load, undo/redo, render/export, Quick Actions, and Handoff semantics; and avoid onboarding overlays, tutorials, macros, command chains, auto-run, autoplay, auto-save, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Workflow Spotlight result metric work must identify only the explicit spotlight focus action, Guide destination, derived spotlight zone, spotlight status/context, selected Delivery Target, ready/review/blocker workflow counts, selected Pattern, editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, stem readiness, audition cue, and next workflow check from current Workflow Spotlight command title/detail plus local project, Workflow Navigator, Beat Map, Export Preflight, export, stem, and spotlight state; preserve Workflow Spotlight derivation, Workflow Navigator item derivation, item order, jump routing, visible Decision Readout behavior, project data, playback, render/export, project schema, privacy boundaries, and sampling boundaries.

Workflow Spotlight Command Reference work must mark the Guide row as `Quick Actions / Readout`, reflect only the existing Workflow Spotlight readout and focus command, preserve Workflow Spotlight derivation and jump handling, and avoid changing project data, command execution, playback, render/export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Guide Quick Start work must derive the top launch strip, read-only Decision Readout labels for the highest-priority path/session/workflow lane, visible Decision Readout action target, read-only Priority Readout labels for why that lane is first and what to verify next, read-only Beat Completion Score percent/status/metric/next-check labels plus Path/Session/Workflow breakdown and bottleneck labels from existing First Beat Path step tones, Session Pass card tones, visible Workflow Navigator item tones, and Workflow Spotlight fallback tone, an explicit bottleneck focus target from the lowest completion breakdown lane, read-only Context Readout labels for the current Path, Session, and Workflow lanes, clicked-lane Result feedback, `guide-quick-start` Quick Action target, `guide-bottleneck-focus` Quick Action target, Quick Actions guide suggestion card, suggestion source/target/metric/pin-capacity/completion/breakdown/bottleneck/pin-state/bottleneck-pin-state/bottleneck-command/bottleneck-metric/recent-target metadata, suggestion reason line, suggestion breakdown line, suggestion bottleneck line, suggestion bottleneck check line, suggestion after-run check line, suggestion pin hint line, suggestion recent guide run line, suggestion Run Bottleneck control, and suggestion bottleneck Pin/Unpin control only from existing First Beat Path summary, Session Pass summary, visible Workflow Navigator/Workflow Spotlight items, current Guide Quick Start decision/context state, current Quick Action definitions, current Quick Action title/detail text, current Guide Quick Start completion score labels, current Guide Quick Start completion breakdown labels, current Guide Quick Start completion bottleneck label, current pinned action ids, current normalized pinned action count, `maxQuickActionPins`, current Quick Action recents, and the current `guide-bottleneck-focus` Quick Action; route its visible Decision Readout action, Path, Session, Workflow, bottleneck focus, Quick Action, bottleneck Quick Action, and suggestion-card execution only through the existing First Beat Path jump, Session Pass focus, and Workflow Navigator/Workflow Spotlight handlers; route any suggestion guide or bottleneck Pin/Unpin control only through existing explicit pinned-command handling; keep launch, decision readout, visible Decision Readout action state, priority readout, Beat Completion Score, completion breakdown, completion bottleneck, bottleneck focus state, context readout, result, suggestion, metadata, reason line, breakdown line, bottleneck line, bottleneck check line, after-run check line, pin hint line, recent guide run line, and pinned-command state UI-local and out of saved project schema and undo history; show the suggestion only for empty-search All or Project Quick Actions scope; clear stale Result feedback when the derived path/session/workflow context changes; preserve all underlying scoring, existing detailed result strips, Quick Actions filtering/order/Spotlight Enter behavior, Pinned Commands defaults/session-only storage, Recent Commands storage/order, mode switching, project data, playback, save/load, render/export, Handoff, and local draft semantics; and avoid onboarding overlays, tutorials, macros, command chains, auto-run, autoplay, auto-pin, auto-save, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Guide Quick Start result metric work must derive the explicit guide action, target lane, route/source, context, completion metric, completion breakdown, and bottleneck label only from the current `guide-quick-start` or `guide-bottleneck-focus` command title/detail after the existing Guide Quick Start handler; preserve Guide Quick Start target derivation, completion scoring, completion breakdown scoring, bottleneck selection, guide suggestion metadata, pinned-command handling, Recent Commands, jump/focus routing, mode switching, project data, playback, save/load, render/export, Handoff, local draft behavior, command search, scope filters, Pinned Commands, and Recent Commands; and avoid changing guide recommendations, auto-running commands, macros, command chains, onboarding overlays, tutorials, autoplay, hidden generation, auto-save, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Guide Quick Start Command Reference work must mark the Guide Quick Start row as `Quick Actions / Readout` and the Guide Bottleneck Focus row as `Quick Actions`, reflect only the existing Guide Quick Start Path, Session, and Workflow readouts, Decision Readout action, Priority/Context readouts, Beat Completion Score, completion breakdown, bottleneck label, bottleneck focus command, guide suggestion Quick Action, pinned-command controls, and local Result feedback; preserve guide target derivation, readout derivation, suggestion metadata, pinned-command handling, jump/focus routing, Quick Actions execution, project data, playback, render/export, project schema, and save/load; and avoid sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

First Beat Path work must derive setup, compose, arrange, mix, deliver, ready/review/blocker counts, next-step labels, read-only Decision Readout labels, direct beat-making Check Hint labels, jump targets, and Jump Result labels only from local Project, StyleProfile, Workflow Navigator, Beat Map, Export Preflight, deterministic export analysis data, the current First Beat Path summary, and the explicitly clicked First Beat Path step; keep path state, Decision Readout state, Check Hint state, and Jump Result state UI-local and out of saved project schema and undo history; route clicks only to existing Transport, Compose, Arrange, Mix, or Deliver panels; show Jump Result only after explicit First Beat Path jumps with target, path metric, audition cue, and next-check feedback; preserve Workflow Navigator item derivation, Beat Map scoring, Export Preflight scoring, Composer Guide, Review Queue, Finish Checklist, Next Move, Quick Actions, project data, playback, save/load, undo/redo, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid onboarding overlays, tutorials, macros, command chains, hidden generation, auto-run, autoplay, auto-save, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

First Beat Path Quick Action work must expose only the current First Beat Path next-step jump command and direct setup, compose, arrange, mix, and deliver step jump commands, derive commands only from existing First Beat Path summary steps, route command runs only through the existing First Beat Path jump handler to Transport, Compose, Arrange, Mix, or Deliver panels, show the same UI-local Jump Result feedback as visible First Beat Path clicks, keep result feedback UI-local and out of saved project schema and undo history, preserve First Beat Path scoring, Workflow Navigator item derivation, Beat Map scoring, Export Preflight scoring, Composer Guide, Review Queue, Finish Checklist, Next Move, project data, playback, save/load, undo/redo, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics, and avoid onboarding overlays, tutorials, macros, command chains, hidden generation, auto-run, autoplay, auto-save, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions First Beat Path result metric work must derive the explicit path action, current or direct path stage, destination panel, path context, current mode, selected Pattern, editable event count, and song length only from the current `first-beat-path-jump` or `first-beat-path-step-*` command title/detail plus local project state after the existing First Beat Path jump handler; preserve First Beat Path step derivation, path scoring, next-step selection, visible jump behavior, direct step command definitions, jump routing, Workflow Navigator, Beat Map, Export Preflight, Session Pass, Guide Quick Start, Quick Actions filtering, command search, Pinned Commands, Recent Commands, project data, playback, save/load, WAV/stem/MIDI export, Handoff Sheet, Handoff Pack, and local draft behavior; and avoid changing path recommendations, auto-running commands, macros, command chains, onboarding overlays, tutorials, autoplay, hidden generation, auto-save, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

First Beat Path Command Reference work must mark the Guide row as `Quick Actions / Readout`, reflect only the existing First Beat Path setup, compose, arrange, mix, and deliver progression, Decision Readout, jump command, direct step commands, and local Jump Result feedback; preserve path scoring, step derivation, jump routing, Quick Actions execution, project data, playback, render/export, project schema, and save/load; and avoid sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Beat Spine work must derive Setup, Drums, 808/Bass, Harmony, Melody, Sound, Arrange, Finish, ready/review/blocker counts, next-card labels, read-only Decision Readout labels, Decision Readout action labels, jump targets, action labels, Jump Result labels, Apply Result labels, any Quick Actions Beat Spine jump/apply commands, and any direct Beat Spine card jump/apply commands only from local Project, StyleProfile, Beat Readiness, arranged Pattern A/B/C event counts, Export Preflight, deterministic export analysis data, existing Beat Spine cards/actions, and explicit before/after project state around user-clicked Apply actions; keep spine state, Decision Readout state, Jump Result state, and Apply Result state UI-local and out of saved project schema and undo history; route Decision Readout actions, Jump clicks, current-target Beat Spine jump commands, and direct Beat Spine card jump commands only to existing Transport, Compose, Sound, Arrange, Master, or Deliver panels through the existing jump handler; route Decision Readout actions, Apply clicks, current-target Beat Spine apply commands, and direct Beat Spine card apply commands only through existing undoable Layer Starter, Sound Preset, Pattern Chain, or Master Finish handlers; disable direct Beat Spine card apply commands when the card has no action; update Jump Result only after explicit Jump clicks or command runs with destination, beat-core metric, audition cue, and next check; update Apply Result only after explicit Apply clicks or command runs with before/after metric, editable scope, audition cue, and next check; preserve First Beat Path, Workflow Navigator, Composer Guide, Beat Map, Export Preflight, Review Queue, Finish Checklist, Next Move, Quick Actions, project data, playback, save/load, undo/redo, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid onboarding overlays, tutorials, macros, command chains, hidden generation, auto-run, autoplay, auto-save, auto-export, sampling, imported audio, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Quick Actions Beat Spine result metric work must derive the explicit spine action, current or direct core card, destination or applied move, card context, current mode, selected Pattern, editable event count, and song length only from current Beat Spine command title/detail plus local project state after the existing Beat Spine jump/apply handler; preserve Beat Spine scoring, card derivation, next-card selection, Decision Readout behavior, visible jump/apply behavior, direct card command definitions, disabled apply semantics, jump/apply routing, First Beat Path, Mode Focus, Session Pass, Composer Guide, Beat Map, Workflow Navigator, Quick Actions filtering, command search, Pinned Commands, Recent Commands, project data, playback, save/load, WAV/stem/MIDI export, Handoff Sheet, Handoff Pack, and local draft behavior; avoid onboarding overlays, tutorials, macros, command chains, hidden generation, auto-run, autoplay, auto-save, auto-export, sampling, imported audio, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Beat Spine Command Reference work must mark the Guide row as `Quick Actions / Readout`, reflect only the existing Beat Spine sample-free core readout, Decision Readout action, Jump/Apply commands, direct card commands, local Jump Result feedback, and local Apply Result feedback; preserve spine scoring, card derivation, jump/apply routing, Quick Actions execution, project data, playback, render/export, project schema, and save/load; and avoid sampling, imported audio, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Session Pass work must derive Guided, Studio, Finish, and Delivery focus cards, active pass labels, read-only Decision Readout labels, and the visible Decision Readout action target only from existing First Beat Path, Review Queue, Finish Checklist, Export Preflight, and project mode state; keep pass state, Decision Readout state, visible Decision Readout action state, and Focus Result state UI-local and out of saved project schema and undo history; route the visible Decision Readout action, Focus buttons, the Quick Actions Session Pass focus command, and any direct Session Pass card Quick Actions only to existing Transport, Compose, Arrange, Mix, Master, or Deliver panels through the same explicit focus path; derive Focus Result labels, session metrics, audition cues, and next-check feedback only from the existing Session Pass summary/cards and the explicitly focused card, and show that result only after explicit visible or command focus; preserve all source scoring, Workflow Navigator behavior, Mode Focus, Composer Guide, Review Queue, Finish Checklist, Export Preflight, Next Move, Quick Actions, project data, playback, save/load, undo/redo, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid onboarding overlays, tutorials, macros, command chains, hidden generation, auto-run, autoplay, auto-save, auto-export, auto-fixing, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Session Pass result metric work must derive the explicit focus action, active or direct pass lane, destination, session context, current mode, selected Pattern, editable event count, and song length only from the current `session-pass-focus` or `session-pass-card-*` command title/detail plus local project state after the existing Session Pass focus handler; preserve Session Pass card derivation, pass scoring, active-card selection, Decision Readout behavior, visible focus behavior, direct card command definitions, focus routing, Workflow Navigator, Mode Focus, Composer Guide, Review Queue, Finish Checklist, Export Preflight, Next Move, Quick Actions filtering, command search, Pinned Commands, Recent Commands, project data, playback, save/load, WAV/stem/MIDI export, Handoff Sheet, Handoff Pack, and local draft behavior; and avoid changing pass recommendations, auto-running commands, macros, command chains, onboarding overlays, tutorials, autoplay, hidden generation, auto-save, auto-export, auto-fixing, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Session Pass Command Reference work must mark the Guide row as `Quick Actions / Readout`, reflect only the existing Session Pass Guided, Studio, Finish, and Delivery focus cards, Decision Readout, visible Decision Readout action, focus command, direct card commands, and local Focus Result feedback; preserve pass scoring, card derivation, focus routing, Quick Actions execution, project data, playback, render/export, project schema, and save/load; and avoid sampling, imported audio, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Pattern variation work must be deterministic, operate only on the selected Pattern A/B/C slot through undoable project history, keep generated drums, bass, melody, chords, velocities, timings, repeats, and probabilities manually editable, and avoid sampling, remote AI, or hidden audio assets.

Pattern Variation Preview work must derive suggestion readout labels, suggested current variation target, pre-click preset, before/after event posture, per-layer change counts, detail title, and preview target only from selected Pattern A/B/C data, existing `createPatternVariation` deterministic dry-run output, the local Pattern Variation suggestion helper, and UI-local preview preset state; keep suggestion readout state and preview state out of saved project schema and undo history; update visible preview target only from explicit Pattern Variation button focus or hover; preserve visible Apply buttons, Quick Actions, Pattern Variation Result, undo/redo, save/load, playback, WAV/stem/MIDI export, and Handoff semantics; and avoid hidden generation, autoplay, auto-arrangement, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Pattern Variation Result work must show feedback only as UI-local preset, before/after event-count, changed-event, audition-cue, and next-check labels derived from before/after selected Pattern A/B/C data around the existing `applyPatternVariation` path, keep visible buttons and Quick Actions shared through that path, preserve undo/redo, save/load, playback, WAV/stem/MIDI export, Handoff semantics, and project schema, and avoid sampling, imported audio, hidden generation, remote AI, accounts, analytics, or cloud sync.

Quick Actions Pattern Variation result metric work must derive selected Pattern, applied preset label, drum/808/chord/Synth counts, and selected-Pattern event count only from the explicit `pattern-variation-*` command id plus local selected Pattern A/B/C data after the existing `applyPatternVariation` path; preserve preset definitions, preview derivation, apply behavior, Pattern A/B/C events outside explicit variation actions, playback, render/export, project schema, remote behavior, and sampling boundaries.

Quick Actions Pattern Variation work must expose only the existing Subtle, Hook, and Break deterministic variation presets as explicit command-palette actions routed through the existing `applyPatternVariation` path, preserve Pattern A/B/C independence, command search, scope filters, Spotlight, Pinned Commands, Recent Commands, command result feedback, undo/redo, save/load, playback, WAV/stem/MIDI export, and Handoff semantics, and avoid new variation algorithms, macros, command chains, autoplay, auto-arrangement, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Pattern Variation Command Reference work must mark the Create row as `Quick Actions / Readout`, reflect only the existing local selected-Pattern suggestion readout, current Subtle/Hook/Break variation target, Pattern Variation Preview, direct variation commands, and local Pattern Variation Result feedback; preserve suggestion derivation, preview derivation, deterministic dry-run output, preset definitions, apply behavior, result metric derivation, Pattern A/B/C events outside explicit Pattern Variation actions, arrangement, mixer, master, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid new variation algorithms, tutorials, macros, command chains, hidden generation, automatic arrangement, automatic export, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Pattern Clone Pad and Quick Actions work must derive any suggestion readout labels, safest target slot, hook/breakdown suggestion, target posture, and expected changed-event count only from selected Pattern A/B/C event counts, existing deterministic `createPatternVariation` dry-run output, and local Pattern Clone suggestion helpers; keep suggestion readout state out of saved project schema and undo history; clone the selected Pattern A/B/C data into an explicit target slot only after a user click or explicit command click; route visible Pattern Clone Pad clicks through the existing clone-and-vary handler; route Quick Actions Pattern Clone commands only through the existing clone-and-vary handler; apply only existing deterministic variation presets to the clone; switch editing focus to the target slot; keep results manually editable through existing pattern editors; show Pattern Clone Result feedback only as UI-local source/target/variation/changed-event/audition/next-check labels derived from before/after Pattern A/B/C data; preserve arrangement block assignments unless the user changes them separately; preserve realtime playback plus WAV/stem/MIDI export semantics; and avoid command chains, sampling, imported audio, hidden generation, remote AI, accounts, analytics, or cloud sync.

Quick Actions Pattern Clone result metric work must derive source Pattern, target Pattern, applied preset label, target drum/music counts, and target event count only from the explicit `pattern-clone-*` command id plus local source/target Pattern A/B/C data after the existing clone-and-vary path; preserve suggestion derivation, target selection, variation preset selection, clone-and-vary behavior, Pattern A/B/C events outside explicit clone actions, playback, render/export, project schema, remote behavior, and sampling boundaries.

Pattern Clone Command Reference work must mark the Create row as `Quick Actions / Readout`, reflect only the existing local selected-Pattern clone suggestion readout, safest target slot, hook/breakdown variation suggestion, direct clone-to-A/B/C commands, and local Pattern Clone Result feedback; preserve suggestion derivation, safest target selection, variation preset selection, deterministic dry-run output, clone-and-vary behavior, result metric derivation, Pattern A/B/C events outside explicit Pattern Clone actions, arrangement, mixer, master, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid new clone algorithms, tutorials, macros, command chains, hidden generation, automatic arrangement, automatic export, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Pattern Stack Pad work must derive 808/bass notes, chord events, and Synth melody notes from the current key after an explicit click, update only the selected Pattern A/B/C bass, chord, and melody event data through undoable project history, keep results manually editable through existing lane editors and inspectors, preserve realtime playback plus WAV/stem/MIDI export semantics, and avoid sampling, imported audio, hidden generation, remote AI, accounts, analytics, or cloud sync.

Pattern Stack Preview work must derive selected Pattern A/B/C 808/chord/Synth posture labels, suggested Pattern Stack targets, pre-click move counts, any Quick Actions Pattern Stack command target, and any direct Pattern Stack pad Quick Actions only from current local key, selected Pattern A/B/C bass note, chord event, and melody note data, and existing Pattern Stack option targets; keep the preview UI-local and out of saved project schema and undo history; route Pattern Stack Pad clicks, the current-target Quick Actions Pattern Stack command, and direct Pattern Stack pad command runs only through the existing undoable Pattern Stack apply path; disable any direct Pattern Stack pad command when that stack already matches the selected Pattern; preserve Pattern Stack definitions and apply behavior, selected-note and selected-chord edit tools, Pattern A/B/C independence, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid automatic arrangement writing, hidden generation, sampling, imported audio, remote AI, plugin hosting, accounts, analytics, or cloud sync.

Quick Actions Pattern Stack result metric work must derive the explicit stack action, current preview or direct stack lane, selected Pattern, applied stack context, 808/chord/Synth posture, editable event count, drum/music layer counts, and song length only from current Pattern Stack command title/detail plus local project state after the existing Pattern Stack apply path; preserve Pattern Stack preview derivation, stack definitions, direct stack command definitions, disabled-state behavior, visible Pattern Stack Result behavior, apply routing, Pattern A/B/C event semantics, Layer Starter, Pattern DNA, Pattern Compare, Pattern Clone, Composer Actions, selected-note and selected-chord tools, arrangement data, Quick Actions filtering, command search, Pinned Commands, Recent Commands, project data, playback, save/load, WAV/stem/MIDI export, Handoff Sheet, Handoff Pack, and local draft behavior; avoid onboarding overlays, tutorials, macros, command chains, hidden generation, auto-run, autoplay, automatic arrangement writing, auto-save, auto-export, sampling, imported audio, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Pattern Stack Result work must update result labels only after explicit Pattern Stack Pad clicks, derive applied stack, Pattern A/B/C scope, before/after 808/chord/Synth counts, changed-layer impact, audition cue, and next-check text only from local before/after selected Pattern A/B/C data and existing Pattern Stack definitions, derive Quick Action result metrics only from the explicitly clicked or command-run stack plus local selected Pattern event data, keep result state UI-local and out of saved project schema and undo history, preserve Pattern Stack Preview, Pattern Stack Pad definitions and apply behavior, selected-note and selected-chord edit tools, Pattern A/B/C independence, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics, and avoid modal confirmations, autoplay, automatic arrangement writing, hidden generation, sampling, imported audio, remote AI, plugin hosting, accounts, analytics, or cloud sync.

Pattern Stack Command Reference work must mark the Create row as `Quick Actions / Readout`, reflect only the existing selected Pattern A/B/C 808/chord/Synth posture, Pattern Stack Preview, current suggested stack, direct stack pad commands, Quick Actions Pattern Stack command, and local Pattern Stack Result feedback; preserve preview derivation, stack definitions, suggested stack selection, visible pad behavior, Quick Actions routing, direct stack pad commands, disabled-state handling, result metrics, audition cues, next-check text, Pattern Stack apply behavior, selected-note and selected-chord tools, Pattern A/B/C independence, arrangement, mixer, sound design, master state, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid hidden generation, automatic arrangement writing, command chains, auto-apply, autoplay, automatic export, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Groove Feel Pad work must derive drum timing, drum chance, note chance, and chord chance changes from explicit local feel presets, update only the selected Pattern A/B/C timing and probability event data through undoable project history, keep event counts stable and results manually editable through existing timing, chance, note, and chord controls, preserve realtime playback plus WAV/stem/MIDI export semantics, and avoid sampling, imported audio, hidden generation, remote AI, accounts, analytics, or cloud sync.

Drum Foundation Pad work must derive selected Pattern A/B/C kick, clap, hat, and perc foundations from explicit local rhythm presets, update only selected Pattern A/B/C drum pattern, velocity, timing, probability, and hat repeat data through undoable project history, keep results manually editable through the drum grid and inspector, preserve realtime playback plus WAV/stem export semantics, and avoid sampling, imported audio, hidden generation, remote AI, accounts, analytics, or cloud sync.

Drum Accent Pad work must derive drum velocity changes from explicit local accent presets, update only the selected Pattern A/B/C active drum velocity data through undoable project history, keep active drum steps stable and results manually editable through existing velocity controls, preserve realtime playback plus WAV/stem export semantics, and avoid sampling, imported audio, hidden generation, remote AI, accounts, analytics, or cloud sync.

Drum step velocity meter work must derive visible active drum-step velocity labels only from existing `drumStepVelocity`, keep the existing velocity bar, step number, chance badges, hat repeat labels, timing badges, selected-drum state, playhead state, click-to-toggle behavior, and editor audition unchanged, stay UI-only with no project schema, save/load, playback, render, stem export, MIDI export, Handoff, sampling, recording, remote AI, or cloud scope changes, and keep text contained inside compact drum step cells.

Drum Move Preview work must derive selected Pattern A/B/C drum posture labels, suggested Drum Foundation/Groove Feel/Drum Accent targets, any Quick Actions Drum Move command target, and pre-click move counts only from local drum pattern, velocity, timing, probability, hat repeat, note chance, chord chance data, and existing Drum Foundation/Groove Feel/Drum Accent option targets, keep the preview UI-local and out of saved project schema and undo history, route pad clicks and any command runs only through existing Drum Foundation, Groove Feel, or Drum Accent apply behavior, preserve Drum Foundation, Groove Feel, and Drum Accent definitions and apply behavior, selected-drum edit tools, Pattern A/B/C independence, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics, and avoid automatic drum writing, hidden generation, sampling, imported audio, remote AI, plugin hosting, accounts, analytics, or cloud sync.

Drum Move Result work must update result labels only after explicit Drum Foundation, Groove Feel, or Drum Accent Pad clicks or Quick Actions Drum Move command runs, derive applied move, selected Pattern A/B/C scope, before/after hit, timing, chance, and velocity posture, changed-move impact, audition cue, and next-check text only from local before/after selected Pattern A/B/C data and existing Drum Foundation/Groove Feel/Drum Accent definitions, keep result state UI-local and out of saved project schema and undo history, preserve Drum Move Preview, Drum Foundation, Groove Feel, and Drum Accent definitions and apply behavior, selected-drum edit tools, Pattern A/B/C independence, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics, and avoid modal confirmations, autoplay, automatic drum writing, hidden generation, sampling, imported audio, remote AI, plugin hosting, accounts, analytics, or cloud sync.

Drum Move Command Reference work must mark the Create row as `Quick Actions / Readout`, reflect only the existing local Drum Move Preview, current Drum Foundation/Groove Feel/Drum Accent target, direct Drum Move command, and local Drum Move Result feedback; preserve preview derivation, target selection, Drum Foundation/Groove Feel/Drum Accent definitions and apply behavior, result metric derivation, Pattern A/B/C event data outside explicit Drum Move actions, selected-drum edit tools, arrangement, mixer, master, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid new drum algorithms, tutorials, macros, command chains, hidden generation, automatic arrangement, automatic export, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Selected drum hit clipboard/readout/audition and Quick Actions work must keep copied hit state, audition state, and pocket readout labels UI-local and out of saved project schema, derive selected-drum pocket readouts and command labels only from the selected Pattern A/B/C lane, step, active state, velocity, chance, microtiming, and hat-repeat fields, route Quick Actions selected-drum step, step reset, duplicate, beat duplicate, previous beat duplicate, and edit commands only through existing selected-drum step-move, step reset, duplicate, beat duplicate, previous beat duplicate, velocity, chance, timing, hat-repeat, copy, paste, and delete handlers, route selected-drum audition only through UI-local one-shot built-in drum synthesis, copy only active drum hits, keep selected-drum-copy UI-local until a separate explicit paste writes project data, paste copied hits only into the copied lane's next empty step in the selected Pattern A/B/C slot, step reset selected hits only onto the selected lane's nearest empty 4-step beat-grid anchor in the selected Pattern A/B/C slot without changing the drum clipboard, duplicate selected hits only into the selected lane's next empty step in the selected Pattern A/B/C slot without changing the drum clipboard, beat duplicate selected hits only into the selected lane's next empty 4-step beat-grid anchor in the selected Pattern A/B/C slot without changing the drum clipboard, previous beat duplicate selected hits only into the selected lane's nearest earlier empty 4-step beat-grid anchor in the selected Pattern A/B/C slot without changing the drum clipboard, preserve lane, velocity, chance, microtiming, and hat-repeat shape, avoid overlapping moved, step-reset, pasted, duplicated, beat-duplicated, or previous-beat-duplicated hits in the same lane and step, keep move, step reset, paste, duplicate, beat duplicate, previous beat duplicate, and selected-drum edit commands undoable and manually editable through existing drum controls, preserve realtime playback plus WAV/stem export semantics, and avoid system clipboard integration, sampling, imported audio, hidden generation, remote AI, accounts, analytics, or cloud sync.

Selected drum step reset work must keep the selected-drum step reset command scoped to the selected Pattern A/B/C slot; derive the target only from the selected drum lane, selected drum step, selected Pattern A/B/C drum hits, and existing 16-step grid; snap only to the nearest 4-step anchor in the same lane when that lane/step is empty; disable reset when no active selected drum hit exists, the selected drum hit already sits on the target anchor, or the target same-lane step is occupied; route reset only through the existing undoable selected-pattern drum edit path; set only the drum step; preserve lane, velocity, chance, microtiming, hat repeat, drum clipboard, playback, WAV/stem export, and manual drum controls; keep the reset drum hit selected and undoable; and avoid whole-pattern quantize, rhythm rewriting, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Selected drum beat duplicate work must keep the selected-drum beat duplicate command scoped to the selected Pattern A/B/C slot; derive the target only from the selected drum lane, selected drum step, selected Pattern A/B/C drum hits, and existing 16-step grid; duplicate only to the next later 4-step anchor in the same lane when that lane/step is empty; disable beat duplicate when no active selected drum hit exists or no target anchor is available; route the duplicate only through the existing undoable selected-pattern drum edit path; preserve lane, velocity, chance, microtiming, hat repeat, drum clipboard, playback, WAV/stem export, and manual drum controls; select the duplicated drum hit after a successful edit; and avoid rhythm rewriting, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Visible selected-drum next beat duplicate button work must keep the button scoped to the selected Pattern A/B/C slot, derive the same next later same-lane 4-step target as the selected-drum beat duplicate command, disable when no active selected drum hit or no valid target exists, and route only through the existing undoable selected-pattern drum edit path without changing drum clipboard, playback, export, sampling scope, or project schema.

Selected drum previous beat duplicate work must keep the selected-drum previous beat duplicate command and visible selected-drum previous-beat button scoped to the selected Pattern A/B/C slot; derive the target only from the selected drum lane, selected drum step, selected Pattern A/B/C drum hits, and existing 16-step grid; duplicate only to the nearest earlier 4-step anchor in the same lane when that lane/step is empty; enforce that 4-step anchor rule inside the handler as well as the Quick Actions and visible-button target derivation; disable previous beat duplicate when no active selected drum hit exists or no earlier target anchor is available; route the duplicate only through the existing undoable selected-pattern drum edit path; preserve lane, velocity, chance, microtiming, hat repeat, drum clipboard, playback, WAV/stem export, and manual drum controls; select the duplicated drum hit after a successful edit; and avoid rhythm rewriting, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Pattern Fill Preview work must derive suggestion readout labels, suggested current tail-move target, pre-click preset, before/after event posture, per-layer tail change counts, detail title, and preview target only from selected Pattern A/B/C data, current key, existing `applyPatternFillPreset` deterministic dry-run output, the shared Pattern Fill suggestion helper used by Next Move, and UI-local preview preset state; keep suggestion readout state and preview state out of saved project schema and undo history; update visible preview target only from explicit Pattern Fill button focus or hover; preserve visible Apply buttons, Quick Actions, Pattern Fill Result, undo/redo, save/load, playback, WAV/stem/MIDI export, and Handoff semantics; and avoid hidden generation, autoplay, auto-arrangement, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Pattern Fill work must be deterministic, operate only on the selected Pattern A/B/C slot through undoable project history, edit only local drum, 808/bass, melody, and chord-tail event data, keep Drum Fill/808 Pickup/Melody Turn/Clear Tail results manually editable, show Pattern Fill Result feedback only as UI-local preset, before/after event-count, changed-tail-event, audition-cue, and next-check labels derived from before/after selected Pattern A/B/C data, preserve realtime playback plus WAV/stem/MIDI export semantics, and avoid sampling, remote AI, hidden randomness, or hidden audio assets.

Quick Actions Pattern Fill work must expose Drum Fill, 808 Pickup, Melody Turn, and Clear Tail only as explicit command-palette actions routed through the existing `applyPatternFill` path, keep Pattern Fill Result feedback UI-local and shared with visible Pattern Fill buttons, preserve command search, scope filters, Spotlight, Pinned Commands, Recent Commands, undo/redo, save/load, playback, WAV/stem/MIDI export, and Handoff semantics, and avoid macros, command chains, autoplay, auto-arrangement, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Pattern Fill result metric work must derive selected Pattern, applied preset label, drum/808/chord/Synth counts, and selected-Pattern event count only from the explicit `fill-*` command id plus local selected Pattern A/B/C data after the existing `applyPatternFill` path; preserve preset definitions, preview derivation, apply behavior, Pattern A/B/C events outside explicit fill actions, playback, render/export, project schema, remote behavior, and sampling boundaries.

Pattern Fill Command Reference work must mark the Create row as `Quick Actions / Readout`, reflect only the existing local selected-Pattern tail-move suggestion readout, current Drum Fill/808 Pickup/Melody Turn/Clear Tail target, Pattern Fill Preview, direct fill commands, and local Pattern Fill Result feedback; preserve suggestion derivation, preview derivation, deterministic dry-run output, preset definitions, apply behavior, result metric derivation, Pattern A/B/C events outside explicit Pattern Fill actions, arrangement, mixer, master, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid new fill algorithms, tutorials, macros, command chains, hidden generation, automatic arrangement, automatic export, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

808 Bassline Pad work must derive 808/bass notes from the current key after an explicit click, update only the selected Pattern A/B/C bass notes through undoable project history, keep results manually editable through the note grid and inspector, preserve glide, chance, realtime playback plus WAV/stem/MIDI export semantics, and avoid sampling, imported audio, hidden generation, remote AI, accounts, analytics, or cloud sync.

808 Glide Pad work must derive 808/bass note length, glide, and chance changes from explicit local movement presets, update only the selected Pattern A/B/C bass note data through undoable project history, keep bass note counts and pitches stable, keep results manually editable through the note grid and inspector, preserve realtime playback plus WAV/stem/MIDI export semantics, and avoid sampling, imported audio, hidden generation, remote AI, accounts, analytics, or cloud sync.

808 Contour Pad work must derive 808/bass pitch direction changes from explicit local contour presets, update only the selected Pattern A/B/C bass note data through undoable project history, keep bass note counts, step positions, lengths, glide flags, and chance values stable, keep results manually editable through the note grid and inspector, preserve realtime playback plus WAV/stem/MIDI export semantics, and avoid sampling, imported audio, hidden generation, remote AI, accounts, analytics, or cloud sync.

808 Move Preview work must derive bass line labels, suggested 808 Bassline/Glide/Contour targets, any Quick Actions 808 Move command target, and pre-click move counts only from current local key, selected Pattern A/B/C bass note data, and existing 808 Bassline/Glide/Contour option targets, keep the preview UI-local and out of saved project schema and undo history, route pad clicks and any command runs only through existing 808 Bassline, 808 Glide, or 808 Contour apply behavior, preserve 808 Bassline, 808 Glide, and 808 Contour definitions and apply behavior, selected-note edit tools, Pattern A/B/C independence, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics, and avoid automatic bass writing, hidden generation, sampling, imported audio, remote AI, plugin hosting, accounts, analytics, or cloud sync.

808 Move Result work must update result labels only after explicit 808 Bassline, 808 Glide, or 808 Contour Pad clicks or Quick Actions 808 Move command runs, derive applied move, selected Pattern A/B/C scope, before/after 808 note count, rhythm, glide, chance, and pitch-range posture, changed-move impact, audition cue, and next-check text only from local before/after selected Pattern A/B/C bass note data and existing 808 Bassline/Glide/Contour definitions, keep result state UI-local and out of saved project schema and undo history, preserve 808 Move Preview, 808 Bassline, 808 Glide, and 808 Contour definitions and apply behavior, selected-note edit tools, Pattern A/B/C independence, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics, and avoid modal confirmations, autoplay, automatic bass writing, hidden generation, sampling, imported audio, remote AI, plugin hosting, accounts, analytics, or cloud sync.

808 Move Command Reference work must mark the Create row as `Quick Actions / Readout`, reflect only the existing local 808 Move Preview, current 808 Bassline/Glide/Contour target, direct 808 Move command, and local 808 Move Result feedback; preserve preview derivation, target selection, 808 Bassline/Glide/Contour definitions and apply behavior, result metric derivation, Pattern A/B/C event data outside explicit 808 Move actions, selected-note edit tools, arrangement, mixer, master, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid new bass algorithms, tutorials, macros, command chains, hidden generation, automatic arrangement, automatic export, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Melody Motif Pad work must derive Synth melody notes from the current key after an explicit click, update only the selected Pattern A/B/C melody notes through undoable project history, keep results manually editable through the note grid and inspector, preserve realtime playback plus WAV/stem/MIDI export semantics, and avoid sampling, imported audio, hidden generation, remote AI, accounts, analytics, or cloud sync.

Melody Accent Pad work must derive Synth melody velocity and chance changes from explicit local accent presets, update only the selected Pattern A/B/C melody note data through undoable project history, keep melody note counts, steps, pitches, and lengths stable, keep results manually editable through the note grid and inspector, preserve realtime playback plus WAV/stem/MIDI export semantics, and avoid sampling, imported audio, hidden generation, remote AI, accounts, analytics, or cloud sync.

Melody Contour Pad work must derive Synth melody pitch contour, length, velocity, and chance changes from explicit local contour presets, update only the selected Pattern A/B/C melody note data through undoable project history, preserve melody note counts and step positions, keep results manually editable through the note grid and inspector, preserve realtime playback plus WAV/stem/MIDI export semantics, and avoid sampling, imported audio, hidden generation, remote AI, accounts, analytics, or cloud sync.

Melody Move Preview work must derive phrase labels, suggested motif/accent/contour targets, any Quick Actions Melody Move command target, and pre-click move counts only from current local key, selected Pattern A/B/C melody note data, and existing Melody Motif/Accent/Contour option targets, keep the preview UI-local and out of saved project schema and undo history, route pad clicks and any command runs only through existing Melody Motif, Melody Accent, or Melody Contour apply behavior, preserve Melody Motif, Melody Accent, and Melody Contour definitions and apply behavior, selected-note edit tools, Pattern A/B/C independence, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics, and avoid automatic melody writing, hidden generation, sampling, imported audio, remote AI, plugin hosting, accounts, analytics, or cloud sync.

Melody Move Result work must update result labels only after explicit Melody Motif, Melody Accent, or Melody Contour Pad clicks or Quick Actions Melody Move command runs, derive applied move, selected Pattern A/B/C scope, before/after Synth note count, rhythm, pitch-range, velocity, and chance posture, changed-move impact, audition cue, and next-check text only from local before/after selected Pattern A/B/C melody note data and existing Melody Motif/Accent/Contour definitions, keep result state UI-local and out of saved project schema and undo history, preserve Melody Move Preview, Melody Motif, Melody Accent, and Melody Contour definitions and apply behavior, selected-note edit tools, Pattern A/B/C independence, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics, and avoid modal confirmations, autoplay, automatic melody writing, hidden generation, sampling, imported audio, remote AI, plugin hosting, accounts, analytics, or cloud sync.

Melody Move Command Reference work must mark the Create row as `Quick Actions / Readout`, reflect only the existing local Melody Move Preview, current Melody Motif/Accent/Contour target, direct Melody Move command, and local Melody Move Result feedback; preserve preview derivation, target selection, Melody Motif/Accent/Contour definitions and apply behavior, result metric derivation, Pattern A/B/C event data outside explicit Melody Move actions, selected-note edit tools, arrangement, mixer, master, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid new melody algorithms, tutorials, macros, command chains, hidden generation, automatic arrangement, automatic export, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Beat Readiness work must derive checks, focus targets, Focus Readout labels, the visible readout action, the Quick Actions Beat Readiness focus command, any direct readiness-check Quick Actions, Focus Result labels, readiness metrics, audition cues, and next-check feedback only from editable Pattern A/B/C, arrangement, deterministic export analysis data, existing Beat Readiness check ids, the visible checks, the explicitly clicked or command-run check, and the current highest-priority readiness check; show Focus Result only after explicit visible or command focus; keep focused check state, readout state, visible readout action state, and result feedback UI-local and out of saved project schema and undo history; route the visible readout action, visible Focus buttons, and command runs only to existing Compose, Arrange, Master, or Deliver panels through the existing focus handler; remain read-only, avoid mutating project state or generating musical events, preserve realtime playback and WAV/stem/MIDI export semantics, and avoid imported audio, sampling, plugin hosting, remote AI, or remote analysis dependencies.

Quick Actions Beat Readiness result metric work must derive the explicit readiness focus action, current priority or direct readiness check, destination panel, readiness status/context, selected Pattern, editable event count, drum/music layer counts, arrangement block count, and song length only from current Beat Readiness command title/detail plus local project state after the existing Beat Readiness focus path; preserve Beat Readiness check derivation, scoring, card order, focus-target selection, direct readiness-check command definitions, visible Focus Readout behavior, focus routing, Focus Result behavior, Composer Guide, Key Compass, Groove Compass, Pattern DNA, Layer Starter, Pattern Stack, Listening Pass, Beat Passport, Production Snapshot, Review Queue, Export Preflight, Pattern A/B/C event data, arrangement data, Quick Actions filtering, command search, Pinned Commands, Recent Commands, project data, playback, save/load, WAV/stem/MIDI export, Handoff Sheet, Handoff Pack, and local draft behavior; avoid onboarding overlays, tutorials, macros, command chains, hidden generation, auto-run, autoplay, automatic arrangement writing, auto-save, auto-export, sampling, imported audio, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Beat Readiness Command Reference work must mark the Guide row as `Quick Actions / Readout`, reflect only the existing local drums, 808/bass, melody/chords, arrangement, export checks, Focus Readout action, Beat Readiness focus command, direct readiness-check commands, and local Focus Result feedback; preserve Beat Readiness check derivation, check order, scoring, focus routing, Quick Actions execution, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid tutorials, macros, command chains, hidden generation, automatic fixes, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Beat Passport work must derive compact target, length, Pattern A/B/C, readiness, export, stem, and master posture summaries only from local project state, Beat Readiness, deterministic export analysis, deterministic stem analysis, selected Delivery Target, and master state; remain read-only; avoid mutating project state or triggering renders/downloads; and avoid imported audio, sampling, platform compliance, LUFS, true-peak, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync.

Beat Passport Focus work must derive focus targets, visible readout action targets, the Quick Actions Beat Passport focus command, any direct Beat Passport metric Quick Actions, Focus Result labels, passport metrics, audition cues, and next-check feedback only from existing Beat Passport metric ids, existing Beat Passport metrics, the visible summary, the explicitly clicked or command-run metric, and the current highest-priority beat identity metric; show Focus Result only after explicit visible or command focus; keep focused metric state, visible readout action state, and result feedback UI-local and out of saved project schema; route the visible readout action, visible Focus buttons, and command runs only to existing Compose, Arrange, Master, or Deliver panels through the existing focus handler; preserve metric derivation, metric order, visible focus buttons, Production Snapshot, Finish Checklist, Review Queue, Beat Map, Structure Lens, Song Form Overview, Mix Coach, Master Finish, Handoff Pack, Handoff Sheet, playback, save/load, undo/redo, WAV/stem/MIDI export, and project data semantics; avoid changing passport scoring, project musical data, arrangement, mixer, master, targets, playback, renders, exports, or Handoff state; and avoid auto-targeting, auto-arrangement, auto-mastering, auto-export, autoplay, command chains, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Beat Passport result metric work must derive the explicit beat identity focus action, current priority or direct passport metric, destination panel, metric status/context, selected Pattern, editable event count, Pattern A/B/C usage, readiness/export/stem/master posture, and song length only from current Beat Passport command title/detail plus local project state after the existing Beat Passport focus path; preserve Beat Passport metric derivation, metric order, focus-target selection, direct metric command definitions, visible Focus Readout behavior, focus routing, Focus Result behavior, Beat Readiness, Listening Pass, Production Snapshot, Review Queue, Export Preflight, Mix Coach, Delivery Target, Session Brief, Pattern A/B/C event data, arrangement data, Quick Actions filtering, command search, Pinned Commands, Recent Commands, project data, playback, save/load, WAV/stem/MIDI export, Handoff Sheet, Handoff Pack, and local draft behavior; avoid onboarding overlays, tutorials, macros, command chains, hidden generation, auto-run, autoplay, automatic arrangement writing, auto-save, auto-export, sampling, imported audio, audio analysis, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Beat Passport Command Reference work must mark the Guide row as `Quick Actions / Readout`, reflect only the existing local beat identity, target, length, Pattern A/B/C, readiness, export, stems, master posture, Focus Readout action, Beat Passport focus command, direct metric commands, and local Focus Result feedback; preserve Beat Passport metric derivation, metric order, focus routing, Quick Actions execution, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid tutorials, macros, command chains, hidden generation, automatic fixes, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Production Snapshot work must derive target fit, song form, Pattern A/B/C coverage, mix posture, handoff posture, Priority Readout labels, and visible priority action targets only from local project state, Beat Readiness, arrangement blocks, Pattern A/B/C event data, deterministic export analysis, deterministic stem analysis, selected Delivery Target, Mix Coach checks, Session Brief fields, and existing Production Snapshot summary metrics/focus labels; remain read-only; keep Priority Readout state and visible priority action state UI-local and out of saved project schema and undo history; route visible Priority action clicks only through the existing Production Snapshot focus handler; avoid mutating project state, triggering exports, saving snapshots, applying fixes, or changing recommendations; preserve realtime playback, save/load, undo/redo, WAV/stem/MIDI export, Handoff Sheet, Beat Passport, Finish Checklist, Review Queue, Beat Map, Structure Lens, Song Form Overview, Next Move, Mix Coach, and Master Finish semantics; and avoid sampling, imported audio, plugin hosting, remote AI, remote analysis, platform compliance, LUFS, true-peak, publishing, licensing, professional mastering, accounts, analytics, or cloud sync claims.

Production Snapshot Focus work must derive focus targets, visible Priority action targets, the Quick Actions Production Snapshot focus command, any direct Production Snapshot metric Quick Actions, Focus Result labels, snapshot metrics, audition cues, and next-check feedback only from existing Production Snapshot metric ids, existing Production Snapshot metrics, the visible summary, the visible Priority action, the explicitly clicked or command-run metric, and the current highest-priority session scan metric; show Focus Result only after explicit visible or command focus; keep focused metric state, visible priority action state, and result feedback UI-local and out of saved project schema; route visible Priority action clicks, focus clicks, and command runs only to existing Compose, Arrange, Mix, or Deliver panels through the existing focus handler; preserve metric derivation, metric order, visible focus buttons, Beat Passport, Finish Checklist, Review Queue, Beat Map, Structure Lens, Song Form Overview, Mix Coach, Master Finish, Handoff Pack, Handoff Sheet, playback, save/load, undo/redo, WAV/stem/MIDI export, and project data semantics; avoid changing snapshot scoring, recommendations, project musical data, arrangement, mixer, master, targets, Session Brief, playback, exports, or Handoff state; and avoid auto-targeting, auto-arrangement, auto-mixing, auto-export, autoplay, command chains, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Production Snapshot result metric work must derive the explicit session scan action, current priority or direct snapshot metric, destination panel, metric status/context, selected Pattern, editable event count, Pattern A/B/C usage, target/form/mix/handoff posture, and song length only from current Production Snapshot command title/detail plus local project state after the existing Production Snapshot focus path; preserve Production Snapshot metric derivation, metric order, focus-target selection, direct metric command definitions, visible Priority action behavior, focus routing, Focus Result behavior, Beat Passport, Beat Readiness, Listening Pass, Review Queue, Export Preflight, Mix Coach, Delivery Target, Session Brief, Pattern A/B/C event data, arrangement data, Quick Actions filtering, command search, Pinned Commands, Recent Commands, project data, playback, save/load, WAV/stem/MIDI export, Handoff Sheet, Handoff Pack, and local draft behavior; avoid onboarding overlays, tutorials, macros, command chains, hidden generation, auto-run, autoplay, automatic arrangement writing, auto-save, auto-export, sampling, imported audio, audio analysis, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Production Snapshot Command Reference work must mark the Guide row as `Quick Actions / Readout`, reflect only the existing local target, song form, Pattern A/B/C coverage, mix, handoff, Priority Readout, Focus Readout, Production Snapshot focus command, direct metric commands, and local Focus Result feedback; preserve Production Snapshot metric derivation, metric order, priority derivation, focus routing, Quick Actions execution, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid tutorials, macros, command chains, hidden generation, automatic fixes, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Finish Checklist work must derive Compose, Arrange, Mix, Master, Master Automation, Handoff readiness, Priority Readout labels, and visible priority action targets only from local project state, Beat Readiness, Structure Lens posture, deterministic export analysis, deterministic stem analysis, Mix Coach checks, selected Delivery Target, Master Automation posture, Session Brief fields, and existing Finish Checklist cards/focus labels; avoid mutating project state, triggering exports, or changing recommendations; keep Priority Readout state and visible priority action state UI-local and out of saved project schema and undo history; route visible Priority action clicks only to existing Compose, Arrange, Mix, Master, or Deliver panels through the existing focus handler; preserve realtime playback, save/load, undo/redo, WAV/stem/MIDI export, Handoff Sheet, Beat Passport, Beat Map, Next Move, Mix Coach, Master Finish, and Master Automation semantics; and avoid sampling, imported audio, plugin hosting, remote AI, remote analysis, platform compliance, LUFS, true-peak, publishing, licensing, professional mastering, accounts, analytics, or cloud sync claims.

Finish Checklist Focus work must derive focus targets, the Quick Actions Finish Checklist focus command, any direct Finish Checklist card Quick Actions, Focus Result labels, checklist metrics, audition cues, and next-check feedback only from existing Finish Checklist card ids, existing Finish Checklist cards, the visible summary, the explicitly clicked or command-run card, and the current highest-priority finish card; show Focus Result only after explicit visible or command focus; keep focused card state and result feedback UI-local and out of saved project schema and undo history; route clicks and command runs only to existing Compose, Arrange, Mix, Master, or Deliver panels through the existing focus handler; preserve checklist card derivation, card order, visible focus buttons, checklist scoring, recommendations, project musical data, arrangement, mixer, master, exports, playback, save/load, undo/redo, and Handoff state; and avoid auto-fixing, auto-mastering, auto-export, autoplay, macros, command chains, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Finish Checklist result metric work must derive the explicit finish-pass action, current priority or direct checklist card, destination panel, checklist status/context, selected Pattern, editable event count, Pattern A/B/C usage, arrangement block count, finish readiness summary, and song length only from current Finish Checklist command title/detail plus local project state after the existing Finish Checklist focus path; preserve Finish Checklist card derivation, card order, scoring, focus-target selection, direct card command definitions, visible Priority action behavior, focus routing, Focus Result behavior, Beat Passport, Beat Readiness, Listening Pass, Production Snapshot, Review Queue, Export Preflight, Mix Coach, Delivery Target, Session Brief, Pattern A/B/C event data, arrangement data, Quick Actions filtering, command search, Pinned Commands, Recent Commands, project data, playback, save/load, WAV/stem/MIDI export, Handoff Sheet, Handoff Pack, and local draft behavior; avoid onboarding overlays, tutorials, macros, command chains, hidden generation, auto-run, autoplay, automatic arrangement writing, auto-save, auto-export, sampling, imported audio, audio analysis, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Finish Checklist Command Reference work must mark the Finish row as `Quick Actions / Readout`, reflect only the existing local Compose, Arrange, Mix, Master, Master Automation, and Handoff readiness cards, Priority Readout, Finish Checklist focus command, direct card commands, and local Focus Result feedback; preserve checklist card derivation, card order, Priority Readout derivation, visible priority action behavior, focus routing, Quick Actions execution, direct card command routing, result labels, disabled-state behavior, project musical data, arrangement, mixer, master, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid auto-fixing, auto-mastering, auto-export, tutorials, macros, command chains, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Review Queue work must derive prioritized production issues, Priority Readout labels, and visible priority action targets only from local project state, Beat Readiness, Structure Lens signals, deterministic export analysis, deterministic stem analysis, Mix Coach checks, selected Delivery Target, and Session Brief fields; avoid mutating project state, triggering exports, auto-fixing, or changing recommendations; keep issue state, Priority Readout state, and visible priority action state out of saved project schema and undo history; route visible Priority action clicks only to existing Compose, Arrange, Mix, Master, or Deliver panels through the existing focus handler; preserve realtime playback, save/load, undo/redo, WAV/stem/MIDI export, Handoff Sheet, Beat Passport, Finish Checklist, Beat Map, Next Move, Mix Coach, and Master Finish semantics; and avoid sampling, imported audio, plugin hosting, remote AI, remote analysis, platform compliance, LUFS, true-peak, publishing, licensing, professional mastering, accounts, analytics, or cloud sync claims.

Review Queue Focus and Fix work must derive focus targets, fix targets, Review Fix Preview labels, the Quick Actions Review Queue focus command, the Quick Actions Review Fix command, any direct Review Queue issue Quick Actions, any direct Review Queue fix Quick Actions, Focus Result labels, queue metrics, audition cues, and next-check feedback only from existing Review Queue issue ids, areas, existing queue items, the visible summary, the explicitly clicked or command-run item, current focused item, and current top item; show Focus Result only after explicit visible or command focus; keep focused item state, Review Fix Preview state, Focus Result feedback, and Review Fix result feedback UI-local and out of saved project schema and undo history; route focus clicks and command runs only to existing Compose, Arrange, Mix, Master, or Deliver panels through the existing focus handler; route fix clicks and command runs through exactly one existing undoable handler per issue limited to Beat Blueprint, Layer Starter, Pattern Chain, Chain Expand, Arrangement Template, Arrangement Move, Delivery Target Alignment, Mix Fix, Master Finish, or Session Brief Starter; preserve issue priority, queue order, visible focus buttons, scoring, recommendations, project musical data, arrangement, mixer, master, exports, playback, save/load, undo/redo, and Handoff state; and avoid automatic fixing loops, autoplay, macros, command chains, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Review Queue result metric work must derive the explicit production review action, current top or direct queue issue, destination panel, issue status/context, selected Pattern, editable event count, Pattern A/B/C usage, queue readiness summary, fix availability, arrangement block count, and song length only from current Review Queue command title/detail plus local project state after the existing Review Queue focus path; preserve Review Queue issue derivation, issue order, scoring, focus-target selection, direct issue command definitions, direct fix command definitions, visible Priority action behavior, focus routing, fix routing, Focus Result behavior, Review Fix Preview behavior, Beat Passport, Beat Readiness, Listening Pass, Production Snapshot, Finish Checklist, Export Preflight, Mix Coach, Delivery Target, Session Brief, Pattern A/B/C event data, arrangement data, Quick Actions filtering, command search, Pinned Commands, Recent Commands, project data, playback, save/load, WAV/stem/MIDI export, Handoff Sheet, Handoff Pack, and local draft behavior; avoid onboarding overlays, tutorials, macros, command chains, hidden generation, auto-run, autoplay, automatic fixing loops, automatic arrangement writing, auto-save, auto-export, sampling, imported audio, audio analysis, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Quick Actions Review Fix result metric work must derive the explicit priority or direct fix action, Project/Review Queue destination, selected issue/fix, fix status/context, applied scope/impact, selected Delivery Target, Review Queue readiness posture, Beat Readiness posture, selected Pattern, editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, stem readiness, audition cue, and next review check only from current Review Fix command title/detail plus local project, Review Queue, Beat Readiness, export, and stem state after the existing Review Fix path; preserve Review Queue issue derivation, issue order, scoring, fix option selection, direct fix command definitions, visible Priority action behavior, focus routing, fix routing, Focus Result behavior, Review Fix Preview behavior, Review Fix result behavior, Beat Passport, Beat Readiness, Listening Pass, Production Snapshot, Finish Checklist, Export Preflight, Mix Coach, Delivery Target, Session Brief, Pattern A/B/C event data, arrangement data, Quick Actions filtering, command search, Pinned Commands, Recent Commands, project data outside explicit fix effects, playback, save/load, WAV/stem/MIDI export, Handoff Sheet, Handoff Pack, and local draft behavior; avoid onboarding overlays, tutorials, macros, command chains, hidden generation, auto-run, autoplay, automatic fixing loops, automatic arrangement writing, auto-save, auto-export, sampling, imported audio, audio analysis, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Review Queue Command Reference work must mark the Guide row as `Quick Actions / Readout`, reflect only the existing local prioritized production issues, Priority Readout, Focus controls, Review Queue focus command, direct issue commands, Review Fix commands, and local Focus/Fix Result feedback; preserve issue derivation, priority order, scoring, recommendations, focus routing, fix routing, Quick Actions execution, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid tutorials, macros, command chains, hidden generation, automatic fixing loops, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Beat Map work must derive workflow stages, song/pattern metrics, Session Brief status, export state, stem state, action suggestions, and any Quick Actions Beat Map action commands only from local project state, selected Delivery Target, Beat Readiness, deterministic export analysis, and deterministic stem analysis; remain read-only by default; route all mutating action buttons and Quick Actions Beat Map action commands through the existing explicit `runNextMove` path and undoable handlers; preserve Beat Readiness, Next Move, Mix Coach, realtime playback, WAV/stem/MIDI export, save/load, and undo/redo semantics; and avoid sampling, imported audio, plugin hosting, remote AI, remote analysis, hidden automation, accounts, analytics, cloud sync, or professional mastering/commercial release claims.

Beat Map Command Reference work must mark the Guide row as `Quick Actions / Readout`, reflect only the existing local workflow stages, song/pattern metrics, export and stem metrics, producer-facing overview, Beat Map action commands, Next Move routing, and local result feedback; preserve Beat Map derivation, stage scoring, metric labels, action suggestions, Next Move routing, Quick Actions execution, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid tutorials, macros, command chains, hidden generation, automatic fixes, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Quick Actions Beat Map result metric work must identify only the explicit Beat Map action, Guide destination, target workflow stage, stage status/context, selected Delivery Target, current completion posture, selected Pattern, editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, stem readiness, package readiness, action route, audition cue, and next Beat Map check from current Beat Map action command title/detail plus local project, target, Beat Map, export, stem, and package state; preserve Beat Map derivation, stage scoring, action suggestions, Next Move routing, action handlers, project data, playback, render/export, project schema, privacy boundaries, and sampling boundaries.

Structure Lens work must derive target fit, section coverage, hook contrast, energy arc, and any Quick Actions Structure Lens action commands only from local arrangement blocks, Pattern A/B/C usage, and the selected Delivery Target; remain read-only until explicit user clicks; route action buttons and Quick Actions Structure Lens action commands through the existing explicit `runNextMove` path and undoable arrangement, Pattern Chain, target alignment, or Hook Lift paths; preserve Beat Readiness, Beat Map, Next Move, realtime playback, WAV/stem/MIDI export, save/load, and undo/redo semantics; and avoid sampling, imported audio, remote AI, remote analysis, hidden auto-arrangement, accounts, analytics, cloud sync, publishing, licensing, or platform compliance claims.

Structure Lens Command Reference work must mark the Guide row as `Quick Actions / Readout`, reflect only the existing local target-fit, section-coverage, hook-contrast, energy-arc, arrangement-action, Next Move routing, and local result feedback; preserve Structure Lens derivation, signal scoring, action suggestions, Next Move routing, Quick Actions execution, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid tutorials, macros, command chains, hidden generation, automatic arrangement fixes, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Quick Actions Structure Lens result metric work must identify only the explicit Structure Lens action, Guide/Arrange destination, target structure signal, signal status/context, selected Delivery Target, target-fit posture, section coverage, hook contrast, energy arc, selected Pattern, editable event count, Pattern A/B/C usage, arrangement block count, song length, action route, audition cue, and next Structure Lens check from current Structure Lens action command title/detail plus local project, target, Structure Lens, and arrangement state; preserve Structure Lens derivation, signal scoring, action suggestions, Next Move routing, action handlers, arrangement data, Pattern data, project data, playback, render/export, project schema, privacy boundaries, and sampling boundaries.

Song Form Overview work must derive section flow, Pattern A/B/C usage, selected block, energy range, bar spans, muted-track posture, segment tones, Priority Readout labels, visible priority action targets, and any Quick Actions Song Form Priority command target only from local arrangement blocks, Pattern A/B/C event data, selected Delivery Target, existing arrangement helpers, the visible Song Form metrics, and the current highest-priority song-form metric; keep Priority Readout state, visible priority action state, and command state UI-local and out of saved project schema and undo history; route segment clicks, visible Song Form Priority action clicks, and Quick Actions Song Form Priority command runs only through existing selected-block navigation without creating undo history or mutating arrangement, Pattern A/B/C, mixer, sound, master, export, snapshot, Beat Map, Structure Lens, Next Move, or Handoff state; preserve realtime playback plus WAV/stem/MIDI export semantics; and avoid sampling, imported audio, remote AI, remote analysis, hidden auto-arrangement, accounts, analytics, cloud sync, publishing, licensing, or platform compliance claims.

Quick Actions Song Form Priority result metric work must derive the explicit priority focus action, Arrange destination, priority metric, target block, section, Pattern A/B/C assignment, bar range, bar length, energy, mute posture, block event count, selected Pattern, editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, mute-map posture, transition-map posture, audition cue, and next song-form check only from current Song Form Priority command id/title/detail plus local arrangement, Pattern, song-form, mute-map, transition-map, selected-block, and export state after the existing selected-block navigation path; preserve Song Form Overview derivation, metric ordering, priority selection, selected-block navigation, Arrangement Mute Map derivation, Arrangement Transition Map derivation, Pattern A/B/C musical events, manual arrangement controls, playback, save/load, WAV/stem/MIDI export, Handoff, command search, scope filters, Pinned Commands, and Recent Commands; and avoid auto-arrangement, command chains, macros, autoplay, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Song Form Overview Command Reference work must mark the Arrange row as `Quick Actions / Readout`, reflect only the existing Song Form Overview readout and Song Form Priority command, preserve Song Form Overview derivation and selected-block navigation, and avoid changing arrangement data, project data, command execution, playback, render/export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Arrangement Mute Map work must derive lanes, section cells, Priority Readout labels, visible priority action targets, Focus Result labels, mute-map metrics, audition cues, and next-check feedback only from existing arrangement `mutedTracks`, bars, sections, Pattern A/B/C assignments, existing arrangement mute labels, realtime playback snapshots, the visible Arrangement Mute Map summary, the explicitly clicked or command-run lane, and the current highest-priority mute lane; show Focus Result only after explicit visible or command focus; keep priority state, visible priority action state, focus state, and Focus Result feedback UI-local and out of saved project schema and undo history; route visible Priority action clicks, visible Focus buttons, and Quick Actions Arrangement Mute Map focus/lane commands only to the Arrange panel through the existing lane-focus path; preserve selected-block mute editing, arrangement playback, save/load, WAV/stem/MIDI export, Song Form Overview, Arrangement Transition Map, Arrangement Focus, Structure Lens, Beat Map, and project schema; and avoid auto-muting, auto-arrangement, hidden generation, sampling, imported audio, sampler devices, remote AI, accounts, analytics, or cloud sync.

Arrangement Transition Map work must derive handoff cards, Priority Readout labels, visible priority action targets, Focus Result labels, transition metrics, audition cues, and next-check feedback only from existing arrangement blocks, `mutedTracks`, bars, sections, Pattern A/B/C assignments, Pattern event counts, energy values, existing arrangement labels, realtime playback snapshots, the visible Arrangement Transition Map summary, the explicitly clicked or command-run transition, and the current highest-priority transition; show Focus Result only after explicit visible or command focus; keep priority state, visible priority action state, focus, Focus Result feedback, and cue state UI-local and out of saved project schema and undo history; route visible Priority action clicks, visible Focus buttons, and Quick Actions Arrangement Transition Map focus/transition commands only to the Arrange panel through the existing transition-focus path; route visible Cue buttons and Quick Actions Transition Loop cue commands only through current selected-block, focused-transition, and transport loop-scope state using existing bounded arrangement playback; preserve selected-block editing, arrangement playback scheduling, save/load, WAV/stem/MIDI export, Song Form Overview, Arrangement Mute Map, Arrangement Focus, Structure Lens, Beat Map, and project schema; and avoid auto-fills, auto-muting, auto-arrangement, autoplay, hidden generation, sampling, imported audio, sampler devices, remote AI, accounts, analytics, or cloud sync.

Quick Actions Arrangement Transition Map and Transition Loop result metric work must derive the explicit transition focus or cue action, priority or direct transition, Arrange/Transport destination, section handoff, from/to Pattern A/B/C, bar range, energy change, muted-layer change, event-density change, selected Pattern, editable event count, Pattern A/B/C usage, arrangement block count, song length, current loop scope, selected block, export readiness, audition cue, and next transition check only from current transition command title/detail plus local arrangement, Pattern, transition map, loop target, and export state after the existing focus or cue path; preserve Arrangement Transition Map derivation, transition ordering, priority selection, focus routing, cue target selection, loop-scope routing, selected-block editing, arrangement data, Pattern A/B/C data, playback scheduling, save/load, WAV/stem/MIDI export, Song Form Overview, Arrangement Mute Map, Arrangement Focus, Structure Lens, Beat Map, project schema, privacy boundaries, and sampling boundaries; avoid tutorials, macros, command chains, hidden generation, auto-run, autoplay, auto-fills, auto-muting, auto-arrangement, sampling, imported audio, audio analysis, sampler devices, audio clips, remote AI, accounts, analytics, or cloud sync.

Arrangement Maps Command Reference work must mark the Arrangement Mute Map and Arrangement Transition Map Arrange rows as `Quick Actions / Readout`, reflect only the existing map readouts and focus commands, preserve map derivation, focus handling, Transition Loop cue behavior, and arrangement data, and avoid changing project data, command execution, playback, render/export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Arrangement Block Jump Quick Actions work must expose only current arrangement block navigation commands, route each command through the existing selected-block navigation handler, set only selected arrangement block plus selected Pattern preview state, reset selected note/drum/chord focus the same way arrangement block clicks do, avoid autoplay and undo history, preserve arrangement blocks, Pattern A/B/C event data, playback start/stop, save/load, mixer/master, WAV/stem/MIDI export semantics, snapshots, Beat Map, Structure Lens, Song Form Overview, Next Move, and Handoff state, and avoid marker persistence, hidden generation, command chains, auto-arrangement, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Arrangement Block Cue Quick Actions work must expose only current arrangement block audition-prep commands, route each command through existing selected-block navigation plus Block loop-scope selection, set only selected arrangement block, selected Pattern preview state, and Block loop scope while playback is stopped, reset selected note/drum/chord focus the same way arrangement block clicks do, avoid autoplay and undo history, preserve arrangement blocks, Pattern A/B/C event data, playback start/stop, save/load, mixer/master, WAV/stem/MIDI export semantics, snapshots, Beat Map, Structure Lens, Song Form Overview, Next Move, and Handoff state, and avoid marker persistence, hidden generation, command chains, auto-arrangement, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Arrangement Block Jump/Cue result metric work must derive the explicit jump or cue action, target block scope, section, Pattern A/B/C assignment, bar range, bar length, event count, song block count, and total bar count only from explicit `arrangement-block-jump-*` or `arrangement-block-cue-*` command ids plus local arrangement state after the existing arrangement block navigation or cue handlers; preserve Arrangement Block Jump and Cue routing, selected Pattern alignment, Section Cue Result feedback, Block loop scope, Pattern A/B/C musical events, manual arrangement controls, playback, save/load, WAV/stem/MIDI export, Handoff, command search, scope filters, Pinned Commands, and Recent Commands; and avoid auto-arrangement, command chains, macros, autoplay, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Section Locator Pad work must derive Intro, Verse, Hook, Bridge, Outro cue pads, Cue Decision labels, Priority Readout labels, visible Cue Decision action targets, any Quick Actions Section Locator Cue Decision command target, visible priority action targets, and Section Cue Result labels only from local arrangement blocks, Pattern A/B/C event counts, selected arrangement block state, transport loop state, visible Section Locator pads, selected section state, playing section state, the current Hook pad, and the first available section; keep Cue Decision state, Priority Readout state, visible priority action state, and Section Cue Result state UI-local and out of saved project schema and undo history; route visible pad clicks, visible Section Locator Cue Decision action clicks, Quick Actions Section Locator Cue Decision command runs, visible Section Locator Priority action clicks, and Arrangement Block Cue command runs only through existing selected-block navigation and UI-local Block loop cueing while playback is stopped; avoid creating undo history, changing arrangement blocks, mutating Pattern A/B/C musical events, altering playback scheduling, exports, snapshots, Beat Map, Structure Lens, Song Form Overview, Next Move, Handoff state, or project schema; and avoid marker persistence, count-in, sampling, imported audio, remote AI, remote analysis, accounts, analytics, or cloud sync.

Section Locator Quick Actions work must expose only Intro/Verse/Hook/Bridge/Outro audition-prep commands, route each command through the existing Section Locator cue handler, set only selected-block navigation plus Block loop scope while playback is stopped, show Section Cue Result feedback only after explicit cue actions, avoid autoplay and undo history, preserve arrangement blocks, Pattern A/B/C event data, playback start/stop, save/load, mixer/master, WAV/stem/MIDI export semantics, snapshots, Beat Map, Structure Lens, Song Form Overview, Next Move, and Handoff state, and avoid marker persistence, count-in, hidden generation, command chains, auto-arrangement, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Section Locator result metric work must derive the explicit section cue action, target section, cued block scope, Pattern A/B/C assignment, bar range, event count, song block count, and total bar count only from explicit `section-locator-*` command id/title/detail/keywords plus local arrangement state after the existing Section Locator cue handler; preserve Section Locator Cue Decision and Priority derivation, Section Cue Result feedback, selected-block navigation, Block loop scope, Pattern A/B/C musical events, manual arrangement controls, playback, save/load, WAV/stem/MIDI export, Handoff, command search, scope filters, Pinned Commands, and Recent Commands; and avoid auto-arrangement, command chains, macros, autoplay, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Section Locator Command Reference work must mark Section Locator as `Quick Actions / Readout` only when it reflects existing Section Locator Cue Decision Readout, Priority Readout, direct Section Locator cue commands, Arrangement Block Cue feedback, and Section Cue Result feedback; it must not change Section Locator cue derivation, cue routing, Quick Actions execution, playback scheduling, render/export, project schema, save/load, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Arrangement Playhead Highlighting work must derive the currently playing arrangement block only from local realtime playback snapshots; render it as UI-only state across Arrangement Track, Section Locator Pads, and Song Form Overview; keep selected arrangement block navigation independent; avoid auto-selecting, auto-scrolling, saving, undo history, or mutating arrangement data; preserve playback scheduling, loop selection, metronome, save/load, WAV/stem/MIDI export semantics; and avoid sampling, imported audio, remote AI, remote analysis, accounts, analytics, or cloud sync.

Next Move work must derive deterministic recommendations, post-click result metrics, audition cues, and next checks from local project, Beat Readiness, export analysis, selected Delivery Target, arrangement blocks, snapshots, and explicit action definitions; keep Beat Readiness cards read-only; run only after explicit user button clicks; keep result strips UI-only and informational rather than modal confirmations, autoplay, auto-save, or export triggers; route mutating actions through undoable project update paths; and avoid imported audio, sampling, plugin hosting, remote AI, remote analysis, hidden automation, accounts, or analytics.

Next Move result metric work must identify only the explicit recommended action, route, action-specific before/after posture, selected Delivery Target, Beat Readiness posture, selected Pattern, editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, stem readiness, audition cue, and next check from local project, Beat Readiness, export analysis, deterministic stem analysis, selected Delivery Target, arrangement blocks, snapshots, and explicit action definitions; preserve recommendation derivation, action ordering, action definitions, action handlers, Beat Map, Structure Lens, Workflow Navigator, Workflow Spotlight, project schema, playback, render/export, privacy boundaries, and sampling boundaries.

Next Move Command Reference work must mark the Guide row as `Quick Actions / Readout`, reflect only the existing local readiness/export-driven action recommendations, explicit action buttons, result metric, audition cue, and next-check feedback; preserve Next Move recommendation derivation, action ordering, action definitions, result metric derivation, audition cues, next-check labels, Quick Actions execution, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid tutorials, macros, command chains, hidden generation, automatic fixes, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Next Move Pattern Chain work must recommend Pattern Chain only from local readiness state, route the click through the existing undoable `applyPatternChain` path, keep Full Beat available as an explicit alternative when arrangement structure is weak, avoid mutating Beat Readiness, and preserve realtime playback plus WAV/stem/MIDI export semantics.

Finish Move Action work must route Master Finish recommendations and Quick Actions through existing `applyMasterFinishPad`, keep actions explicit, local, undoable, and editable, update only master state and master mixer volume, preserve project/render semantics, and avoid hidden automatic mastering, LUFS, true-peak, platform claims, sampling, imported audio, or remote AI.

Selected note edit tool work must keep 808 and Synth note move, transpose, step reset, beat duplicate, previous beat duplicate, pitch reset, octave, length, length reset, 808 glide reset, 808 glide, velocity, chance, copy, paste, duplicate, delete, audition, and any Quick Actions selected-note step-reset/duplicate-beat/duplicate-previous-beat/pitch-reset/length-reset/length/glide-reset/glide/velocity/chance/delete/edit/audition commands scoped to the selected Pattern A/B/C slot, derive selected-note degree/role readouts only from local key and pitch, route Quick Actions selected-note edit commands only through existing selected-note move, step reset, beat duplicate, previous beat duplicate, pitch, pitch reset, octave, length, 808 glide, velocity, chance, copy, paste, duplicate, and delete handlers, with step reset reusing the existing selected-note move path, beat duplicate and previous beat duplicate reusing the existing undoable selected-pattern note insertion path, length reset reusing the existing selected-note length update path, and 808 glide reset reusing the existing selected-note glide update path, route selected-note audition only through UI-local one-shot built-in 808/Synth synthesis, keep note clipboard, audition, and readout state UI-local and out of saved project schema, keep selected-note-copy UI-local until an explicit paste or duplicate writes project data, paste copied notes only into the copied track's next empty step, preserve length/glide/velocity/chance fields unless length, glide, velocity, or chance is directly edited, avoid overlapping duplicate, beat-duplicate, previous-beat-duplicate, pasted, moved, or step-reset notes at the same step and pitch, keep pitch moves scale-aware, keep paste/duplicate/beat duplicate/previous beat duplicate/move/delete/step reset/pitch reset/length reset/length/glide reset/glide/velocity/chance edits undoable, and avoid sampling, remote AI, or hidden audio assets.

Selected note step reset work must keep the selected-note step reset command scoped to the selected Pattern A/B/C slot; derive the target only from the selected note step, selected note length, selected note track, same-track notes, and existing 16-step grid; snap only to the nearest 4-step anchor that can preserve the selected note length inside the grid; disable reset when no active selected note exists, the selected note already starts on the target anchor, or another same-track note already occupies the target step and pitch; route reset only through the existing selected-note move path; set only the note step; preserve pitch, length, glide, velocity, chance, copy/paste/duplicate/delete, playback, WAV/stem/MIDI export, and manual note controls; keep the note selected and undoable; and avoid melody rewriting, bassline rewriting, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Selected note beat duplicate work must keep the selected-note beat duplicate command scoped to the selected Pattern A/B/C slot; derive the target only from the selected note step, selected note length, selected note track, selected note pitch, same-track notes, and existing 16-step grid; duplicate only to the next later 4-step anchor that can preserve the selected note length inside the grid and has no same-track note at the target step and pitch; enforce that 4-step anchor rule inside the handler as well as the Quick Actions target derivation; disable beat duplicate when no active selected note exists or no target anchor is available; route the duplicate only through the existing undoable selected-pattern note insertion path; preserve pitch, length, glide, velocity, chance, note clipboard, playback, WAV/stem/MIDI export, and manual note controls; select the duplicated note after a successful edit; and avoid melody rewriting, bassline rewriting, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Visible selected-note next beat duplicate button work must keep the button scoped to the selected Pattern A/B/C slot, derive the same next later length-preserving 4-step target as the selected-note beat duplicate command, disable when no active selected note or no valid target exists, and route only through the existing undoable selected-pattern note insertion path without changing note clipboard, playback, export, sampling scope, or project schema.

Selected note previous beat duplicate work must keep the selected-note previous beat duplicate command and visible selected-note previous-beat button scoped to the selected Pattern A/B/C slot; derive the target only from the selected note step, selected note length, selected note track, selected note pitch, same-track notes, and existing 16-step grid; duplicate only to the nearest earlier 4-step anchor that can preserve the selected note length inside the grid and has no same-track note at the target step and pitch; enforce that 4-step anchor rule inside the handler as well as the Quick Actions and visible-button target derivation; disable previous beat duplicate when no active selected note exists or no earlier target anchor is available; route the duplicate only through the existing undoable selected-pattern note insertion path; preserve pitch, length, glide, velocity, chance, note clipboard, playback, WAV/stem/MIDI export, and manual note controls; select the duplicated note after a successful edit; and avoid melody rewriting, bassline rewriting, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Selected note pitch reset work must keep the selected-note pitch reset command scoped to the selected Pattern A/B/C slot; derive the target only from the current project key, selected note track, and that track's Keyboard Capture octave default; route reset only through the existing selected-note pitch update path; disable reset when no active selected note exists, the selected note already matches the default pitch, or another note already occupies the target step/pitch; set only the note pitch; preserve step, length, glide, velocity, chance, copy/paste/duplicate/delete, playback, WAV/stem/MIDI export, and manual note controls; keep the note selected and undoable; and avoid melody rewriting, bassline rewriting, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Selected note length reset work must keep the selected-note length reset command scoped to the selected Pattern A/B/C slot; derive the target only from the selected note track's current Keyboard Capture length default; route reset only through the existing selected-note length update path; disable reset when no active selected note exists or the selected note already matches the default length; set only the note length; preserve step, pitch, glide, velocity, chance, copy/paste/duplicate/delete, playback, WAV/stem/MIDI export, and manual note controls; keep the note selected and undoable; and avoid melody rewriting, bassline rewriting, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Selected note glide reset work must keep the selected-note glide reset command scoped to the selected Pattern A/B/C slot and limited to selected 808 notes; derive the target only from the current Keyboard Capture 808 glide default; route reset only through the existing selected-note glide update path; disable reset when no active selected 808 note exists or the selected 808 note already matches the default glide state; set only the note glide flag; preserve step, pitch, length, velocity, chance, copy/paste/duplicate/delete, playback, WAV/stem/MIDI export, and manual note controls; keep the note selected and undoable; and avoid whole-bassline glide rewrites, melody rewriting, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Selected chord harmonic Quick Actions work must keep selected-chord root down, selected-chord root up, selected-chord root reset, and selected-chord quality cycle scoped to the selected Pattern A/B/C slot; derive root movement and root reset only from the current key's `scalePitchNames` and derive quality changes only from the existing `chordQualities` palette; route every command only through existing undoable selected-chord root/quality update handlers that use `updateChordEvent`; keep selected-chord harmonic readouts, Chord Pads, Chord Rhythm Pads, Chord Voicing Pads, chord copy/paste/duplicate/delete, playback, WAV/stem/MIDI export, and manual chord controls intact; and avoid automatic reharmonization, progression replacement, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Selected chord inversion reset work must keep the selected-chord inversion reset command scoped to the selected Pattern A/B/C slot; route reset only through the existing selected chord inversion reset handler and `updateChordEvent`; disable reset when no active selected chord exists or the selected chord is already root position; set only `inversion: 0`; preserve chord root, quality, step, length, velocity, chance, copy/paste/duplicate/delete, playback, WAV/stem/MIDI export, and manual chord controls; keep the chord selected and undoable; and avoid reharmonization, progression replacement, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Selected event reset work must route selected-note chance reset, selected-drum chance reset, selected-drum timing reset, selected-hat repeat reset, and selected-chord chance reset only through existing selected-event update handlers; keep reset commands scoped to the selected Pattern A/B/C slot; disable reset commands when the selected event is missing or already at the reset value; use explicit reset values of music chance 100%, drum chance 100%, drum timing On grid, and hat repeat 1x; keep reset edits undoable and manually editable afterward; and avoid whole-pattern quantize, bulk reset, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Selected velocity reset work must route selected-note velocity reset, selected-drum velocity reset, and selected-chord velocity reset only through existing selected-event velocity update handlers; keep reset commands scoped to the selected Pattern A/B/C slot; disable reset commands when the selected event is missing or already at the reset value; use the current Keyboard Capture default for 808/Synth note velocity, lane/step `defaultDrumVelocity` for drum velocity, and 50% for chord velocity; keep reset edits undoable and manually editable afterward; and avoid whole-pattern normalization, automatic mixing, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Selected Event Tools Command Reference work must mark the Create row as `Quick Actions / Readout`, reflect only the existing selected-drum pocket readout, selected-note degree/role readout, selected-chord harmonic readout, direct selected-drum/note/chord edit commands, selected-event reset commands, velocity reset commands, audition commands, and local selected-event delete feedback; preserve readout derivation, command availability, edit handlers, reset handlers, velocity reset handlers, audition routing, delete feedback derivation, Pattern A/B/C event data outside explicit selected-event actions, arrangement, mixer, master, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid new edit algorithms, tutorials, macros, command chains, hidden generation, automatic arrangement, automatic export, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Selected drum step move work must keep selected-drum step-left and selected-drum step-right commands scoped to the selected Pattern A/B/C slot; route moves only through the existing selected drum move handler and `updateCurrentPattern`; block moves when no active selected drum hit exists, the target step is outside the 16-step grid, or the same lane already has an active hit at the target step; preserve selected hit velocity, chance, microtiming, and hat repeat values at the target step while resetting source step chance, timing, and hat repeat to safe defaults; keep the moved hit selected and undoable; and avoid whole-pattern shifts, quantize passes, lane remapping, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Selected drum step reset work must keep selected-drum step-reset commands scoped to the selected Pattern A/B/C slot; route resets only through the existing selected drum step reset handler and `updateCurrentPattern`; block resets when no active selected drum hit exists, the selected drum hit already sits on the nearest 4-step anchor, or the same lane already has an active hit at the target anchor; preserve selected hit velocity, chance, microtiming, and hat repeat values at the target step while resetting source step chance, timing, and hat repeat to safe defaults; keep the reset hit selected and undoable; and avoid whole-pattern quantize, lane remapping, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Selected drum duplicate work must keep the selected-drum duplicate command scoped to the selected Pattern A/B/C slot; route duplication only through the existing selected drum duplicate handler and `updateCurrentPattern`; block duplication when no active selected drum hit exists or the selected lane has no empty target step; copy the selected hit into the same lane's next empty step using the existing step search order; preserve velocity, chance, microtiming, and hat repeat values on the duplicate; keep the duplicate hit selected; leave the UI-local drum clipboard unchanged; keep the edit undoable; and avoid whole-lane cloning, random fills, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Arrangement editor work must let users change a block's section, Pattern A/B/C assignment, bar length, and energy without losing existing pattern, mixer, master, or save/load data. Selecting or reassigning a block should keep the pattern editor aligned with that block's assigned pattern. Selected-block role readouts must derive only from local arrangement block, timeline, pattern-event, energy, and muted-track fields, stay UI-local and out of saved project schema, avoid changing Song Form Overview or Structure Lens semantics, and preserve playback, WAV/stem export, and MIDI export behavior.

Arrangement track mute work must migrate older arrangement blocks to no muted tracks, keep mutes scoped to the selected block without mutating Pattern A/B/C events, apply the same mute decisions to realtime arrangement playback, full-mix WAV export, and stem export, and preserve selected-pattern preview behavior.

Arrangement structure and visible/Quick Actions selected-block work must preserve at least one block, keep selection/pattern alignment after copy, paste, duplicate, move, split, merge, and delete actions, derive Selected Block Edit Preview Decision and Priority Readout labels plus the Quick Actions Selected Block Edit Decision command target only from local arrangement blocks, selected block state, clipboard state, block count, bar count, and existing selected-block edit affordances, derive Selected Block Edit Result labels only from local before/after arrangement blocks, selected block state, block count, bar count, and existing selected-block edit handlers, keep arrangement block clipboard state, Selected Block Edit Preview Decision state, Selected Block Edit Priority Readout state, visible Selected Block Priority action state, and Selected Block Edit Result state UI-local and out of saved project schema and undo history, disable or no-op the visible Selected Block Edit Preview Decision, Quick Actions Selected Block Edit Decision, and Selected Block Priority actions when no local recommendation is available, route visible Preview Decision, Quick Actions Selected Block Edit Decision, visible and Quick Actions selected-block priority, plus direct selected-block edit commands only through existing arrangement copy, paste, duplicate, move, split, merge, and delete handlers after explicit user clicks or command clicks, keep selected-block-copy UI-local until a separate explicit paste writes project data, paste copied blocks after the selected block, preserve section, Pattern A/B/C assignment, bar length, energy, and muted-track fields unless directly edited, avoid mutating Pattern A/B/C musical event data, keep paste/duplicate/move/split/merge/delete actions undoable, and make realtime playback plus WAV/stem/MIDI export follow the current arrangement length.

Quick Actions Selected Block result metric work must derive the explicit selected-block edit action, selected block scope, section, Pattern A/B/C assignment, bar length, song block count, total bar count, and structural delta only from explicit `selected-block-*` command id/title/detail/keywords plus local arrangement state after the existing selected-block edit handlers; preserve Selected Block Edit Preview Decision and Priority derivation, arrangement block clipboard routing, copy/paste/duplicate/move/split/merge/delete handlers, Pattern A/B/C musical events, manual arrangement controls, playback, save/load, WAV/stem/MIDI export, Handoff, command search, scope filters, Pinned Commands, and Recent Commands; and avoid auto-arrangement, command chains, macros, autoplay, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Arrangement split work must split only the selected arrangement block, preserve total arrangement bars, duplicate the selected block's section, Pattern A/B/C assignment, energy, and muted tracks into the resulting blocks, select the new second block, keep the action undoable, avoid mutating Pattern A/B/C event data, and make realtime playback, WAV/stem export, and MIDI export follow the split structure.

Arrangement merge work must merge only the selected block with the next block, preserve total arrangement bars, keep the selected block's section, Pattern A/B/C assignment, energy, and muted tracks as the merged block identity, keep the result within max per-block bar bounds, select the merged block, keep the action undoable, avoid mutating Pattern A/B/C event data, and make realtime playback, WAV/stem export, and MIDI export follow the merged structure.

Arrangement template work must replace only arrangement blocks, keep existing Pattern A/B/C musical data and mixer/master state intact, reset selection safely to a valid block, align the selected pattern with the first template block, remain undoable, and make WAV/stem export follow the applied template length.

Arrangement Template Preview work must derive suggested template labels, direct template command labels, Priority Readout labels, Preview Decision labels, visible priority and Preview Decision action targets, section/bar posture, Pattern A/B/C spread, energy posture, muted-track posture, pre-click arrangement move counts, any Quick Actions Arrangement Template Decision command target, any Quick Actions Arrangement Template command target, and any direct Arrangement Template Quick Actions only from current local arrangement state and existing Arrangement Template definitions; direct template commands, visible Arrangement Template Priority action clicks, visible Arrangement Template Preview Decision action clicks, and Quick Actions Arrangement Template Decision command runs must route through the existing Arrangement Template apply path and disable templates that already match the arrangement; keep preview, decision, and Priority Readout state UI-local and out of saved project schema and undo history; preserve Arrangement Template definitions and apply behavior, manual arrangement controls, Structure Lens, Song Form Overview, Section Locator Pads, Arrangement Arc Preview and Result, Arrangement Focus Preview and Result, Pattern Chain Preview and Result, musical events, mixer, sound design, master state, Delivery Target, Beat Readiness, Beat Map, Next Move, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid auto-arrangement, auto-apply, modal confirmations, autoplay, render downloads, auto-export, sampling, imported audio, audio clips, hidden generation, remote AI, remote analysis, plugin hosting, accounts, analytics, or cloud sync.

Arrangement Template Result work must update result labels only after explicit Arrangement Template clicks, visible Arrangement Template Priority action clicks, visible Arrangement Template Preview Decision action clicks, Quick Actions Arrangement Template Decision command runs, or Quick Actions Arrangement Template command runs, derive applied template, full-arrangement scope, before/after section flow, Pattern A/B/C spread, bar length, energy posture, muted-track posture, changed block/field impact, audition cue, and next-check text only from local before/after arrangement state and existing Arrangement Template definitions; keep result state UI-local and out of saved project schema and undo history; preserve Arrangement Template Preview, Preview Decision Readout, Arrangement Template definitions and apply behavior, manual arrangement controls, Structure Lens, Song Form Overview, Section Locator Pads, Arrangement Arc Preview and Result, Arrangement Focus Preview and Result, Pattern Chain Preview and Result, musical events, mixer, sound design, master state, Delivery Target, Beat Readiness, Beat Map, Next Move, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid auto-arrangement, auto-apply, modal confirmations, autoplay, render downloads, auto-export, sampling, imported audio, audio clips, hidden generation, remote AI, remote analysis, plugin hosting, accounts, analytics, or cloud sync.

Quick Actions Arrangement Template result metric work must derive the applied song-form template, section flow, Pattern A/B/C spread, block count, hook block count, and bar count only from explicit `arrangement-template`, `arrangement-template-decision`, or `arrangement-template-direct-*` command id/title/detail/keywords plus local arrangement state after the existing `applyArrangementTemplate` handler; preserve Arrangement Template definitions, preview/priority/decision derivation, apply routing, Pattern A/B/C musical events, manual arrangement controls, playback, save/load, WAV/stem/MIDI export, Handoff, command search, scope filters, Pinned Commands, and Recent Commands; and avoid auto-arrangement, command chains, macros, autoplay, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Arrangement Template Command Reference work must mark Arrangement Template as `Quick Actions / Readout` only when it reflects existing Arrangement Template Preview, Preview Decision Readout, Priority Readout, current/direct Arrangement Template commands, and Arrangement Template Result feedback; it must not change Arrangement Template definitions, apply routing, Quick Actions execution, playback, render/export, project schema, save/load, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Pattern Chain work must replace only arrangement blocks with deterministic Pattern A/B/C sequences, keep existing Pattern A/B/C musical data and mixer/sound/master state intact, reset selection safely to the first chain block, align the selected pattern with that block, remain undoable, make realtime playback plus WAV/stem/MIDI export follow the chain, and avoid sampling, imported audio, plugin hosting, remote AI, hidden randomness, macros, accounts, analytics, or cloud sync.

Quick Actions Pattern Chain work must expose every `patternChainIds` preset as an explicit command-palette action routed only through the existing `applyPatternChain` handler, expose any Quick Actions Pattern Chain Decision command target only through the existing Pattern Chain apply or Chain Expand handlers, keep the existing Chain Expand command routed only through `expandPatternChain`, preserve Pattern Chain Preview/Result, command search, scope filters, Spotlight, Pinned Commands, Recent Commands, undo/redo, selected block/pattern alignment, save/load, playback, WAV/stem/MIDI export, and Handoff semantics, and avoid new presets, macros, command chains, autoplay, auto-arrangement, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Pattern Chain Command Reference work must mark Pattern Chain as `Quick Actions / Readout` only when it reflects existing Pattern Chain Preview, Preview Decision Readout, Priority Readout, direct Pattern Chain commands, Chain Expand, and Pattern Chain Result feedback; it must not change Pattern Chain definitions, apply routing, Quick Actions execution, playback, render/export, project schema, save/load, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Chain Expand Command Reference work must mark Chain Expand as `Quick Actions / Readout` only when it reflects existing Pattern Chain Preview, Preview Decision expand target, Priority Readout context, Quick Actions Chain Expand command, and Pattern Chain Result feedback; it must not change Chain Expand transforms, apply routing, Quick Actions execution, playback, render/export, project schema, save/load, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Pattern Chain step editor work must cycle only arrangement block Pattern A/B/C assignments through existing undoable project update paths, keep each block's section, bars, energy, muted tracks, and Pattern A/B/C musical event data intact, align the selected block and selected pattern after edits, make realtime playback plus WAV/stem/MIDI export follow the edited chain, and avoid sampling, imported audio, plugin hosting, remote AI, hidden randomness, macros, accounts, analytics, or cloud sync.

Pattern Chain expand work must transform only arrangement blocks into a longer deterministic song-form outline, derive any Quick Actions Chain Expand command only from the existing Chain Expand handler, preserve Pattern A/B/C musical event data plus mixer/sound/master state, align selected pattern to the first expanded block, remain undoable, make realtime playback plus WAV/stem/MIDI export follow the expanded arrangement, and avoid sampling, imported audio, plugin hosting, remote AI, hidden randomness, macros, accounts, analytics, or cloud sync.

Pattern Chain Preview work must derive suggested Pattern Chain or Chain Expand labels, Priority Readout labels, Preview Decision labels, visible priority and Preview Decision action targets, any Quick Actions Pattern Chain Decision command target, Pattern A/B/C sequence, section/bar posture, energy posture, and pre-click arrangement move counts only from current local arrangement state, visible Pattern Chain preview state, and existing Pattern Chain and Chain Expand transforms; visible Pattern Chain Priority action clicks, visible Pattern Chain Preview Decision action clicks, and Quick Actions Pattern Chain Decision command runs must route only through the existing Pattern Chain apply or Chain Expand handlers and disable when aligned; keep preview, decision, and Priority Readout state UI-local and out of saved project schema and undo history; preserve Pattern Chain definitions, Pattern Chain apply behavior, Pattern Chain step editor behavior, Chain Expand behavior, manual arrangement controls, Structure Lens, Song Form Overview, Section Locator Pads, Arrangement Arc Preview, Arrangement Focus, musical events, mixer, sound design, master state, Delivery Target, Beat Readiness, Beat Map, Next Move, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid auto-arrangement, auto-apply, modal confirmations, autoplay, render downloads, auto-export, sampling, imported audio, audio clips, hidden generation, remote AI, remote analysis, plugin hosting, accounts, analytics, or cloud sync.

Pattern Chain Result work must update result labels only after explicit Pattern Chain preset clicks, visible Pattern Chain Priority action clicks, visible Pattern Chain Preview Decision action clicks, Quick Actions Pattern Chain Decision command runs, direct Pattern Chain Quick Actions command runs, or Chain Expand clicks/command runs, derive applied chain action, full-arrangement scope, before/after Pattern A/B/C sequence, section/bar posture, energy posture, muted-track posture, changed block/field impact, audition cue, and next-check text only from local before/after arrangement state and existing Pattern Chain or Chain Expand transforms; keep result state UI-local and out of saved project schema and undo history; preserve Pattern Chain Preview, Preview Decision Readout, Pattern Chain definitions, Pattern Chain apply behavior, Pattern Chain step editor behavior, Chain Expand behavior, manual arrangement controls, Structure Lens, Song Form Overview, Section Locator Pads, Arrangement Arc Preview and Result, Arrangement Focus Preview and Result, musical events, mixer, sound design, master state, Delivery Target, Beat Readiness, Beat Map, Next Move, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid auto-arrangement, auto-apply, modal confirmations, autoplay, render downloads, auto-export, sampling, imported audio, audio clips, hidden generation, remote AI, remote analysis, plugin hosting, accounts, analytics, or cloud sync.

Quick Actions Pattern Chain result metric work must derive the applied chain or expand action, Pattern A/B/C sequence, block count, hook block count, and bar count only from the explicit `pattern-chain-*`, `pattern-chain-decision`, or `chain-expand` command id/title/detail plus local arrangement state after the existing `applyPatternChain` and `expandPatternChain` handlers; preserve Pattern Chain definitions, preview/priority/decision derivation, apply/expand routing, Pattern A/B/C musical events, manual arrangement controls, playback, save/load, WAV/stem/MIDI export, Handoff, command search, scope filters, Pinned Commands, and Recent Commands; and avoid auto-arrangement, command chains, macros, autoplay, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Arrangement length work must keep per-block bar counts bounded, migrate older project files without bar counts to one bar per block, keep changes undoable, and make export meter, full-mix WAV export, and stem export follow total arrangement bars.

Arrangement playback work must map realtime playback through arrangement blocks, per-block bar counts, and Pattern A/B/C assignments by default while preserving a selected-pattern preview mode, accurate transport status, Space shortcut behavior, and existing mixer/sound/master processing.

Transport Loop work must keep Song, selected Block, and Pattern loop scope as explicit local playback state, use selected-block start bar and bar length only for realtime audition, preserve Space/Stop behavior and live project reads, avoid writing loop scope into saved project data, and keep WAV/stem/MIDI export plus Handoff Sheet semantics tied to the full arrangement rather than the audition loop.

Arrangement energy work must keep Energy as deterministic arrangement-block playback/render interpretation, apply the same gain rule to realtime arrangement playback, WAV export, and stem export, preserve selected-pattern preview at neutral energy, and avoid mutating stored pattern events.

Arrangement Move work must transform only the selected arrangement block's existing energy and mutedTracks state through undoable project history, offer only explicit user-clicked move presets, visible Arrangement Move Preview Decision action clicks, visible Arrangement Move Priority action clicks, Quick Actions Arrangement Move Decision command runs, or Quick Actions Arrangement Move command runs, derive any visible priority and Preview Decision action targets, command targets, Priority Readout labels, Preview Decision labels, and Result labels only from local selected arrangement block section, Pattern, bar, energy, muted-track state, and existing Arrangement Move preset definitions, route visible Arrangement Move Preview Decision action clicks and Quick Actions Arrangement Move Decision command runs only through the existing Arrangement Move apply path, keep Preview Decision Readout, Priority Readout, visible Arrangement Move Preview Decision action state, Quick Actions Arrangement Move Decision command state, visible Arrangement Move Priority action state, and Result state UI-local and out of saved project schema and undo history, disable or no-op the visible Arrangement Move Preview Decision action, Quick Actions Arrangement Move Decision command, and visible Arrangement Move Priority action when no selected block or local recommendation exists, keep Drop/Build/Hook Lift/Reset deterministic and editable afterward, avoid mutating Pattern A/B/C musical events, preserve realtime playback plus WAV/stem/MIDI export semantics, and avoid imported audio, sampling, plugin hosting, remote AI, hidden randomness, hidden automation, or hidden assets.

Quick Actions Arrangement Move result metric work must derive the applied selected-block move preset, selected block scope, section, Pattern A/B/C assignment, bar length, energy, mute posture, changed-field count, song block count, and total bar count only from explicit `arrangement-move` or `arrangement-move-decision` command id/title/detail/keywords plus local arrangement state after the existing `applyArrangementMoveToSelected` handler; preserve Arrangement Move preset definitions, priority/decision derivation, apply routing, Pattern A/B/C musical events, manual arrangement controls, playback, save/load, WAV/stem/MIDI export, Handoff, command search, scope filters, Pinned Commands, and Recent Commands; and avoid auto-arrangement, command chains, macros, autoplay, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Arrangement Move Command Reference work must mark Arrangement Move as `Quick Actions / Readout` only when it reflects existing Arrangement Move Preview Decision Readout, Priority Readout, current Arrangement Move commands, and Arrangement Move Result feedback; it must not change Arrangement Move definitions, apply routing, Quick Actions execution, playback, render/export, project schema, save/load, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Arrangement Focus work must derive selected-block summaries, visible priority and Preview Decision action targets, and direct Focus preset commands from local arrangement, Pattern A/B/C event state, and existing Arrangement Focus preset definitions, offer only explicit user-clicked Focus presets, visible Arrangement Focus Priority action clicks, visible Arrangement Focus Preview Decision action clicks, Quick Actions Arrangement Focus Decision command runs, or Quick Actions Arrangement Focus command runs, update only the selected block's section, Pattern assignment, bar length, energy, and mutedTracks through undoable arrangement paths, disable direct Focus preset commands, the Quick Actions Arrangement Focus Decision command, the visible Arrangement Focus Priority action, and the visible Arrangement Focus Preview Decision action when no block is selected or the preset already matches the selected block, keep selected pattern aligned with the block, preserve realtime playback plus WAV/stem/MIDI export semantics, and avoid mutating Pattern A/B/C musical events, sampling, imported audio, plugin hosting, remote AI, hidden automation, accounts, analytics, or cloud sync.

Arrangement Focus Preview work must derive suggested selected-block focus preset labels, direct focus preset command labels, Priority Readout labels, Preview Decision labels, section/pattern/bar posture, energy posture, muted-track posture, pre-click field move counts, visible priority and Preview Decision action targets, Quick Actions Arrangement Focus Decision command target, Quick Actions Arrangement Focus command target, and any direct Arrangement Focus preset Quick Actions only from current local selected arrangement block state, visible Arrangement Focus summary, visible Arrangement Focus preview, and existing Arrangement Focus preset definitions; direct focus commands, visible Arrangement Focus Priority action clicks, visible Arrangement Focus Preview Decision action clicks, and Quick Actions Arrangement Focus Decision command runs must route through the existing Arrangement Focus apply path and disable no-op presets; keep preview, decision, Priority Readout state, visible Arrangement Focus Priority action state, visible Arrangement Focus Preview Decision action state, and Quick Actions Arrangement Focus Decision command state UI-local and out of saved project schema and undo history; preserve Arrangement Focus preset definitions and apply behavior, manual arrangement controls, selected-block role readout, Arrangement Playback Readout, Structure Lens, Song Form Overview, Section Locator Pads, Arrangement Arc Preview, Pattern Chain Preview, musical events, mixer, sound design, master state, Delivery Target, Beat Readiness, Beat Map, Next Move, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid auto-arrangement, auto-apply, modal confirmations, autoplay, render downloads, auto-export, sampling, imported audio, audio clips, hidden generation, remote AI, remote analysis, plugin hosting, accounts, analytics, or cloud sync.

Arrangement Focus Result work must update result labels only after explicit Arrangement Focus preset clicks, visible Arrangement Focus Priority action clicks, visible Arrangement Focus Preview Decision action clicks, Quick Actions Arrangement Focus Decision command runs, or Quick Actions Arrangement Focus command runs, including direct focus preset commands, derive applied preset, selected-block scope, before/after section, Pattern assignment, bar length, energy, muted-track posture, changed-field impact, audition cue, and next-check text only from local before/after selected arrangement block state and existing Arrangement Focus preset definitions; keep result state UI-local and out of saved project schema and undo history; preserve Arrangement Focus Preview, Preview Decision Readout, Arrangement Focus preset definitions and apply behavior, manual arrangement controls, selected-block role readout, Arrangement Playback Readout, Structure Lens, Song Form Overview, Section Locator Pads, Arrangement Arc Preview, Pattern Chain Preview, musical events, mixer, sound design, master state, Delivery Target, Beat Readiness, Beat Map, Next Move, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid auto-arrangement, auto-apply, modal confirmations, autoplay, render downloads, auto-export, sampling, imported audio, audio clips, hidden generation, remote AI, remote analysis, plugin hosting, accounts, analytics, or cloud sync.

Quick Actions Arrangement Focus result metric work must derive the applied selected-block focus preset, selected block scope, section, Pattern A/B/C assignment, bar length, energy, mute posture, changed-field count, song block count, and total bar count only from explicit `arrangement-focus`, `arrangement-focus-decision`, or `arrangement-focus-preset-*` command id/title/detail/keywords plus local arrangement state after the existing `applyArrangementFocus` handler; preserve Arrangement Focus preset definitions, preview/priority/decision derivation, apply routing, Pattern A/B/C musical events, manual arrangement controls, playback, save/load, WAV/stem/MIDI export, Handoff, command search, scope filters, Pinned Commands, and Recent Commands; and avoid auto-arrangement, command chains, macros, autoplay, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Arrangement Focus Command Reference work must mark Arrangement Focus as `Quick Actions / Readout` only when it reflects existing Arrangement Focus Preview, Preview Decision Readout, Priority Readout, current/direct Arrangement Focus commands, and Arrangement Focus Result feedback; it must not change Arrangement Focus definitions, apply routing, Quick Actions execution, playback, render/export, project schema, save/load, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Arrangement Mute Map result metric work must derive the explicit priority or direct mute-map focus action, Arrange destination, focused layer, lane status/context, section-by-section mute/live posture, selected Pattern, editable event count, Pattern A/B/C usage, selected block, arrangement block count, song length, export readiness, transition-map posture, audition cue, and next mute-map check only from current Arrangement Mute Map command id/title/detail plus local arrangement, Pattern, mute-map, transition-map, selected-block, and export state after the existing lane-focus path; preserve Arrangement Mute Map derivation, priority selection, focus routing, selected-block mute editing, Pattern A/B/C musical events, manual arrangement controls, playback, save/load, WAV/stem/MIDI export, Handoff, command search, scope filters, Pinned Commands, and Recent Commands; and avoid auto-arrangement, command chains, macros, autoplay, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Arrangement Arc Pad work must derive full-song section, Pattern A/B/C assignment, bar length, energy, and muted-track posture from explicit local arc presets, update only existing arrangement block fields through undoable project history, keep block counts stable, keep arrangement controls manually editable, preserve Pattern A/B/C musical events, mixer, sound design, master state, Delivery Target, Beat Readiness, Beat Map, Structure Lens, Next Move, project files, snapshots, realtime playback, WAV/stem/MIDI export, and Handoff Sheet semantics, and avoid sampling, imported audio, audio clips, hidden auto-arrangement, remote AI, plugin hosting, accounts, analytics, or cloud sync.

Arrangement Arc Preview work must derive suggested full-song arc labels, direct arc pad command labels, Priority Readout labels, Preview Decision labels, visible priority and Preview Decision action targets, section/bar posture, Pattern A/B/C spread, energy posture, muted-track posture, pre-click arrangement move counts, any Quick Actions Arrangement Arc Decision command target, Quick Actions Arrangement Arc command target, and any direct Arrangement Arc pad Quick Actions only from current local arrangement state and existing Arrangement Arc Pad definitions; direct arc pad commands, visible Arrangement Arc Priority action clicks, visible Arrangement Arc Preview Decision action clicks, and Quick Actions Arrangement Arc Decision command runs must route through the existing Arrangement Arc apply path and disable pads already matching the arrangement; keep preview, decision, Priority Readout state, and Quick Actions Arrangement Arc Decision command state UI-local and out of saved project schema and undo history; preserve Arrangement Arc Pad definitions and apply behavior, manual arrangement controls, Structure Lens, Song Form Overview, Section Locator Pads, Arrangement Focus, Pattern Chain, musical events, mixer, sound design, master state, Delivery Target, Beat Readiness, Beat Map, Next Move, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid auto-arrangement, auto-apply, modal confirmations, autoplay, render downloads, auto-export, sampling, imported audio, audio clips, hidden generation, remote AI, remote analysis, plugin hosting, accounts, analytics, or cloud sync.

Arrangement Arc Result work must update result labels only after explicit Arrangement Arc Pad clicks, visible Arrangement Arc Priority action clicks, visible Arrangement Arc Preview Decision action clicks, Quick Actions Arrangement Arc Decision command runs, or Quick Actions Arrangement Arc command runs, derive applied arc pad, full-arrangement scope, before/after section flow, Pattern A/B/C spread, bar length, energy posture, muted-track posture, changed block/field impact, audition cue, and next-check text only from local before/after arrangement state and existing Arrangement Arc Pad definitions; keep result state UI-local and out of saved project schema and undo history; preserve Arrangement Arc Preview, Preview Decision Readout, Arrangement Arc Pad definitions and apply behavior, manual arrangement controls, Structure Lens, Song Form Overview, Section Locator Pads, Arrangement Focus, Pattern Chain, musical events, mixer, sound design, master state, Delivery Target, Beat Readiness, Beat Map, Next Move, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid auto-arrangement, auto-apply, modal confirmations, autoplay, render downloads, auto-export, sampling, imported audio, audio clips, hidden generation, remote AI, remote analysis, plugin hosting, accounts, analytics, or cloud sync.

Quick Actions Arrangement Arc result metric work must derive the applied full-song arc pad, section flow, Pattern A/B/C spread, average energy, energy range, mute posture, block count, and bar count only from explicit `arrangement-arc`, `arrangement-arc-decision`, or `arrangement-arc-pad-*` command id/title/detail/keywords plus local arrangement state after the existing `applyArrangementArc` handler; preserve Arrangement Arc pad definitions, preview/priority/decision derivation, apply routing, Pattern A/B/C musical events, manual arrangement controls, playback, save/load, WAV/stem/MIDI export, Handoff, command search, scope filters, Pinned Commands, and Recent Commands; and avoid auto-arrangement, command chains, macros, autoplay, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Arrangement Arc Command Reference work must mark Arrangement Arc as `Quick Actions / Readout` only when it reflects existing Arrangement Arc Preview, Preview Decision Readout, Priority Readout, current/direct Arrangement Arc commands, and Arrangement Arc Result feedback; it must not change Arrangement Arc definitions, apply routing, Quick Actions execution, playback, render/export, project schema, save/load, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Live playback edit work must read current project state while scheduling future steps, let selected-pattern preview and arrangement playback respond to edits without stopping, update BPM and master output changes on future scheduling/output, preserve Stop and Space behavior, and state that already-triggered audio is not rewritten.

Undo/redo work must record bounded local project-edit history for editing actions, ignore playback/export side effects, keep keyboard shortcuts out of focused inputs, clear history when a different project file is loaded, and keep edit history readouts UI-local, derived only from labeled undo/redo entries, stack depth, and project status. History labels must come from existing explicit project-edit status text and stay out of persisted project schema.

Undo/Redo Result work must update result labels only after successful undo or redo from toolbar buttons, desktop shortcuts, Native Command Menu, or Quick Actions; derive the restored/replayed edit label, current event count, remaining undo/redo depth, recovery cue, and next-check text only from the existing undo/redo stacks and restored local project state; clear stale result state on blocked empty-stack undo/redo attempts, new project mutations, project replacement, and history restore transitions before the new result is set; keep result state UI-local and out of saved project schema, history entries, realtime playback state, render/export output, MIDI export, Handoff, and local draft data; and avoid history branching UI, timeline diffing, confirmation modals, autoplay, auto-save, auto-export, sampling, imported audio, remote AI, accounts, analytics, and cloud sync.

Quick Actions Undo/Redo result metric work must identify only the explicit history action, command context, restored or replayed edit label, selected Pattern, editable drum/808/Synth/chord counts, total editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, and next listening or history check from current Undo/Redo command title/detail plus restored local project state; preserve undo/redo stack semantics, history entry labels, project restoration, toolbar buttons, desktop shortcuts, Native Command Menu routing, Command Reference, local draft recovery, snapshots, playback, save/load, render/export, MIDI export, Handoff, project schema, privacy boundaries, and sampling boundaries; and avoid history branching UI, timeline diffs, confirmation modals, autosave, auto-export, macros, command chains, hidden generation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Project File Result work must update result labels only after successful project save, browser download fallback, Electron open, or browser import; derive file action, active file label, editable event count, safety cue, and next-check text only from explicit file action results and local project state; clear stale result state on canceled, failed, or invalid file actions, new project mutations, project replacement, and history restore transitions; keep result state UI-local and out of saved project schema, local draft records, undo/redo history, realtime playback state, render/export output, MIDI export, Handoff, and local draft data; and avoid autosave, cloud sync, accounts, analytics, destructive filesystem actions, remote AI, sampling, imported audio, and sample-pack workflows.

Quick Actions Project File result metric work must identify only the explicit save or open action, command context, default project-file name, file-safety posture, selected Pattern, editable drum/808/Synth/chord counts, total editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, and next file-safety or listening check from current Project File command title/detail plus local project state; preserve serialization, file dialogs, browser download/import fallback, Electron save/open behavior, Project File Result, Local Draft Recovery Result, Project Safety Readout, local draft storage, snapshots, undo/redo history reset semantics, playback, save/load, render/export, MIDI export, Handoff, project schema, privacy boundaries, and sampling boundaries; and avoid autosave, file versioning, destructive filesystem actions, cloud sync, accounts, analytics, remote AI, macros, command chains, hidden generation, sampling, imported audio, sample-pack workflows, autoplay, or auto-export.

Local Draft Recovery Result work must update result labels only after explicit Restore Draft or Clear Draft actions; derive restored/cleared state, saved draft label, affected project label, editable event count, draft character count, safety cue, and next-check text only from the existing local draft recovery record and local project state; clear stale result state on no-op restore attempts, new project mutations, project replacement, history restore transitions, and file save/open/import failure or success paths; keep result state UI-local and out of saved project schema, local draft records, undo/redo history payloads, realtime playback state, render/export output, MIDI export, Handoff, and project files; and avoid autosave, file versioning, cloud sync, accounts, analytics, destructive filesystem actions, remote AI, sampling, imported audio, and sample-pack workflows.

Quick Actions Local Draft result metric work must identify only the explicit restore or clear action, command context, local draft-safety posture, selected Pattern, editable drum/808/Synth/chord counts, total editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, and next recovery or safety check from current Local Draft command title/detail plus local project state; preserve local draft storage format, parser behavior, localStorage key/version/size limits, restore routing, clear routing, Local Draft Recovery Result, Project File Result, Project Safety Readout, project files, snapshots, undo/redo history payloads, playback, save/load, render/export, MIDI export, Handoff, project schema, privacy boundaries, and sampling boundaries; and avoid autosave, file versioning, destructive filesystem actions, command chains, cloud sync, accounts, analytics, remote AI, hidden generation, sampling, imported audio, sample-pack workflows, autoplay, or auto-export.

Local draft Quick Actions work must expose only explicit Project-scope Restore Draft and Clear Draft commands, keep both disabled when no local draft recovery record exists, and route command runs only through the same handlers as the visible Restore Draft and Clear Draft controls; reuse Local Draft Recovery Result and Quick Action Result feedback after explicit command runs; preserve local draft storage format, parser behavior, localStorage key/version/size limits, project files, undo/redo history payloads, playback, render/export, MIDI export, Handoff, command ranking, pinned/recent command behavior, and visible banner behavior; and avoid autosave, file versioning, destructive filesystem actions, command chains, cloud sync, accounts, analytics, remote AI, sampling, imported audio, and sample-pack workflows.

Restore Draft and Clear Draft Command Reference work must mark both Project rows as `Quick Actions / Readout`, reflect only the existing renderer-local draft recovery, Project Safety Readout context, and Local Draft Recovery Result feedback; preserve local draft storage format, localStorage key/version/size limits, parser behavior, Restore Draft behavior, Clear Draft behavior, visible local draft banner behavior, Project Safety Readout, Project File Result, Local Draft Recovery Result, save/open/import/download behavior, file dialogs, `.grooveforge.json` serialization, project schema, undo/redo history payloads, realtime playback, render/export, MIDI export, Handoff Pack, Handoff Sheet, snapshots, current project data outside explicit Restore Draft clicks, privacy boundaries, and sampling boundaries; and avoid autosave, file versioning, destructive filesystem actions, cloud sync, accounts, analytics, remote AI, sampling, imported audio, sample-pack workflows, macros, command chains, autoplay, or auto-export.

Project Snapshots and Snapshot Compare Command Reference work must mark both Project rows as `Quick Actions / Readout`, reflect only existing local project-file idea slots, Snapshot slot role readout, Snapshot Compare focus command, direct comparison metric commands, and Snapshot Compare Focus Result feedback; preserve Project Snapshot save, rename, restore, delete, capacity, nested snapshot stripping, snapshot payloads, Snapshot Compare derivation, focus routing, focus result labels, Project Safety Readout, Project File Result, Local Draft Recovery Result, save/open/import/download behavior, file dialogs, `.grooveforge.json` serialization, project schema beyond existing snapshot data, undo/redo history payloads, realtime playback, render/export, MIDI export, Handoff Pack, Handoff Sheet, arrangement, musical events, mixer/master state, privacy boundaries, and sampling boundaries; and avoid autosave, file versioning, destructive filesystem actions, cloud sync, accounts, analytics, remote AI, sampling, imported audio, sample-pack workflows, macros, command chains, autoplay, auto-restore, or auto-export.

Quick Actions Project Snapshot result metric work must identify only the explicit save snapshot action, command context, local idea-slot posture, latest snapshot name/summary, selected Pattern, editable drum/808/Synth/chord counts, total editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, and next compare or durable-file safety check from current Save Snapshot command title/detail plus local project state; preserve Project Snapshot save, rename, restore, delete, capacity, nested snapshot stripping, snapshot payloads, Snapshot Compare derivation, Project Safety Readout, Project File Result, Local Draft Recovery Result, save/open/import/download behavior, file dialogs, `.grooveforge.json` serialization, project schema beyond existing snapshot data, undo/redo history payloads, realtime playback, render/export, MIDI export, Handoff Pack, Handoff Sheet, arrangement, musical events, mixer/master state, privacy boundaries, and sampling boundaries; and avoid autosave, file versioning, destructive filesystem actions, cloud sync, accounts, analytics, remote AI, hidden generation, sampling, imported audio, sample-pack workflows, macros, command chains, autoplay, auto-restore, or auto-export.

Project Safety Readout Quick Action work must expose only an explicit Project-scope readout command, route command runs only to UI-local status/result feedback, and derive result metrics only from the current Project Safety Readout command title/detail plus local project state: readout context, default project-file name, snapshot slot count, selected Pattern, editable drum/808/Synth/chord counts, total editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, and next save/recovery check; preserve Project Safety Readout derivation, Project File Result, Local Draft Recovery Result, Project Snapshot save/rename/restore/delete behavior, local draft storage, save/open/import/download behavior, file dialogs, `.grooveforge.json` serialization, project schema, undo/redo history payloads, realtime playback, render/export, MIDI export, Handoff Pack, Handoff Sheet, arrangement, musical events, mixer/master state, privacy boundaries, and sampling boundaries; and avoid autosave, file versioning, destructive filesystem actions, cloud sync, accounts, analytics, remote AI, hidden generation, sampling, imported audio, sample-pack workflows, macros, command chains, autoplay, auto-restore, or auto-export.

Desktop shortcut work must keep playback, Pattern A/B/C selection, save/open, undo/redo, and selected event deletion scoped to the app window, must not fire from focused editable controls, and must route destructive deletion through normal undoable project history.

Native Command Menu work must define allowlisted Electron menu command ids, send them only through the safe preload bridge, and route renderer handling only to existing Open Project, Save Project, Undo, Redo, Quick Actions, Command Reference, Play/Stop, and Delete Selected Event handlers; preserve save/open dialogs, focused-input keyboard guards, undo/redo history, selected-event deletion semantics, playback scheduling, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack behavior; keep menu accelerators display-only when renderer shortcuts own input guards; and avoid global OS automation, background autosave, macros, destructive filesystem commands, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Beat Terms Glossary work must keep the Command Reference glossary static and informational, add only local Pattern, Drums, 808/Bass, Chords, Swing, Velocity/Chance, Style Profile, Beat Blueprint, Sound, Arrangement, Section, Hook, Pattern Chain, Space FX, Stem, Headroom, Mix/Master, Export Meter, Master Automation, Beat Readiness, Delivery Target, MIDI, Handoff Sheet, and Handoff entries, keep term search limited to term, meaning, and target text, preserve Command Reference opening, closing, section filters, Search Spotlight derivation, Beat Terms Reference Quick Action routing, project data, playback, save/load, render/export, snapshots, privacy boundaries, and sampling boundaries; and avoid tutorials, onboarding overlays, remote lookup, remote AI, accounts, analytics, cloud sync, imported audio, sampling, sampler devices, audio analysis, macros, command chains, autoplay, auto-fixes, or project mutations.

Command Reference work must stay UI-local and read-only, open only from the header button, Help menu, `?`/`CmdOrCtrl+/` desktop shortcut, or explicit Quick Actions command, close through the same local overlay path, keep any section filter non-persistent and limited to displaying All, Desktop, Project, Guide, Create, Sound, Arrange, Mix, Finish, Deliver, or Beat Terms entries, keep any text search non-persistent and limited to filtering command, shortcut, target, section, term, meaning, and target-label text, keep search autofocus, Clear Search, Show All recovery controls, and any Search Spotlight readout limited to UI-local search/filter state without executing commands, derive any Search Spotlight only from the first current visible command or Beat Terms match, cover current local command-map items including Project Safety Readout, Project File Result, local draft recovery commands, Transport Position Readout, Loop Scope, Metronome, First Beat Path, Beat Spine, Mode Focus, Mode Switch, Session Pass, Session Brief Compass, Reference Alignment, Composer Guide, Key Compass, Groove Compass, Listening Pass, Beat Passport, Production Snapshot, Beat Readiness, Review Queue, Workflow Navigator, Workflow Spotlight, Beat Map, Structure Lens, Next Move, Tap Tempo, Tempo Nudge, Swing Feel, Key Retarget, Style Quick Picks, Keyboard Capture, Capture Step Mode, MIDI Input, Editor Audition, Pattern Playback Readout, Arrangement Playback Readout, Beat Blueprints, Style Inspector, Composer Actions, Pattern Stack, Pattern Compare, Pattern Compare Decision, Pattern DNA, Pattern Variation, Pattern Fill, Pattern Clone, Pattern Copy/Clear, Drum Move, 808 Move, Melody Move, Chord Move, selected-event edit tools, Sound Preset Decision, Sound Preset, Drum Kit Decision, Drum Kit, Sound Focus Decision, Sound Focus, Timbre Check, Sound Snapshot A/B Decision, Sound Snapshot A/B, Space FX Decision, Space FX, Pattern Chain, Chain Expand, Arrangement Template, Arrangement Arc, Arrangement Focus, Arrangement Move, Section Locator, Song Form Overview, Arrangement Mute Map, Arrangement Transition Map, Stem Audition Readout, Stem Audition Decision, Stem Audition, Mix Snapshot A/B Decision, Mix Snapshot A/B, Mix Balance Decision, Mix Balance, Mix Coach, Master Finish Decision, Master Finish, Finish Checklist, Delivery Target Alignment, Export Preflight, Export Format Readout plus Handoff Export Format focus/metric commands, Handoff Send Order, Handoff Manifest Audit, Handoff Export Receipt, Handoff Package Check, Handoff Next Export, direct export commands, Style Goal Cue commands, Style Goal Action commands, Audible Pattern Follow, Audible Arrangement Follow, Master Automation Decision and fade commands, and Beat Terms without running them, keep Beat Terms static and informational, route any Beat Terms Reference Quick Action only through the existing Command Reference open handler, and avoid mutating project data, undo history, playback, save/load, render/export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Style Goal Cues and Style Goal Actions Command Reference work must mark both Create rows as `Quick Actions / Readout`, reflect only the existing direct style-writing cue commands, local Style Goal Cue Result feedback, matching result-action routing, direct Style Goal Action commands, and local Style Goal Action Result feedback; preserve Style Goal scoring, card derivation, command derivation, cue target derivation, cue-result labels, result-action matching, Style Goal Action result derivation, Composer Action derivation, Composer Action execution, Layer Starter, Drum Move, 808 Move, Melody Move, Chord Move, Pattern Chain, arrangement actions, Pattern A/B/C event data outside explicit clicked actions, selected Pattern behavior, loop-scope behavior, playback start/stop, save/load, undo/redo, project schema, render/export, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid tutorials, macros, command chains, hidden generation, automatic arrangement, automatic mixing, automatic mastering, automatic export, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Keyboard Capture, Capture Step Mode, MIDI Input, and Editor Audition Command Reference work must mark all four Create rows as `Quick Actions / Readout`, reflect only the existing local input posture, capture placement/defaults, Web MIDI status, selected-event audition, Input Capture Result feedback, and Editor Audition success/fallback Result feedback; preserve Keyboard Capture toggling, target selection, key mapping, capture defaults, capture insertion, duplicate blocking, Capture Step Mode behavior, Web MIDI permission handling, input parsing, arm/disarm behavior, note mapping, status handling, Editor Audition one-shot synthesis, selected-event routing, selected-event state, audition result derivation, project schema, save/load, undo/redo, local drafts, realtime playback, render/export, MIDI export, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid recording, audio input, MIDI output, clock sync, sampler devices, imported audio, sampling, hidden generation, remote AI, accounts, analytics, cloud sync, macros, command chains, autoplay, or auto-export.

Editor Audition work must be explicit, UI-local, and one-shot; derive selected Drum, 808, Synth, and Chord audition only from the selected Pattern A/B/C event plus current built-in sound and mixer/master posture; route visible controls and Quick Actions selected-event audition commands only through the local Web Audio scheduler one-shot path; keep audition out of project schema, undo history, save/load, realtime playback state, render/export output, MIDI export, Handoff, and local draft data; avoid autoplay loops, recording, audio input, sample import, sampler devices, MIDI output, clock sync, remote AI, accounts, analytics, and cloud sync.

Editor Audition Result work must update result labels only after explicit selected drum hit, 808/Synth note, or chord audition succeeds from visible controls or Quick Actions, or after that same explicit attempt is blocked by runtime audio startup; derive the result target, Pattern label, pocket/pitch/voicing metric, audition cue, runtime fallback cue, and next-check text only from the selected Pattern A/B/C event, current local editor state, and local runtime outcome; clear stale result state on project mutation, project replacement, undo/redo restore, or selected event changes; keep result state UI-local and out of saved project schema, undo history, realtime playback state, render/export output, MIDI export, Handoff, and local draft data; and avoid autoplay, recording, audio input, sample import, sampler devices, remote AI, accounts, analytics, and cloud sync.

Quick Actions selected-event result metric work must identify only the explicit selected drum, selected note, or selected chord edit/audition action, selected-event lane, command context, selected Pattern, editable drum/note/chord counts, total editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, and next manual edit or audition check from current selected-event command title/detail plus local project state; preserve selected drum/note/chord edit algorithms, selected-event state, copy/paste clipboard behavior, audition synthesis, undo/redo, keyboard capture, MIDI input, Key Compass, Groove Compass, Pattern DNA, Pattern A/B/C event data, arrangement data, playback, save/load, render/export, project schema, privacy boundaries, and sampling boundaries; and avoid macros, command chains, hidden generation, autoplay, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Selected Event Delete Result work must update result labels only after successful selected drum hit, 808/Synth note, or chord deletion from visible controls, desktop deletion, Native Command Menu, or Quick Actions; derive the result target, Pattern label, pocket/pitch/harmony metric, Undo cue, and next-check text only from the removed selected Pattern A/B/C event and current local editor state; clear stale result state on no-op or blocked delete attempts, project mutation, project replacement, undo/redo restore, selected Pattern changes, or selected event changes; keep result state UI-local and out of saved project schema, undo history beyond the existing delete edit, realtime playback state, render/export output, MIDI export, Handoff, and local draft data; and avoid confirmation modals, soft-delete staging, multi-select deletion, autoplay, recording, audio input, sample import, sampler devices, remote AI, accounts, analytics, and cloud sync.

Keyboard Capture work must route desktop key presses into scale-locked 808 or Synth notes as editable local musical events, keep focused inputs/textareas/selects/contenteditable controls protected, make every captured note undoable through project history, expose the target, target-specific octave bank, degree-labeled key map, UI-local Capture Step Mode, UI-local command-strip posture readout, and UI-local capture defaults in the 808/Melody editor, derive posture readouts only from existing capture state, target, next step, step mode, and defaults, route any Quick Actions Keyboard Capture toggle only through the existing UI-local enabled-state setter, route any Quick Actions capture target commands only through the existing UI-local target setter, route any Quick Actions capture-step-mode commands only through the existing UI-local step-mode setter, route any Quick Actions capture-default commands only through the existing UI-local defaults updater, apply captured-note octave/length plus 808/Synth velocity and 808 glide defaults only to newly captured notes, keep enabled state, target, step mode, degree labels, posture readouts, and capture defaults out of saved project schema, keep next-empty capture as the default, use selected-step replacement only after an explicit mode change and same-track selected note, and avoid Web MIDI permission prompts, audio recording, sampling, imported audio, sampler tracks, remote AI, accounts, analytics, or cloud sync.

Web MIDI Input work must request MIDI access only after explicit user action, pass `sysex: false`, derive input status only from local MIDIAccess/MIDIInput state, parse Note On messages only, map notes into selected 808 or Synth scale-locked Pattern A/B/C events through existing Keyboard Capture defaults, Capture Step Mode, and undoable note insertion, keep MIDI access/device/message/arm/status plus capture step mode UI-local and out of project schema, route any Quick Actions MIDI Input Connect command only through the existing explicit connect/refresh handler, route any Quick Actions MIDI Arm/Disarm command only through existing UI-local arm state and enable it only when a connected MIDI input exists, preserve keyboard capture, save/load, playback, render/export, MIDI file export, Handoff behavior, and avoid SysEx, MIDI output, clock sync, controller mapping, background recording, audio input, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Input Capture Result work must update result labels only after successful Desktop Keyboard Capture or Web MIDI note capture inserts or replaces an 808/Synth note; derive source, captured pitch, Pattern label, step, length, velocity, capture mode, audition cue, and next-check text only from local capture state, selected Pattern A/B/C, current project key, and the captured note payload; clear stale result state on project mutation, project replacement, undo/redo restore, selected Pattern changes, capture target changes, Capture Step Mode changes, or capture default changes; keep result state UI-local and out of saved project schema, undo history, realtime playback state, render/export output, MIDI export, Handoff, MIDI device state, and local draft data; and avoid showing results for blocked duplicate, out-of-range, or ignored capture attempts, recording, audio input, sample import, sampler devices, MIDI output, clock sync, remote AI, accounts, analytics, and cloud sync.

Quick Actions Input Capture setup result metric work must identify only the explicit Keyboard Capture, Capture Target, Capture Step Mode, Capture Default, MIDI Connect, or MIDI Arm/Disarm setup command, Create route, selected 808/Synth target, Keyboard Capture armed posture, MIDI armed/status/input posture, placement mode, selected Pattern, editable event count, Pattern A/B/C usage, pitch map, octave, length, velocity, glide default, current 808/Synth note counts, arrangement block count, song length, export readiness, audition cue, and next input-capture check from current input setup command id/title/detail plus UI-local Keyboard Capture/MIDI state and local project state after the existing input-state handler; preserve Keyboard Capture toggling, target selection, key mapping, capture defaults, Capture Step Mode behavior, MIDI permission handling, MIDI connect/refresh, MIDI arm/disarm, MIDI input selection, note insertion/replacement algorithms, duplicate blocking, Pattern A/B/C musical events, selected-note edit defaults, project schema, playback, save/load, render/export, privacy boundaries, and sampling boundaries; and avoid auto-capture, note insertion from setup commands, command chains, macros, autoplay, auto-save, auto-export, sampling, imported audio, MIDI output, clock sync, remote AI, accounts, analytics, or cloud sync.

Quick Actions work must remain local and explicit; must not fire while focused editable targets receive keyboard input; must route mutating commands such as Tap Tempo, Metronome, Tempo Nudge, Swing Feel, Key Retarget, Style Quick Picks, direct Beat Blueprint Apply, Delivery Target select, Layer Starter and direct layer starts, Pattern Stack and direct stack pads, Drum Move, 808 Move, Melody Move, Chord Move, selected-drum edit tools, selected-note edit tools, selected-chord edit tools, selected-block priority/edit tools, Delivery Target Align, Session Brief Starter commands, Hook Fix commands, Topline Fix commands, Review Fix commands, Sound Preset Decision, Sound Preset, and direct preset commands, Drum Kit Decision, Drum Kit, and direct kit pad commands, Sound Focus Decision, Sound Focus, and direct focus pad commands, Sound Snapshot A/B Decision, Sound Snapshot A/B recall commands, Beat Spine apply and direct card apply, Pattern Chain, Chain Expand, Arrangement Template and direct template commands, Arrangement Arc and direct arc pad commands, Arrangement Focus and direct focus preset commands, Arrangement Move, Stem Audition, Mix Snapshot A/B Decision, Mix Snapshot A/B, Mix Balance Decision, Mix Balance, and direct balance pad commands, Space FX Decision, and Space FX through existing explicit project update paths or documented UI-local handlers; must route Sound Snapshot A/B capture and clear commands through documented UI-local handlers; must route selected-block-copy, selected-drum-copy, selected-note-copy, and selected-chord-copy through UI-local clipboard handlers until a separate explicit paste or duplicate command writes project data; must route selected-drum-audition, selected-note-audition, and selected-chord-audition only through the UI-local one-shot editor audition handler; must route UI-local Keyboard Capture toggle, capture target, capture-step-mode, capture-default, MIDI Input Connect, and MIDI Arm/Disarm commands only through existing input-state handlers; must route export commands including Handoff Next Export only through existing explicit export handlers after user command clicks; must keep project/open/save/export actions user-triggered; must derive post-run result status, metrics, audition cues, and next checks only from explicit command metadata and local before/after project state; direct export Quick Actions result metrics must derive only from explicit WAV, stems, MIDI, or Handoff Sheet command ids, current local project state, deterministic export/stem analysis, existing filename helpers, arrangement length, Delivery Target, and Session Brief state while preserving export file contents and handlers; pinned commands must store only bounded UI-local action ids and must run only current Quick Action definitions after explicit clicks; must keep result strips UI-only and informational rather than modal confirmations, macros, autoplay, auto-save, auto-export, note insertion, or project data; focus-only commands such as First Beat Path jump and step jump, Mode Focus jump and card jump, Beat Spine jump and direct card jump, Style Inspector focus and lane focus with local Focus Result feedback, Timbre Check focus with local result feedback, Key Compass focus and card focus, Groove Compass focus and card focus, Pattern DNA focus and card focus with local Focus Result feedback, Beat Readiness focus and card focus with local Focus Result feedback, Audible Pattern Follow, Audible Arrangement Follow, Listening Pass focus and checkpoint focus with local Focus Result feedback, Beat Passport focus and metric focus with local Focus Result feedback, Production Snapshot focus and metric focus with local Focus Result feedback, Reference Alignment focus and card focus with local Focus Result feedback, Hook Readiness focus and card focus with local Focus Result feedback, Topline Space focus and card focus with local Focus Result feedback, Arrangement Mute Map focus and lane focus with local Focus Result feedback, Arrangement Transition Map focus and transition focus with local Focus Result feedback, Handoff Export Format focus and metric focus with local Focus Result feedback, Handoff Send Order focus with local Focus Result feedback, Handoff Manifest Audit focus with local Focus Result feedback, Handoff Export Receipt focus with local Focus Result feedback, Handoff Package Check focus and card focus with local Focus Result feedback, Finish Checklist focus and card focus with local Focus Result feedback, Composer Guide focus, Session Pass focus and card focus, Session Brief Compass focus and card focus with local Focus Result feedback, Review Queue focus with local Focus Result feedback, Export Preflight focus and card focus with local Focus Result feedback, and Workflow Spotlight focus must use existing panel jump handlers or documented field refs without changing project data; UI-local cue commands such as Arrangement Block Cue, Section Locator, Pattern Cue, Groove Compass Cue, Style Goal Cue, Beat Blueprint Preview Cue, Transition Loop cue, Hook Loop cue, and Topline Loop cue must only set existing selection and loop state without autoplay or data mutation; and must avoid scripting, global OS shortcuts, sampling, plugin hosting, remote AI, automation recording, accounts, analytics, or cloud sync.

Quick Actions Style Quick Pick work must derive commands and result feedback only from local `styleProfiles`, explicit command metadata, and local project data; route command runs only through the existing style-selection handler used by the Style dropdown and Style Quick Picks; allow the current style to be explicitly reapplied through the same path; show applied style, BPM, swing, bass/melody roles, Pattern A/B/C event count, current edit Pattern, audition cue, and next check only after an explicit command run; preserve Style Inspector focus behavior, current-style starter preview/apply behavior, undo/redo, save/load, playback, WAV/stem/MIDI export, and Handoff semantics; and avoid auto-applying styles, adding or changing style profiles, hidden generation, command chains, sampling, imported audio, sampler devices, remote AI, accounts, analytics, or cloud sync.

Quick Actions current-style starter work must derive the matched starter only from local `project.styleId`, `styleProfiles`, `beatBlueprints`, and `suggestedBlueprintId(project)`; route the command only through the existing `onApplyBlueprint` / Beat Blueprint apply/result path; show starter name, style, key, BPM, arrangement length, Pattern A/B/C event count, edit Pattern, audition cue, and next check only after explicit command runs; keep the command explicit, searchable, UI-local, and out of saved project schema; preserve the existing generic Blueprint command, scope filters, Spotlight, Recent Commands, command result strips, undo/redo, save/load, playback, WAV/stem/MIDI export, and Handoff semantics; and avoid auto-applying on style change, command-chain behavior, hidden generation, sampling, imported audio, sampler devices, remote AI, accounts, analytics, or cloud sync.

Quick Actions current-style starter preview work must derive the preview target only from local `project.styleId`, `styleProfiles`, `beatBlueprints`, and `suggestedBlueprintId(project)`; route the command only through existing Beat Blueprint preview state without mutating project data, undo/redo history, save/load data, playback, render/export, Handoff state, or apply results; keep the preview result informational and UI-local; show starter name, style, key, BPM, arrangement template, no-edit posture, audition cue, and next check only after explicit preview command runs; preserve the generic Blueprint apply command, current-style starter apply command, Blueprint panel preview/apply behavior, Quick Actions scope filters, Spotlight, Recent Commands, and command result strips; and avoid auto-applying, command-chain behavior, modal confirmations, hidden generation, sampling, imported audio, sampler devices, remote AI, accounts, analytics, or cloud sync.

Quick Actions direct Blueprint preview/apply work must derive commands and result metrics only from local `beatBlueprints`, `styleProfiles`, existing Blueprint metadata, and local project data; route Preview commands only through existing Beat Blueprint preview state and route Apply commands only through the existing `onApplyBlueprint` / Beat Blueprint apply/result path; reveal the existing Beat Blueprints panel through the same UI-local Blueprint focus path; preserve current-style starter commands, generic Blueprint command, Blueprint panel preview/apply behavior, Quick Actions scope filters, Spotlight, Recent Commands, command result strips, undo/redo, save/load, playback, WAV/stem/MIDI export, Handoff state, and saved project schema; and avoid auto-applying, command-chain behavior, hidden generation, sampling, imported audio, sampler devices, remote AI, accounts, analytics, or cloud sync.

Quick Actions Beat Blueprint Preview Decision work must derive the command target only from local `beatBlueprintPreviewId`, `beatBlueprints`, `project.styleId`, `styleProfiles`, `suggestedBlueprintId(project)`, and the existing Beat Blueprint Preview Decision helper; route Apply Preview decisions only through the existing `onApplyBlueprint` / Beat Blueprint apply/result path and route Compare Style Match decisions only through existing Beat Blueprint preview state; keep decision result feedback UI-local and derived from command metadata plus local before/after project state; preserve Blueprint panel preview/apply behavior, Preview Listening Cue behavior, Quick Actions scope filters, Spotlight, Recent Commands, command result strips, undo/redo, save/load, playback, WAV/stem/MIDI export, Handoff state, and saved project schema; and avoid auto-playing, auto-applying, command-chain behavior, hidden generation, sampling, imported audio, sampler devices, remote AI, accounts, analytics, or cloud sync.

Quick Actions Beat Blueprint Preview Cue work must derive the command target only from local `beatBlueprintPreviewId`, `beatBlueprints`, `project.styleId`, `styleProfiles`, `suggestedBlueprintId(project)`, and the existing Beat Blueprint Preview Listening Cue helper; route the command only through the existing Beat Blueprint preview cue handler and existing Song/Pattern transport loop selection; keep the command disabled while playback is running; preserve Blueprint preview/apply behavior, Quick Actions scope filters, Spotlight, Recent Commands, command result strips, undo/redo, save/load, playback start/stop, WAV/stem/MIDI export, Handoff state, and saved project schema; and avoid auto-playing, auto-applying, command-chain behavior, hidden generation, sampling, imported audio, sampler devices, remote AI, accounts, analytics, or cloud sync.

Beat Blueprints Command Reference work must mark the Create row as `Quick Actions / Readout`, reflect only the existing sample-free style starts, current-style Match, Preview Decision, Preview Listening Cue, direct Preview/Apply commands, and local Beat Blueprint Result feedback; preserve Beat Blueprint definitions, supported styles, current-style match derivation, preview derivation, Preview Decision logic, Preview Listening Cue logic, direct Preview/Apply command generation, focus routing, result metrics, audition cues, next-check text, Blueprint apply behavior, undo behavior, Pattern A/B/C event generation, arrangement template selection, sound preset selection, mixer/master defaults, project data, playback, render/export, project schema, save/load, snapshots, Handoff Pack, Handoff Sheet, privacy boundaries, and sampling boundaries; and avoid auto-apply, command chains, hidden generation, autoplay, auto-export, audio analysis, imported audio, sampling, sampler devices, remote AI, accounts, analytics, or cloud sync.

Quick Actions Blueprint focus work must use only UI-local refs and explicit Blueprint Quick Action clicks to reveal the existing Beat Blueprints panel; preserve in-panel Blueprint preview/apply behavior, existing Quick Action ids, search/filter/Spotlight/Recent/result behavior, project data, undo/redo, save/load, playback, render/export, Handoff state, and Beat Blueprint result derivation; and avoid duplicating Blueprint preview UI, command-chain behavior, auto-apply, hidden generation, sampling, imported audio, sampler devices, remote AI, accounts, analytics, or cloud sync.

Quick Actions Scope Filter work must derive scope filters and Scope Filter Result feedback only from existing Quick Action groups and ids, current search query, current scope counts, filtered visible commands, and the first runnable Enter target, keep the selected scope and result feedback UI-local and out of saved project schema, localStorage, undo history, and analytics, show Scope Filter Result feedback only after explicit scope-filter clicks with selected scope, search posture, shown/matching command count, Enter target, and next explicit command check, preserve command search token behavior, scope count derivation, filtered result order, Spotlight Enter behavior within the filtered visible list, explicit command clicks, post-run result strips, Pinned Commands, Recent Commands, keyboard shortcut guards, and all command run handlers, avoid mutating project data from filtering alone, and avoid macros, auto-run, autoplay, auto-save, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Search Result work must derive Search Result feedback only from the current search query, selected scope, scope counts, filtered visible Quick Actions, and the first runnable Enter target; keep query state and result feedback UI-local and out of saved project schema, localStorage, undo history, and analytics; show Search Result feedback after search query edits with query posture, selected scope, shown/matching command count, Enter target, and next explicit command check; preserve command search token behavior, scope count derivation, filtered result order, Scope Filter result behavior, Spotlight Enter behavior, explicit command clicks, post-run result strips, Pinned Commands, Recent Commands, keyboard shortcut guards, and all command run handlers; and avoid mutating project data from searching alone, macros, auto-run, autoplay, auto-save, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Search Hint chips work must derive visible hint chips only from an empty search query, selected scope, fixed scope-aware hint terms, and existing Quick Action command definitions; keep hint state UI-local and out of saved project schema, localStorage, undo history, and analytics; route hint clicks only through the existing query-change handler so they fill search text without running commands; preserve command search token behavior, scope count derivation, filtered result order, Search Result behavior, Scope Filter result behavior, Search Recovery guidance, Search Recovery Result behavior, Spotlight Enter behavior, explicit command clicks, post-run result strips, Pinned Commands, Recent Commands, keyboard shortcut guards, and all command run handlers; and avoid mutating project data from hint clicks, macros, auto-run, autoplay, auto-save, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Search Hint Result work must derive result feedback only after explicit Search Hint clicks from the applied query, selected scope, scope counts, filtered visible Quick Actions, and the first runnable Enter target; keep hint result feedback UI-local and out of saved project schema, localStorage, undo history, and analytics; route hint clicks only through the dedicated hint apply handler and existing query-change state path without running commands; preserve command search token behavior, scope count derivation, filtered result order, Search Result behavior, Scope Filter result behavior, Search Hint chip derivation, Search Recovery guidance, Search Recovery Result behavior, Spotlight Enter behavior, explicit command clicks, post-run result strips, Pinned Commands, Recent Commands, keyboard shortcut guards, and all command run handlers; and avoid mutating project data from hint result feedback, macros, auto-run, autoplay, auto-save, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Search Recovery work must derive no-match recovery guidance only from the current search query, selected scope, scope counts, and zero visible Quick Actions; keep recovery guidance UI-local and out of saved project schema, localStorage, undo history, and analytics; show recovery guidance only when the current query/scope combination shows zero commands; route Clear Search and best-scope switch controls only through existing query and scope handlers; preserve command search token behavior, scope count derivation, filtered result order, Search Result behavior, Scope Filter result behavior, Spotlight Enter behavior, explicit command clicks, post-run result strips, Pinned Commands, Recent Commands, keyboard shortcut guards, and all command run handlers; and avoid mutating project data from recovery controls, macros, auto-run, autoplay, auto-save, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Search Recovery Result work must derive result feedback only after explicit Search Recovery Clear Search or best-scope switch clicks from the previous query/scope, recovered query/scope, scope counts, filtered visible Quick Actions, and the first runnable Enter target; keep recovery result feedback UI-local and out of saved project schema, localStorage, undo history, and analytics; route recovery result controls only through existing query and scope handlers; preserve command search token behavior, scope count derivation, filtered result order, Search Result behavior, Scope Filter result behavior, Search Recovery guidance, Spotlight Enter behavior, explicit command clicks, post-run result strips, Pinned Commands, Recent Commands, keyboard shortcut guards, and all command run handlers; and avoid mutating project data from recovery result feedback, macros, auto-run, autoplay, auto-save, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Spotlight work must derive spotlight text only from filtered visible Quick Actions, current scope, scope counts, search query, and the first runnable action; keep spotlight state UI-local and out of saved project schema; preserve command search matching, scope counts, filtered result order, Enter behavior, explicit command clicks, disabled command behavior, command handlers, result strip derivation, keyboard shortcut guards, Quick Actions open/close behavior, project data, playback, save/load, undo/redo, render/export, and Handoff semantics; and avoid command ranking changes, macros, command chains, auto-run, autoplay, auto-save, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Pinned Commands work must update pinned-command state only after explicit pin or unpin clicks, store only bounded action ids, keep the list, inspected pinned id, and Pinned Command Result feedback UI-local, session-only, and out of saved project schema, localStorage, undo history, and analytics, prune commands that no longer exist, inspect pinned commands only by showing current availability, group, detail, display-only group/target metadata, and UI-local inspect result feedback from current Quick Action definitions without running or mutating project data, show pin/unpin result feedback only for explicit pin or unpin clicks with slot count, command availability, command detail, and next explicit-run check, run pinned commands only after explicit user clicks through current Quick Action definitions and existing command handlers, preserve command search matching, scope counts, filtered result order, Spotlight Enter behavior, Recent Commands, command-run result strips, keyboard shortcut guards, Native Command Menu routing, project data, playback, save/load, render/export, and Handoff semantics, and avoid persistence, command ranking changes, macros, command chains, auto-run, autoplay, auto-save, auto-export, sampling, imported audio, remote AI, accounts, or cloud sync.

Pinned Command inspection compatibility checks must still inspect pinned commands only by showing current availability, group, detail, and display-only group/target metadata from current Quick Action definitions without running or mutating project data; Pinned Command Result feedback adds a UI-local summary after explicit inspect clicks.

Quick Actions Recent Commands work must update recent-command state only after explicit command-palette runs, keep the list, inspected recent id, and Recent Command Result feedback UI-local, session-only, bounded, and out of saved project schema, localStorage, analytics, and undo history, inspect recents only by showing current availability, group, target, last-result metadata, and UI-local inspect result feedback from current Quick Action definitions and recent result state without running or mutating project data, show Recent Command Result feedback only after explicit recent inspect clicks with inspected command status, availability, target, last result, and next explicit-rerun check, rerun recents only after explicit user clicks through current Quick Action definitions and existing command handlers, preserve command search matching, scope counts, filtered result order, Spotlight Enter behavior, command-run result strips, Pinned Commands, keyboard shortcut guards, Native Command Menu routing, project data, playback, save/load, render/export, and Handoff semantics, and avoid persistence, command ranking changes, macros, command chains, auto-run, autoplay, auto-save, auto-export, sampling, imported audio, remote AI, accounts, or cloud sync.

Recent Command inspection compatibility checks must still inspect recents only by showing current availability, group, target, and last-result metadata from current Quick Action definitions and recent result state without running or mutating project data; Recent Command Result feedback adds a UI-local summary after explicit inspect clicks.

Export work must verify a non-silent WAV with expected duration, expected sample rate/channel count, no audio frames above the selected ceiling, and reproducibility from saved project data.

Render noise work must keep offline WAV export, stem export, and export meter analysis deterministic from render-relevant project data, avoid `Math.random()` in the offline render path, and avoid tying audio output to transient UI mode, project title, save timestamps, remote services, imported samples, or hidden audio assets.

Export meter work must label measurements honestly as peak, RMS, peak-minus-RMS dynamics, headroom, and limiter activity unless a standards-complete LUFS or true-peak implementation is added and validated.

Stem export work must render isolated drum, 808, synth, and chord WAV files from the current arrangement, keep full-mix WAV export intact, and avoid depending on imported audio or sample assets.

Stem level meter work must derive drum, 808, synth, and chord meter values from the deterministic offline stem render path, show peak/RMS/headroom honestly without LUFS or true-peak claims, avoid mutating project state, keep existing realtime playback and WAV/stem/MIDI export semantics intact, and avoid imported audio, sampling, plugin hosting, or remote analysis dependencies.

Mix Coach work must derive suggestions and Priority Readout labels only from deterministic full-mix and stem export analysis, including peak-minus-RMS dynamics from existing export analysis and existing Mix Coach checks, remain read-only, keep Priority Readout state UI-local and out of saved project schema and undo history, preserve project state and existing realtime/WAV/stem/MIDI export semantics, communicate peak/RMS/dynamics/headroom/limiter/stem-balance checks without LUFS, true-peak, platform compliance, or mastering-fix claims, and avoid imported audio, sampling, plugin hosting, remote AI, or remote analysis dependencies.

Mix Coach Focus work must derive focused card/readout state, per-card Focus buttons, the Quick Actions Mix Coach focus command, direct Mix Coach check Quick Actions, Focus Result labels, mix metrics, audition cues, and next-check feedback only from existing deterministic Mix Coach checks, the visible Mix Coach check list, the explicitly clicked or command-run check, the current highest-priority Mix Coach check, and UI-local focus state; show Focus Result only after explicit visible or command focus; keep focused card state and Focus Result feedback UI-local and out of saved project schema and undo history; route button clicks and command runs only through the existing Mix Coach focus state and master panel scroll path; avoid changing thresholds, export analysis, stem analysis, mixer/master state, playback, render/export, project schema, or saved files; and avoid auto-fixing, autoplay, LUFS/true-peak claims, platform compliance claims, sampling, remote AI, accounts, analytics, or cloud sync.

Mix Coach Command Reference work must mark the Mix row as `Quick Actions / Readout`, reflect only the existing Mix Coach readout, Priority/Focus state, focus command, and direct check commands, preserve Mix Coach scoring, focus handling, mixer/master state, playback, render/export, and project schema, and avoid changing command execution, mix fixes, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Mix Fix work must run only after explicit user button clicks or Mix Fix Quick Action command runs, derive action tone and Quick Actions result metrics/follow-up only from deterministic local full-mix/stem analysis, current project state, and existing Mix Fix preset ids, route all mixer/master changes through undoable project update paths, keep fixes limited to editable mixer/master state, avoid hidden automatic mastering, and avoid LUFS, true-peak, platform compliance, sampling, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync claims.

Mix Fix Preview work must derive preview labels only from current local project state, deterministic export analysis, deterministic stem analysis, and existing Mix Fix actions; stay UI-local and out of saved project schema; show the suggested explicit fix, scope, issue detail, and pre-click move count before Apply; preserve Mix Coach thresholds, Mix Fix action order, Mix Fix apply behavior, mixer/master semantics, stem analysis, export analysis, save/load, snapshots, undo/redo, playback, WAV/stem/MIDI export, Master Finish, Master Output Role, Quick Actions, Next Move, Handoff Sheet, and Handoff Pack semantics; and avoid automatic fixing, auto-apply, modal confirmations, LUFS, true-peak, platform compliance, sampling, imported audio, remote AI, remote analysis, accounts, analytics, plugin hosting, or cloud sync.

Mix Fix Result work must update result labels only after explicit Headroom, Stem Balance, or Low End Mix Fix clicks or Mix Fix Quick Action command runs; derive applied fix, editable mixer/master scope, before/after export, headroom, stem-balance, low-end, changed-control posture, Quick Actions result metric, audition cue, and next-check text only from local before/after project state, deterministic export analysis, deterministic stem analysis, and existing Mix Fix actions; keep result state UI-local and out of saved project schema and undo history; preserve Mix Fix Preview, Mix Coach thresholds, Mix Fix action order, Mix Fix apply behavior, mixer/master semantics, stem analysis, export analysis, save/load, snapshots, undo/redo, playback, WAV/stem/MIDI export, Master Finish, Master Output Role, Quick Actions, Next Move, Handoff Sheet, and Handoff Pack semantics; and avoid automatic fixing, auto-apply, modal confirmations, autoplay, auto-export, LUFS, true-peak, platform compliance, sampling, imported audio, remote AI, remote analysis, accounts, analytics, plugin hosting, or cloud sync.

Master Finish Pad work must derive master preset, ceiling, master output gain, and any Quick Actions Master Finish Decision/current/direct result metric/follow-up from explicit local demo, vocal, store, and club posture presets plus current local master state; update only editable master state and the master mixer channel volume through undoable project history; preserve musical events, arrangement, sound design, non-master mixer channels, Delivery Target, Beat Readiness, Beat Map, Next Move, Mix Coach, project files, snapshots, realtime playback, WAV/stem/MIDI export, and Handoff Sheet semantics; keep manual master controls editable; and avoid hidden automatic mastering, LUFS, true-peak, platform compliance, sampling, imported audio, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync claims.

Master Finish Preview work must derive preview labels, Preview Decision labels, visible Preview Decision action targets, pre-click finish move counts, any Quick Actions Master Finish Decision/current/direct command targets, and any direct Master Finish pad Quick Actions only from current local master state and existing Master Finish pad options; keep preview/decision state UI-local and out of saved project schema and undo history; show the suggested pad, preset, ceiling, output gain, finish-move count, and explicit decision action before Apply; route Master Finish Pad clicks, the visible Preview Decision action, the Quick Actions Master Finish Decision command, the current-target Quick Actions Master Finish command, and any direct Master Finish pad command only through the existing undoable Master Finish apply path; disable the visible Preview Decision action, Master Finish Decision command, and direct Master Finish pad commands when the pad already matches current master output posture; preserve Master Finish pad definitions, suggested pad selection, apply behavior, master preset semantics, master ceiling limits, master mixer gain semantics, save/load, snapshots, undo/redo, playback, WAV/stem/MIDI export, Mix Coach, Master Output Role, Quick Actions, Next Move, Handoff Sheet, and Handoff Pack semantics; and avoid automatic mastering, auto-apply, modal confirmations, LUFS, true-peak, platform compliance, sampling, imported audio, remote AI, remote analysis, accounts, analytics, plugin hosting, or cloud sync.

Master Finish Decision Command Reference work must mark the Finish row as `Quick Actions / Readout`, reflect only the existing Master Finish Preview Decision Readout, suggested output command, current-target command, and direct finish pad commands, preserve pad definitions, suggested pad selection, master preset, ceiling, output gain, undo/redo, playback, WAV/stem/MIDI export, Handoff Pack, and project schema, and avoid changing command execution, hidden mastering, autoplay, auto-export, LUFS, true-peak, platform compliance, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Master Finish Command Reference work must mark the Finish row as `Quick Actions / Readout`, reflect only the existing Master Finish Preview, Result, current-target command, and direct finish pad commands, preserve pad definitions, suggested pad selection, master preset, ceiling, output gain, undo/redo, playback, WAV/stem/MIDI export, Handoff Pack, and project schema, and avoid changing command execution, hidden mastering, autoplay, auto-export, LUFS, true-peak, platform compliance, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Master Finish Result work must update result labels only after explicit Master Finish Pad clicks, visible Preview Decision action clicks, or Master Finish Decision/current/direct Quick Action command runs, derive applied pad, editable master output scope, before/after preset, ceiling, output gain, changed-finish impact, Quick Actions result metric, audition cue, and next-check text only from local before/after project master state and existing Master Finish pad definitions, keep result state UI-local and out of saved project schema and undo history, preserve Master Finish Preview, Preview Decision Readout, Master Finish pad definitions, suggested pad selection, apply behavior, master preset semantics, master ceiling limits, master mixer gain semantics, save/load, snapshots, undo/redo, playback, WAV/stem/MIDI export, Mix Coach, Master Output Role, Quick Actions, Next Move, Handoff Sheet, and Handoff Pack semantics, and avoid automatic mastering, auto-apply, modal confirmations, autoplay, auto-export, LUFS, true-peak, platform compliance, sampling, imported audio, remote AI, remote analysis, accounts, analytics, plugin hosting, or cloud sync.

Quick Actions Master Finish result metric work must identify only the explicit Master Finish Decision, current Master Finish, or direct Master Finish pad action, decision/current/direct context, target finish pad, current and target master posture, finish move count, selected Pattern, editable drum/808/Synth/chord counts, total editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, stem readiness, and next listen/export/manual-trim check from local command metadata plus current project state; preserve Master Finish pad definitions, preview derivation, decision target derivation, disabled-state rules, apply handlers, master preset/ceiling/output semantics, Master Automation, Mix Snapshot, Mix Balance, Stem Audition, Mix Coach, Space FX, generated Pattern A/B/C events, arrangement data, project schema, playback, render/export, Handoff, privacy boundaries, platform-loudness boundaries, and sampling boundaries; and avoid platform loudness compliance, LUFS/true-peak guarantees, auto-mastering, automatic export, command chains, hidden generation, autoplay, imported audio, remote AI, accounts, analytics, or cloud sync.

Master Automation Pad and Quick Actions work must derive None, Fade In, Fade Out, and Intro/Outro master volume fade events, Preview labels, Preview Decision action targets, the Quick Actions Master Automation Decision command, the current suggested fade command, direct fade commands, and Decision/current/direct result metric/follow-up text only from local arrangement length, current automation posture, and explicit pad definitions; store automation as editable project event data; route pad clicks, the visible Preview Decision action, the Quick Actions Master Automation Decision command, the current-target Quick Actions Master Automation command, and any direct Master Automation fade command only through the same undoable master automation pad handler; disable visible decision, Master Automation Decision command, and current/direct command runs when the current editable automation already matches the target; apply the same master automation gain to realtime playback, full-mix WAV render, and stem export; keep preview, decision, and result feedback UI-local and out of saved project schema; preserve Master Finish, Mix Coach, Export Meter, manual master controls, save/load, snapshots, undo/redo, MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid audio import, sampling, sampler devices, hidden mastering, autoplay, auto-export, command chains, remote AI, accounts, analytics, or cloud sync.

Master Automation Decision Command Reference work must mark the Finish row as `Quick Actions / Readout`, reflect only the existing Master Automation Preview Decision Readout, suggested fade command, current-target command, and direct fade commands, preserve pad derivation, automation event storage, undo/redo, realtime playback, WAV/stem render gain, Handoff Pack, and project schema, and avoid changing command execution, hidden mastering, autoplay, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Master Automation Command Reference work must mark the Finish row as `Quick Actions / Readout`, reflect only the existing Master Automation Preview, Result, current-target command, and direct fade commands, preserve pad derivation, automation event storage, undo/redo, realtime playback, WAV/stem render gain, Handoff Pack, and project schema, and avoid changing command execution, hidden mastering, autoplay, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Master Automation Result work must update result labels only after explicit Master Automation Pad clicks, visible Preview Decision action clicks, or Master Automation Decision/current/direct Quick Action command runs, derive applied preset, before/after automation event count, arrangement range, audition cue, and next-check text only from local before/after project automation state and existing Master Automation pad definitions, preserve realtime playback and WAV/stem export parity, and avoid platform loudness claims, automatic mastering, sampling, imported audio, remote analysis, plugin hosting, accounts, analytics, or cloud sync.

Quick Actions Master Automation result metric work must identify only the explicit Master Automation Decision, current Master Automation, or direct Master Automation pad action, decision/current/direct context, target fade pad, current and target automation posture, automation event delta, selected Pattern, editable drum/808/Synth/chord counts, total editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, stem readiness, master posture, and next listen/export/manual-trim check from local command metadata plus current project state; preserve Master Automation pad definitions, preview derivation, decision target derivation, disabled-state rules, apply handlers, automation event storage, realtime playback gain, WAV/stem render gain, Master Finish, Mix Snapshot, Mix Balance, Stem Audition, Mix Coach, Space FX, generated Pattern A/B/C events, arrangement data, project schema, render/export, Handoff, privacy boundaries, platform-loudness boundaries, and sampling boundaries; and avoid platform loudness compliance, LUFS/true-peak guarantees, auto-mastering, automatic export, command chains, hidden generation, autoplay, imported audio, remote AI, accounts, analytics, or cloud sync.

Master output role readout work must derive only from local master preset, master ceiling, master mixer output gain, and deterministic export analysis; stay UI-local and out of saved project schema; preserve Master Finish Pads, Mix Coach, Export Meter, playback, WAV/stem/MIDI export, and Handoff Sheet semantics; and avoid hidden automatic mastering, LUFS, true-peak, platform compliance, sampling, imported audio, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync claims.

Stem Audition Pad work must derive Full Mix/Drums/808/Synth/Chords audition states and any Quick Actions Stem Audition Decision or direct Stem Audition command targets from explicit local pad definitions and the visible Stem Audition Decision summary, update only existing mixer `solo` and `muted` fields through undoable project history, route pad clicks, Stem Audition Decision Readout action clicks, Quick Actions Stem Audition Decision command runs, and any direct Quick Actions Stem Audition command runs only through the existing undoable Stem Audition pad apply path, keep mixer controls manually editable afterward, preserve musical events, arrangement blocks, volume/pan/EQ/Drive/Glue/Space, sound design, master state, Delivery Target, Beat Readiness, Beat Map, Next Move, Finish Checklist, Review Queue, project files, snapshots, realtime playback, WAV/stem/MIDI export, and Handoff Sheet semantics, and avoid rendered stem playback, stem separation, sampling, imported audio, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync.

Stem Audition Readout work must derive Full Mix, soloed stem, manual custom audition, tone, active-stem, muted, solo, Decision Readout labels, visible Decision Readout action target, and any Quick Actions Stem Audition Decision command target only from local mixer `solo` and `muted` state plus existing Stem Audition pad options; keep the readout, Decision Readout state, and command state UI-local and out of saved project schema; route the visible Decision Readout action and Quick Actions Stem Audition Decision command only through the existing undoable Stem Audition pad apply path; otherwise avoid mutating mixer channels, triggering playback, rendering audio, exporting stems, changing Stem Audition Pads, Mix Balance Pads, Mix Coach, Master Finish, Handoff Pack, or project files; preserve realtime playback, WAV/stem/MIDI export, save/load, snapshots, undo/redo, and Handoff Sheet semantics; and avoid rendered stem playback, stem separation, sampling, imported audio, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync.

Stem Audition Command Reference work must mark Stem Audition as `Quick Actions / Readout` only when it reflects existing Stem Audition Readout, Stem Audition Decision, direct Stem Audition commands, and local stem-audition result feedback; it must not change Stem Audition pad definitions, readout derivation, decision routing, Quick Actions execution, mixer levels, playback scheduling, render/export, project schema, save/load, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Stem Audition Decision Command Reference work must mark the Mix row as `Quick Actions / Readout`, reflect only the existing Stem Audition Decision Readout, next audition command, and direct Stem Audition commands, preserve readout derivation, mixer solo/mute updates, playback, render/export, and project schema, and avoid changing project data beyond explicit existing audition pad commands, command execution, rendered stem playback, stem separation, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Stem Audition result metric work must identify only the explicit Stem Audition Decision or direct Stem Audition action, decision/direct context, target Full Mix or stem, current audition posture, selected Pattern, editable drum/808/Synth/chord counts, total editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, stem readiness, mixer solo/mute posture, and next listening/manual-trim check from local command metadata plus current project state; preserve Stem Audition pad definitions, readout derivation, decision target derivation, disabled-state rules, apply handlers, mixer solo/mute behavior, mixer level/pan/EQ/Drive/Glue/Space algorithms, Mix Balance, Mix Coach, Space FX, Master Finish, generated Pattern A/B/C events, arrangement data, project schema, playback, render/export, Handoff, privacy boundaries, and sampling boundaries; and avoid rendered stem playback, stem separation, auto-mixing, auto-apply, command chains, hidden generation, autoplay, imported audio, remote AI, accounts, analytics, or cloud sync.

Mix Balance Pad work must derive mixer channel volume, pan, low-cut, air, Drive/Glue, Space send changes, and any Quick Actions Mix Balance Decision/current/direct result metric/follow-up from explicit local rough-balance presets, update only editable mixer channel state through undoable project history, route visible pad clicks, visible Preview Decision action clicks, the Quick Actions Mix Balance Decision command, the current-target Quick Actions Mix Balance command, and direct pad command runs only through the existing undoable Mix Balance apply path, disable visible decision, Mix Balance Decision, and direct pad commands when the pad already matches current mixer posture, keep musical events, arrangement, sound design, master preset, Delivery Target, Beat Readiness, Beat Map, Next Move, Mix Coach, project files, snapshots, realtime playback, WAV/stem/MIDI export, and Handoff Sheet semantics intact, keep results manually editable through existing Mixer controls, and avoid hidden mastering, render downloads, sampling, imported audio, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync.

Mix Balance Preview work must derive suggested rough-balance pad labels, direct balance pad command labels, Drums/808/Synth/Chords target posture, audition posture, Preview Decision labels, visible Preview Decision action targets, pre-click mix move counts, any Quick Actions Mix Balance Decision/current/direct command targets, and any direct Mix Balance pad Quick Actions only from current local mixer state and existing Mix Balance Pad definitions; keep preview/decision state UI-local and out of saved project schema and undo history; route Mix Balance Pad clicks, the visible Preview Decision action, the Quick Actions Mix Balance Decision command, the current-target Quick Actions Mix Balance command, and any direct Mix Balance pad command only through the existing undoable Mix Balance apply path; disable the visible Preview Decision action, Mix Balance Decision command, and direct Mix Balance pad commands when the pad already matches current mixer posture; preserve Mix Balance Pad definitions and apply behavior, Mix Balance Result, manual Mixer controls, Stem Audition Pads, Stem Audition Readout, Mix Coach, Master Finish, musical events, arrangement, sound design, master state, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid auto-mixing, auto-apply, modal confirmations, autoplay, render downloads, auto-export, hidden mastering, LUFS, true-peak, platform compliance, sampling, imported audio, remote AI, remote analysis, plugin hosting, accounts, analytics, or cloud sync.

Mix Balance Decision Command Reference work must mark Mix Balance Decision as `Quick Actions / Readout` only when it reflects existing Mix Balance Preview, Preview Decision Readout action, Quick Actions Mix Balance Decision routing, direct Mix Balance pad commands, and local Mix Balance Result feedback; it must not change Mix Balance pad definitions, preview derivation, decision routing, Quick Actions execution, mixer update paths, playback scheduling, render/export, project schema, save/load, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Mix Balance Command Reference work must mark Mix Balance as `Quick Actions / Readout` only when it reflects existing Mix Balance Preview, current-target Mix Balance command routing, direct Mix Balance pad commands, and local Mix Balance Result feedback; it must not change Mix Balance pad definitions, preview derivation, decision routing, Quick Actions execution, mixer update paths, playback scheduling, render/export, project schema, save/load, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Mix Balance result metric work must identify only the explicit Mix Balance Decision, current Mix Balance, or direct Mix Balance pad action, preview/apply context, target balance posture, current editable Drums/808/Synth/Chords channel posture, selected Pattern, editable drum/808/Synth/chord counts, total editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, stem readiness, and next listening/manual-trim check from local command metadata plus current project state; preserve Mix Balance pad definitions, preview derivation, disabled-state rules, apply handlers, mixer volume/pan/mute/solo/EQ/Drive/Glue/Space algorithms, Stem Audition, Mix Coach, Space FX, Master Finish, generated Pattern A/B/C events, arrangement data, project schema, playback, render/export, Handoff, privacy boundaries, and sampling boundaries; and avoid auto-mixing, auto-apply, command chains, hidden generation, autoplay, imported audio, remote AI, accounts, analytics, or cloud sync.

Mix Balance Result work must update result labels only after explicit Mix Balance Pad clicks, visible Preview Decision action clicks, or Mix Balance Decision/current/direct Quick Action command runs, derive applied pad, editable mixer channel scope, before/after Drums, 808, Synth, Chords, and Audition posture, changed-channel/control impact, audition cue, and next-check text only from local before/after mixer state and existing Mix Balance Pad definitions, keep result state UI-local and out of saved project schema and undo history, preserve Mix Balance Pad definitions, apply behavior, manual Mixer controls, Stem Audition Pads, Stem Audition Readout, Mix Coach, Master Finish, musical events, arrangement, sound design, master state, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics, and avoid hidden mixing, automatic mastering, modal confirmations, autoplay, render downloads, auto-export, sampling, imported audio, remote AI, remote analysis, plugin hosting, accounts, analytics, or cloud sync.

Mix Snapshot A/B work must derive captured pass summaries, recall payloads, decision readout labels, visible Decision Readout action targets, any Quick Actions Mix Snapshot Decision or capture/recall/clear command targets, and any slot-specific Quick Actions result metrics only from the current local project mixer/master state, deterministic export analysis, deterministic stem analysis, explicit command id, and UI-local slot state; route capture and clear panel buttons, visible Decision Readout capture action clicks, Quick Actions Mix Snapshot Decision capture runs, and Quick Actions capture/clear commands only through the existing Mix Snapshot capture/clear handlers; route recall panel buttons, visible Decision Readout recall action clicks, Quick Actions Mix Snapshot Decision recall runs, and Quick Actions recall commands only through the existing undoable project update path that replaces mixer channels, master preset, and master ceiling from the selected slot; keep A/B slot state and decision readout state UI-local and out of saved project schema; compare headroom, stem balance, master output, and audible stem posture without auto-applying changes; preserve Mix Balance Pads, Stem Audition Pads, Stem Audition Readout, Mix Coach, Mix Fix, Master Finish, Project Snapshots, save/load, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid automatic recall, automatic capture, reference audio, rendered reference imports, audio uploads, auto-mixing, auto-mastering, autoplay, render downloads, auto-export, LUFS/true-peak/platform-compliance claims, sampling, imported audio, remote AI, remote analysis, plugin hosting, accounts, analytics, or cloud sync.

Mix Snapshot A/B Decision Command Reference work must mark the Mix row as `Quick Actions / Readout`, reflect only the existing Mix Snapshot Decision Readout, capture/recall/listen-next guidance, A/B slot command targets, and slot-specific result metrics, preserve slot derivation, capture/clear handlers, undoable mixer/master recall paths, playback, render/export, project schema, and A/B slot UI-local storage, and avoid changing command execution, automatic recall, automatic capture, auto-mixing, auto-mastering, autoplay, render downloads, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Mix Snapshot A/B Command Reference work must mark the Mix row as `Quick Actions / Readout`, reflect only the existing Mix Snapshot A/B comparison readout, A/B slot state, capture/recall/clear commands, decision readout, and slot-specific result metrics, preserve slot derivation, capture/clear handlers, undoable mixer/master recall paths, playback, render/export, project schema, and A/B slot UI-local storage, and avoid changing command execution, automatic recall, automatic capture, auto-mixing, auto-mastering, autoplay, render downloads, auto-export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Mix Snapshot result metric work must identify only the explicit Mix Snapshot Decision, capture, recall, or clear action, decision/direct context, target A/B slot or clear action, command slot state, current mix/export posture, selected Pattern, editable drum/808/Synth/chord counts, total editable event count, Pattern A/B/C usage, arrangement block count, song length, stem readiness, master posture, and next listen/capture/recall check from local command metadata plus current project state; preserve Mix Snapshot slot derivation, capture/clear handlers, decision target derivation, undoable mixer/master recall paths, mixer algorithms, Stem Audition, Mix Balance, Mix Coach, Space FX, Master Finish, musical events, arrangement data, project schema, playback, render/export, Handoff, privacy boundaries, and sampling boundaries; and avoid rendered A/B playback, automatic capture, automatic recall, auto-mixing, auto-mastering, command chains, hidden generation, autoplay, imported audio, remote AI, accounts, analytics, or cloud sync.

MIDI export work must write deterministic Standard MIDI Files from editable project events, follow arrangement Pattern A/B/C assignments, block lengths, track mutes, BPM, drum repeats, note/chord lengths, and chord inversions, include drum, 808, synth, and chord tracks, and avoid depending on rendered audio, imported samples, or sampler assets.

Handoff Sheet export work must write a local plain text summary only after an explicit user click, derive content from local project state, selected Delivery Target, Session Brief, arrangement data, deterministic export analysis, and deterministic stem analysis, avoid mutating project state or triggering audio render downloads, preserve realtime playback plus WAV/stem/MIDI export semantics, and avoid media uploads, copyrighted reference audio, platform compliance, publishing, licensing, LUFS/true-peak guarantees, sampling, imported audio, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync.

Handoff Pack work must group WAV, stem, MIDI, and Handoff Sheet deliverables as explicit user-clicked actions routed through existing export handlers, derive status only from local project state, selected Delivery Target, deterministic export analysis, deterministic stem analysis, and Session Brief fields, avoid auto-export/background render/downloads, preserve existing realtime playback plus WAV/stem/MIDI/Handoff Sheet semantics, and avoid media uploads, platform compliance, publishing, licensing, LUFS/true-peak guarantees, sampling, imported audio, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync.

Handoff Pack Command Reference work must mark the Finish row as `Quick Actions / Readout`, reflect only the existing Handoff Pack deliverable status, route, send-order, receipt, file-manifest, manifest-audit, export-format, and package-check readouts plus explicit export commands, preserve Handoff Pack derivation, export handlers, file names, file contents, receipts, playback, render/export, and project schema, and avoid changing command execution, creating ZIP archives, batch exports, native folder writes, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Handoff Pack route readout work must derive only from local project state, selected Delivery Target, deterministic export analysis, deterministic stem analysis, Session Brief fields, and existing Handoff Pack item status; stay UI-local and out of saved project schema; avoid changing export file contents, file names, download handlers, playback, render, WAV/stem/MIDI export, or Handoff Sheet semantics; and avoid auto-export, background rendering, media upload, platform compliance, publishing, licensing, LUFS/true-peak guarantees, sampling, imported audio, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync.

Handoff Pack file manifest work must derive expected filenames only from existing WAV, stem, MIDI, and Handoff Sheet filename helpers plus local Handoff Pack item status; stay UI-local and out of saved project schema; avoid changing exported file contents, download handlers, render, MIDI, Handoff Sheet contents, playback, save/load, snapshots, or export click semantics; and avoid auto-export, zip packaging, background rendering, media upload, platform compliance, publishing, licensing, sampling, imported audio, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync.

Handoff Pack Manifest Audit work must derive planned-file, readiness, latest-receipt, next-step labels, any Quick Actions Handoff Manifest Audit focus command, and Focus Result feedback only from existing Handoff Pack item statuses, file manifest entries, selected Delivery Target, Send Order, and latest explicit Handoff Export Receipt; the Quick Actions Handoff Manifest Audit focus command must route only to the existing Deliver/Handoff Pack surface without exporting files; stay UI-local and out of saved project schema and undo history; preserve Handoff Pack buttons, Send Order, route readout, file manifest, receipt state, file names, file contents, render/download handlers, MIDI bytes, Handoff Sheet contents, playback, save/load, snapshots, and export click semantics; and avoid auto-export, retries, zip packaging, background rendering, media upload, platform compliance, publishing, licensing, sampling, imported audio, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync.

Quick Actions Handoff Manifest Audit result metric work must identify only the explicit planned-file audit action, Deliver destination, manifest status/context, selected Pattern, editable event count, Pattern A/B/C usage, WAV/stem/MIDI/Handoff Sheet planned-file posture, latest export receipt, next handoff step, arrangement block count, and song length from current Handoff Manifest Audit command title/detail plus local project state; preserve manifest derivation, focus routing, receipt behavior, export handlers, file contents, render/download behavior, remote behavior, project schema, privacy boundaries, and sampling boundaries.

Handoff Manifest Audit Command Reference work must mark the Deliver row as `Quick Actions / Readout`, reflect only the existing planned WAV/stem/MIDI/Handoff Sheet file readiness, latest receipt context, next missing delivery step, Handoff Manifest Audit focus command, and Handoff Pack follow-up, preserve Handoff Pack item statuses, file manifest derivation, Manifest Audit readiness derivation, receipt derivation, focus routing, Send Order, package checks, export handlers, file names, file contents, render bytes, MIDI bytes, Handoff Sheet contents, playback, save/load, snapshots, project schema, privacy boundaries, and sampling boundaries, and avoid changing command execution, auto-export, batch export, retries, zip packaging, native folder writing, background rendering, uploads, platform compliance, publishing/licensing claims, sampling, imported audio, sampler devices, remote AI, accounts, analytics, or cloud sync.

Handoff Package Check work must derive file-set, export-order, latest-receipt, session-context cards, Priority Readout labels, visible priority action targets, Focus Result labels, package metrics, audition cues, and next-check feedback only from existing Handoff Pack item status, file manifest entries, Send Order, latest explicit Handoff Export Receipt, selected Delivery Target, Session Brief, deterministic export analysis, deterministic stem analysis, the visible summary, existing Handoff Package Check cards/focus labels, the current highest-priority package card, and the explicitly clicked or command-run card; stay UI-local and out of saved project schema and undo history; keep Priority Readout state and visible priority action state UI-local and out of saved project schema and undo history; show Focus Result only after explicit visible or command focus; route visible Priority action clicks, visible Focus controls, and Quick Actions Handoff Package focus/card commands only to the existing Deliver panel through the existing focus handler; preserve Handoff Pack buttons, Send Order, route readout, file manifest, Manifest Audit, receipt state, file names, file contents, render/download handlers, MIDI bytes, Handoff Sheet contents, playback, save/load, snapshots, and export click semantics; and avoid ZIP/archive creation, native folder writing, batch export, auto-export, retries, background rendering, media upload, platform compliance, publishing, licensing, sampling, imported audio, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync.

Quick Actions Handoff Package Check result metric work must identify only the explicit package focus action, priority or direct package lane, Deliver destination, package status/context, selected Delivery Target, WAV filename/export/headroom posture, stem filename/count against target stem goal, MIDI filename/song length posture, Handoff Sheet filename, Session Brief context, latest export receipt and next receipt step, package ready/review/blocker counts, selected Pattern, editable event count, Pattern A/B/C usage, arrangement block count, song length, and next Handoff Package check from current Handoff Package Check command title/detail plus local project, export, stem, receipt, and brief state; preserve package card derivation, priority selection, focus routing, direct export command ids, export handlers, render bytes, MIDI bytes, Handoff Sheet contents, file names, download behavior, Handoff Export Receipt state derivation, Handoff Pack, Handoff Send Order, Handoff Manifest Audit, Handoff Next Export, project schema, privacy boundaries, and sampling boundaries.

Handoff Package Check Command Reference work must mark the Deliver row as `Quick Actions / Readout`, reflect only the existing Handoff Package Check readout, Priority/Focus state, focus command, and direct package card commands, preserve package-check derivation, export order, latest receipt, session context, files, export handlers, and project schema, and avoid changing command execution, creating ZIP archives, batch exports, native folder writes, playback, render/export, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Handoff Export Format Readout and Focus work must derive WAV format, duration, full-mix filename, stem file count, audible stem count, MIDI scope, Handoff Sheet context, Priority Readout labels, visible priority action targets, focus controls, the Quick Actions Handoff Export Format focus command, direct Export Format metric commands, Focus Result labels, export-format metrics, audition cues, and next-check feedback only from deterministic local export analysis, stem analysis, arrangement length, existing filename helpers, Delivery Target, Session Brief, visible Export Format metrics, existing Export Format metric/focus labels, the explicitly clicked or command-run metric, and the current highest-priority Export Format metric; keep the readout, Priority Readout state, visible priority action state, focus state, and Focus Result feedback UI-local and out of project schema and undo history; show Focus Result only after explicit visible or command focus; route visible Priority action clicks, button clicks, and command runs only to the existing Deliver/Handoff Pack surface through the existing focus handler without exporting files; preserve all export handlers, file contents, filenames, render bytes, MIDI bytes, Handoff Sheet text, Handoff Pack receipt behavior, save/load, playback, and sampling boundaries; and avoid configurable render settings, dither, normalization, batch export, zip packaging, upload, cloud sync, remote AI, background rendering, or platform compliance claims.

Quick Actions Handoff Export Format result metric work must identify only the explicit deliverable-format action, current priority or direct export-format metric, destination panel, metric status/context, selected Pattern, editable event count, Pattern A/B/C usage, WAV/stem/MIDI/Handoff Sheet posture, arrangement block count, and song length from current Handoff Export Format command title/detail plus local project state; preserve export-format derivation, focus routing, export handlers, file contents, render/download behavior, remote behavior, project schema, privacy boundaries, and sampling boundaries.

Export Format Readout Command Reference work must mark the Deliver row as `Quick Actions / Readout`, reflect only the existing WAV sample-rate/channel format, duration, full-mix filename, stem count and audible-stem count, MIDI scope, Handoff Sheet context, Export Format focus command, direct metric commands, and Handoff Pack follow-up, preserve export-format metric derivation, focus routing, deterministic export analysis, deterministic stem analysis, arrangement length analysis, filename helpers, Delivery Target, Session Brief use, Handoff Pack status, receipt behavior, export handlers, file names, file contents, render bytes, MIDI bytes, Handoff Sheet text, playback, save/load, snapshots, project schema, privacy boundaries, and sampling boundaries, and avoid changing command execution, configurable render settings, dither, normalization, auto-export, batch export, retries, zip packaging, native folder writing, background rendering, uploads, platform compliance, publishing/licensing claims, sampling, imported audio, sampler devices, remote AI, accounts, analytics, or cloud sync.

Handoff Pack Send Order work must derive next deliverable, sequence labels, any Quick Actions Handoff Send Order focus command, and any Quick Actions next handoff export target only from existing Handoff Pack item ids and statuses, the existing send-order package card, and local Delivery Target context; the Quick Actions Handoff Send Order focus command must route only to the existing Deliver/Handoff Pack surface without exporting files; the Quick Actions next handoff export command must run only the current next item's existing export handler after an explicit command click and must not chain multiple deliverables; stay UI-local and out of saved project schema; avoid changing Handoff Pack scoring, route readout, file manifest, export file contents, file names, download handlers, render, MIDI, Handoff Sheet contents, playback, save/load, snapshots, or export click semantics; and avoid auto-export, zip packaging, background rendering, media upload, platform compliance, publishing, licensing, sampling, imported audio, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync.

Quick Actions Handoff Send Order result metric work must identify only the explicit send-order focus action, Deliver destination, current next deliverable, send-order status/context, selected Pattern, editable event count, Pattern A/B/C usage, WAV/stem/MIDI/Handoff Sheet sequence posture, package readiness, latest export receipt, arrangement block count, and song length from current Handoff Send Order command title/detail plus local project state; preserve send-order derivation, next-export routing, receipt behavior, export handlers, file contents, render/download behavior, remote behavior, project schema, privacy boundaries, and sampling boundaries.

Handoff Send Order Command Reference work must mark the Deliver row as `Quick Actions / Readout`, reflect only the existing WAV -> stems -> MIDI -> Handoff Sheet delivery sequence, current next deliverable, Handoff Send Order focus command, Handoff Next Export target, and Handoff Pack follow-up, preserve Handoff Pack item statuses, send-order derivation, next-export command routing, export handlers, file names, file contents, render bytes, MIDI bytes, Handoff Sheet contents, receipt state, route readout, file manifest, playback, save/load, snapshots, project schema, privacy boundaries, and sampling boundaries, and avoid changing command execution, auto-export, batch export, retries, zip packaging, native folder writing, background rendering, uploads, publishing/licensing claims, sampling, imported audio, sampler devices, remote AI, accounts, analytics, or cloud sync.

Handoff Next Export Command Reference work must mark the Deliver row as `Quick Actions / Readout`, reflect only the existing Handoff Pack Send Order next deliverable, current next-export command, and explicit export handler routing, preserve Handoff Pack item statuses, send order, latest receipt, export file contents, file names, download handlers, render, MIDI, Handoff Sheet contents, playback, project schema, and export click semantics, and avoid changing command execution, auto-export, batch export, zip packaging, background rendering, media upload, platform compliance, sampling, imported audio, remote AI, accounts, analytics, or cloud sync.

Quick Actions Handoff Next Export result metric work must identify only the explicit next-export action, Deliver destination, current next deliverable, exported or receipt deliverable file, selected Delivery Target, target length and stem goal, WAV filename/export/headroom posture, stem filename/count against target stem goal, MIDI filename/song length posture, Handoff Sheet filename and Session Brief context, send-order status/sequence, selected Pattern, editable event count, Pattern A/B/C usage, arrangement block count, song length, latest UI-local receipt, package ready/review/blocker counts, package readiness, and next handoff step from current Handoff Next Export command title/detail plus local project, export, stem, receipt, and brief state; preserve next-export selection, single-deliverable routing, direct export command ids, export handlers, render bytes, MIDI bytes, Handoff Sheet contents, file names, download behavior, Handoff Export Receipt state derivation, Handoff Pack, Handoff Send Order, Handoff Manifest Audit, Handoff Package Check, project schema, privacy boundaries, and sampling boundaries.

Direct Exports Command Reference work must mark the Deliver row as `Quick Actions / Readout`, reflect only the existing explicit WAV, stems, MIDI, and Handoff Sheet export commands plus deliverable-specific local result metrics and follow-up checks; preserve direct export command ids, export handlers, render bytes, MIDI bytes, Handoff Sheet contents, file names, download behavior, Handoff Export Receipt behavior, Export Preflight, Handoff Pack, Handoff Send Order, Handoff Manifest Audit, Handoff Package Check, Handoff Next Export, direct export result metric derivation, project schema, save/load, undo/redo, playback, arrangement, mixer, master, snapshots, local drafts, privacy boundaries, and sampling boundaries; and avoid batch export, auto-export, retries, ZIP/archive creation, native folder writing, background rendering, uploads, platform compliance, publishing/licensing flows, sampling, imported audio, sampler devices, remote AI, accounts, analytics, or cloud sync.

Quick Actions Direct Exports result metric work must identify only the explicit WAV/stems/MIDI/Handoff Sheet export action, Deliver destination, exported deliverable/file, selected Delivery Target, target length and stem goal, WAV filename/export/headroom posture, stem filename/count against target stem goal, MIDI filename/song length posture, Handoff Sheet filename and Session Brief context, selected Pattern, editable event count, Pattern A/B/C usage, arrangement block count, song length, latest UI-local receipt, package ready/review/blocker counts, package readiness, send-order next step, and next handoff check from current direct export command title plus local project, export, stem, receipt, and brief state; preserve direct export command ids, export handlers, render bytes, MIDI bytes, Handoff Sheet contents, file names, download behavior, Handoff Export Receipt state derivation, Handoff Pack, Handoff Send Order, Handoff Manifest Audit, Handoff Package Check, Handoff Next Export, project schema, privacy boundaries, and sampling boundaries.

Handoff Export Receipt work must update receipt state only from explicit WAV, stem, MIDI, or Handoff Sheet export handlers; derive any Quick Actions Handoff Export Receipt focus command only from the existing latest receipt card and route it only to the existing Deliver/Handoff Pack surface without exporting files; keep receipt state, receipt focus, and receipt focus result feedback UI-local and out of saved project schema; preserve existing Handoff Pack buttons, Quick Actions, project status strings, file names, file contents, render/download handlers, MIDI bytes, Handoff Sheet contents, Handoff Pack scoring, Send Order, route readout, file manifest, playback, save/load, snapshots, and export click semantics; and avoid auto-export, retries, zip packaging, background rendering, media upload, platform compliance, publishing, licensing, sampling, imported audio, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync.

Quick Actions Handoff Export Receipt result metric work must identify only the explicit receipt focus action, Deliver destination, latest receipt status/context, latest deliverable/file, selected Pattern, editable event count, Pattern A/B/C usage, package readiness, next handoff step, arrangement block count, and song length from current Handoff Export Receipt command title/detail plus local project state; preserve receipt derivation, focus routing, direct export handlers, file contents, render/download behavior, remote behavior, project schema, privacy boundaries, and sampling boundaries.

Handoff Export Receipt Command Reference work must mark the Deliver row as `Quick Actions / Readout`, reflect only the existing latest explicit WAV/stem/MIDI/Handoff Sheet receipt, Handoff Export Receipt focus command, direct export result metrics, and Handoff Pack follow-up; preserve receipt derivation, receipt focus routing, direct export command ids, export handlers, file names, file contents, render bytes, MIDI bytes, Handoff Sheet contents, Handoff Pack scoring, Send Order, route readout, file manifest, playback, save/load, snapshots, project schema, privacy boundaries, and sampling boundaries; and avoid auto-export, retries, zip packaging, batch export, native folder writing, background rendering, uploads, publishing/licensing claims, sampling, imported audio, sampler devices, remote AI, accounts, analytics, or cloud sync.

Product framing work must preserve the core boundary: GrooveForge is for all-genre beat creation, pattern programming, sound design, arrangement, mixing/mastering, and export first; sampling is an optional extension and should not dominate MVP docs, UI copy, architecture, roadmap order, or plan titles.

Product framing work must lead with a positive beat-workstation definition: users directly create drums, 808/bass, melody/chords, sound design, arrangement, mix/master, and export. Sampling guardrails belong in boundary or optional-phase sections, not as the first product summary.

Concept correction work must make the all-genre beat-production mini DAW framing explicit: direct beat composition, sound design, arrangement, mixing/mastering, and export are the product spine, while sample import, chopping, pitch/stretch, one-shot mapping, and sampler workflows are optional later paths.

Concept correction work must preserve this center list in durable docs: Pattern Programming, Drum Sequencing, 808/Bass Synthesis, Melody/Chord Composition, Sound Design, Arrangement, Mixing, Mastering, and Export.

Concept correction work must preserve the user's intent that GrooveForge is for "비트(모든 장르)를 만드는" work, not a trap-only, sample-pack, or sampler-first product.

Concept correction work must treat built-in drum kits, internal one-shots, and any future sample 808 source as sound-source details inside direct drum sequencing or the 808/Bass engine. These terms must not move the product framing toward sample-pack browsing, chopping, audio-clip arrangement, or sampler setup.

Brief alignment work fails if the project can be summarized as a sampling app, sample-chopping tool, sample-pack workflow, or sampler setup surface. The expected summary is an all-genre beat-production mini DAW where sampling is an accessory module.

Current concept-audit work must record the attached Korean correction as a direct-composition brief: the accepted MVP path is BPM/key/style, drums, 808/bass, melody/chords, sound design, arrangement, mixer/master, and export. Any sample import, chop/slice, loop stretch, sample 808, `AudioClipEvent`, `AudioClip`, `audio`, `sampler`, sampler device, sample browser, or waveform example in that brief is optional extension material until a user-approved sampling-phase plan exists.

First-run UX, default navigation, and roadmap work must lead with direct beat composition: pattern programming, drums, 808/bass, melody/chords, sound design, arrangement, mixer/master, and export. Sample browsing, chopping, sampler setup, audio clips, and audio warping must stay opt-in optional-sampling paths unless the user explicitly changes the product direction.

Sampling placement work fails if the default empty project, first visible action, MVP proof, primary navigation, onboarding language, priority table, or plan title makes sample browsing, sample import, chopping, sampler setup, or audio clips appear before editable musical events and beat-making controls.

Sampling placement work also fails if product drafts, README sections, architecture diagrams, or roadmap summaries present an optional sampling sequence as a co-equal product spine beside the direct beat-composition sequence. Sampling references must stay structurally subordinate to the all-genre beat-making core unless the user explicitly starts an optional-sampling phase.

Docs that mention clips before optional sampling is implemented must clarify that core clips are pattern, MIDI, or automation containers, not imported audio assets.

Any new plan that mentions sample import, chopping, sampler tracks, or audio warping must explicitly mark that work as optional sampling-phase work or document the user-approved exception in its Decision Log.

Any product, architecture, UI, roadmap, or QA wording that implies users must import, chop, browse, or map samples before making a complete beat fails the product boundary. The expected proof remains a sample-free beat made from built-in drums, synth 808/bass, melody/chords, arrangement, mixer/master, and export.

Any product or architecture wording that promotes `AudioClipEvent`, sampler assets, sample browsing, or chopping above editable musical events fails the product boundary until optional sampling is explicitly in scope.

Any future optional-sampling schema must keep `NoteEvent`, `DrumHitEvent`, and automation sufficient for a complete beat, and must keep `AudioClipEvent` additive, opt-in, and outside the core MVP proof.

Data model examples fail the product boundary if they add `AudioClipEvent` to the core `MusicalEvent` union or include `audio`/`sampler` in the MVP track union or default project track list before an explicit optional-sampling plan exists.

External-brief correction work must rewrite any `type MusicalEvent = ... | AudioClipEvent` or `type TrackType = ... | "audio" | "sampler"` example into a core MVP union plus a clearly labeled optional sampling extension. The core MVP union remains `NoteEvent`, `DrumHitEvent`, and `AutomationEvent`; the core MVP track family remains `drum_rack`, `bass_808`, `synth`, `chord`, `fx_return`, and `master`.

Concept-audit work must also catch sampling drift introduced through technical examples: waveform UI, audio clip schemas, sampler tracks, sample asset graphs, and `AudioClipEvent` examples belong in an optional sampling section until the user explicitly starts that phase.

Default instrument panel, device-palette, and architecture-diagram work fails if it lists `sampler`, sample browser, chop pads, or audio clips beside the MVP built-in drum rack, synth 808/bass, simple synth, chord synth, FX, mixer, and master devices. Those entries must move to an explicitly optional sampling section unless the user starts sampling-phase work.

Five-area starter UI drafts may use Transport, Pattern Editor, Instrument Panel, Arrangement View, and Mixer/Master, but they fail if the default Instrument Panel includes `sampler`; the first-run panel must lead with built-in drum kit/rack, synth 808/bass, simple synth, chord synth, effects, and sound-design controls.

Brief alignment work must rewrite external examples that include `AudioClipEvent` in the core `MusicalEvent` union so the MVP union remains `NoteEvent`, `DrumHitEvent`, and `AutomationEvent`, with `AudioClipEvent` documented only as a future optional extension.

808/Bass engine work must keep synth 808 as the default MVP source and treat sample 808 support only as a later optional source mode unless the user explicitly approves optional-sampling work.

808/Bass velocity work must treat velocity as first-class BassNote event data: old projects must migrate missing 808 velocity to a safe default, new manual grid notes, Bassline Pads, Pattern Stack notes, Desktop Keyboard Capture, Web MIDI Note On capture, copy/paste, duplicate, selected-note edits, editor audition, realtime playback, WAV/stem export, and arrangement MIDI export must preserve or apply BassNote velocity without adding sampling, recording, remote AI, cloud sync, hidden automation, or sampler scope.

Note grid velocity meter work must derive visible 808/Synth note-cell velocity bars and labels only from existing `NoteView.velocity`, keep note length fill, chance badges, glide markers, playhead, selected-note state, click-to-toggle behavior, and editor audition unchanged, stay UI-only with no project schema, save/load, playback, render, MIDI export, Handoff, sampling, recording, remote AI, or cloud scope changes, and keep text contained inside compact note cells.

Mixer/master work must keep mixing and mastering separate. Volume, pan, mute, solo, master output gain, and preset ceiling controls must affect both realtime playback and WAV export when implemented. Mixer channel role readouts must derive only from local mixer channel volume, pan, mute, solo, low-cut, air, Drive/Glue, and Space send fields, stay UI-local and out of saved project schema, avoid changing Mix Coach, Mix Balance Pads, Stem Audition Pads, Master Finish, playback, or export semantics, and avoid hidden mixing, mastering, LUFS/true-peak, platform-safety claims, sampling, imported audio, remote AI, accounts, analytics, or cloud sync. Loudness presets are targets and checks, not automatic proof that a beat is platform-safe.

Channel EQ work must keep low-cut/air controls in mixer state, migrate older project files safely, keep non-master track EQ separate from master processing, and make EQ values affect realtime playback, full-mix WAV export, and stem export.

Channel dynamics work must keep Drive/Glue controls in mixer state, migrate older project files safely, keep non-master track saturation/compression separate from master processing, label simplified compression honestly, and make Drive/Glue values affect realtime playback, full-mix WAV export, and stem export.

FX send work must keep Space/send controls in mixer state, migrate older project files safely, use deterministic built-in processing rather than imported audio or plugin hosting, make send amount audible in realtime playback and full-mix WAV export, prevent muted, solo-excluded, or arrangement-muted tracks from leaking into the FX return, and keep stem export isolated to the requested stem's own send return.

Space FX Pad work must derive dry, room, wide, and wash send postures, Preview labels, Preview Decision action targets, Quick Actions Space FX Decision and current/direct command targets only from explicit local pad definitions and current mixer send values; update only editable mixer `send` values for Drums, 808, Synth, and Chords through undoable project history; route pad clicks, the visible Preview Decision action, Quick Actions Space FX Decision command runs, and any Quick Actions Space FX command runs only through the existing undoable Space FX pad apply path; disable visible decision, Space FX Decision, and direct command runs when the current editable sends already match the target; keep preview, decision, and result state UI-local and out of saved project schema and undo history; preserve manual Space sliders, mixer volume/pan/mute/solo/EQ/Drive/Glue controls, Mix Balance Pads, Stem Audition Pads, Mix Coach, Master Finish, project files, snapshots, realtime playback, WAV/stem/MIDI export, Handoff Sheet, and Handoff Pack semantics; and avoid new audio assets, imported audio, sampling, plugin hosting, remote AI, remote analysis, autoplay, auto-export, hidden mastering, accounts, analytics, or cloud sync.

Space FX Decision Command Reference work must mark the Sound row as `Quick Actions / Readout`, reflect only the existing Space FX Preview Decision Readout, suggested dry/room/wide/wash send posture, current/direct Space FX commands, result metrics, and follow-up labels, preserve pad derivation, disabled-state rules, undoable mixer send update paths, playback, render/export, project schema, and preview/result UI-local storage, and avoid changing command execution, automatic apply, autoplay, auto-mixing, auto-mastering, audio analysis, imported audio, sampling, sample browsing, sampler tracks, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync.

Space FX Command Reference work must mark the Sound row as `Quick Actions / Readout`, reflect only the existing Space FX preview/readout, dry/room/wide/wash pad posture, current/direct Space FX commands, send-change preview, result metrics, and follow-up labels, preserve pad derivation, disabled-state rules, undoable mixer send update paths, playback, render/export, project schema, and preview/result UI-local storage, and avoid changing command execution, automatic apply, autoplay, auto-mixing, auto-mastering, audio analysis, imported audio, sampling, sample browsing, sampler tracks, plugin hosting, remote AI, remote analysis, accounts, analytics, or cloud sync.

Quick Actions Space FX result metric work must identify only the explicit Space FX Decision, current Space FX, or direct Space FX pad action, preview/apply context, target dry/room/wide/wash send posture, current editable Drums/808/Synth/Chords send posture, selected Pattern, editable drum/808/Synth/chord counts, total editable event count, Pattern A/B/C usage, arrangement block count, song length, export readiness, and next listening/manual-trim check from local command metadata plus current project state; preserve Space FX pad definitions, preview derivation, disabled-state rules, apply handlers, mixer send algorithms, mixer volume/pan/mute/solo/EQ/Drive/Glue controls, Mix Balance, Stem Audition, Mix Coach, Master Finish, generated Pattern A/B/C events, arrangement data, project schema, playback, render/export, Handoff, privacy boundaries, and sampling boundaries; and avoid auto-apply, command chains, hidden generation, autoplay, imported audio, remote AI, accounts, analytics, or cloud sync.
