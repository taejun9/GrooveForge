
# Harness Architecture

The harness exists to make the repository readable and enforceable for coding agents.

## Principles

- Plans are first-class artifacts.
- `AGENTS.md` is a map, not a full manual.
- Durable knowledge lives in `docs/`.
- Validation is encoded as scripts when practical.
- Feature work does not happen directly on `main`.
- Work happens on task branches and worktrees for git repositories.
- QA and review are separate loops.

## Bootstrap Note

This repository was initialized from an unborn `main` branch with no existing project files. The base scaffold is the initial repository foundation. Future implementation tasks should use the worktree lifecycle below.

## Worktree Lifecycle

```text
main
  -> git fetch
  -> create codex/plan-NNN-task branch
  -> create .worktree/plan-NNN-task checkout
  -> create active exec plan
  -> implement
  -> run QA
  -> run review
  -> complete plan and review mirror
  -> merge to main
  -> push main
  -> delete merged branch with git branch -d
  -> remove worktree
```

## Agent Readability

Agents should be able to answer these questions from repo files alone:

- What is the product?
- What work is active?
- What has been completed?
- How do I validate a change?
- What is prohibited?
- Where do meeting notes and reviews go?

If an answer only exists in chat, move it into an exec plan, meeting note, review, or product/quality document.

Distribution env checks treat `GROOVEFORGE_DISTRIBUTION_ENV_FILE` as an override, not an extra input. When it is set, redacted loader, release doctor, prepare-env, channel live-check, update-feed live-check, and next-actions evidence should inspect that configured file as the single active local env source so default ignored `.env.distribution.local` placeholders do not contaminate operator receipts.

## Current Harness Commands

```sh
python3 harness/scripts/run_qa.py
python3 harness/scripts/run_quality_gate.py
npm run renderer:smoke
npm run workflow:smoke
npm run persona:smoke
npm run harness:smoke
npm run desktop:local-delivery-package-smoke
npm run desktop:local-package-reopen-smoke
npm run desktop:local-delivery-zip-smoke
npm run delivery:bundle-zip-smoke
npm run typecheck
npm run build
npm run desktop:smoke
npm run desktop:crash-report-regression-smoke
npm run desktop:launch-smoke
npm run desktop:project-io-smoke
npm run desktop:package-smoke
npm run desktop:packaged-project-io-smoke
npm run desktop:adhoc-sign-smoke
npm run desktop:hardened-runtime-readiness-smoke
npm run desktop:dmg-smoke
npm run desktop:pkg-smoke
npm run desktop:pkg-payload-smoke
npm run desktop:pkg-payload-project-io-smoke
npm run desktop:install-smoke
npm run desktop:installed-project-io-smoke
npm run desktop:gatekeeper-readiness-smoke
npm run desktop:release-manifest-smoke
npm run desktop:release-notes-smoke
npm run desktop:support-artifact-smoke
npm run desktop:update-feed-config-smoke
npm run desktop:update-metadata-policy-smoke
npm run desktop:update-metadata-artifacts-smoke
npm run desktop:auto-update-readiness-smoke
npm run desktop:developer-id-readiness-smoke
npm run desktop:developer-id-signing-smoke
npm run desktop:notarization-smoke
npm run desktop:notarized-gatekeeper-smoke
npm run desktop:distribution-manual-qa-smoke
npm run desktop:distribution-channel-qa-smoke
npm run desktop:distribution-handoff-smoke
npm run desktop:distribution-bundle-manifest-smoke
npm run desktop:distribution-env-template-smoke
npm run release:prepare-env-smoke
npm run release:prepare-env-write-smoke
npm run release:channel-private-input-template-smoke
npm run release:channel-setup-brief-smoke
npm run release:channel-placeholder-input-receipt-smoke
npm run desktop:distribution-private-inputs-smoke
npm run release:channel-unblock-smoke
npm run release:channel-private-input-template
npm run release:channel-setup-brief
npm run release:channel-placeholder-input-receipt
npm run release:channel-apply-private-env-preflight
npm run release:channel-apply-private-env-preflight-smoke
npm run release:channel-apply-private-env-preflight-blocked-smoke
npm run release:channel-apply-private-env-remediation-smoke
npm run release:channel-apply-private-env
npm run release:channel-apply-private-env-success-smoke
npm run release:channel-apply-private-env-proof
npm run release:channel-apply-private-env-proof-smoke
npm run release:channel-setup-wizard
npm run release:channel-setup-wizard-success-smoke
npm run release:channel-setup-wizard-input-file-success-smoke
npm run release:channel-live-check
npm run release:channel-live-check-strict
npm run release:channel-live-check-strict-success-smoke
npm run release:channel-edit-packet-smoke
npm run release:update-feed-live-check
npm run release:update-feed-live-check-strict
npm run release:update-feed-live-check-strict-success-smoke
npm run release:update-feed-edit-packet-smoke
npm run release:update-feed-post-edit-proof
npm run release:update-feed-post-edit-proof-success-smoke
npm run release:update-metadata-publish-packet-smoke
npm run release:developer-id-operator-packet-smoke
npm run release:external-completion-run-packet-smoke
npm run release:external-completion-resume-packet-smoke
npm run release:update-feed-checkpoint-smoke
npm run release:progress-refresh-smoke
npm run release:completion-summary-refresh-smoke
npm run release:progress-freshness-smoke
npm run desktop:completion-audit-smoke
npm run desktop:external-distribution-gate-smoke
npm run desktop:external-remediation-smoke
npm run desktop:completion-status-smoke
npm run desktop:external-operator-runbook-smoke
npm run desktop:external-readiness-ledger-smoke
npm run desktop:completion-progress-smoke
npm run qa
npm run verify
npm run release:check
npm run release:progress
npm run release:doctor
npm run release:channel-unblock-smoke
npm run release:channel-private-input-template
npm run release:channel-private-input-template-smoke
npm run release:channel-apply-private-env
npm run release:channel-apply-private-env-success-smoke
npm run release:channel-apply-private-env-proof
npm run release:channel-apply-private-env-proof-smoke
npm run release:channel-setup-wizard
npm run release:channel-setup-wizard-success-smoke
npm run release:channel-setup-wizard-input-file-success-smoke
npm run release:channel-live-check
npm run release:channel-live-check-strict
npm run release:channel-live-check-strict-success-smoke
npm run release:external-preflight
npm run release:next-actions
npm run release:proof-bundle
npm run release:final-handoff
npm run release:final-handoff-success-redaction-smoke
npm run release:final-handoff-refresh-smoke
npm run release:audience-completion-handoff-smoke
npm run release:completion-report-packet-smoke
npm run release:operator-completion-brief-smoke
npm run release:channel-clearance-transition-smoke
npm run release:channel-clearance-transition-post-clearance-smoke
npm run release:auto-update-transition-smoke
npm run release:update-feed-live-check
npm run release:update-feed-live-check-strict
npm run release:update-feed-live-check-strict-success-smoke
npm run release:update-feed-edit-packet-smoke
npm run release:update-feed-post-edit-proof
npm run release:update-feed-post-edit-proof-success-smoke
npm run release:update-metadata-publish-packet-smoke
npm run release:developer-id-operator-packet-smoke
npm run release:external-completion-run-packet-smoke
npm run release:external-completion-resume-packet-smoke
npm run release:update-feed-checkpoint-smoke
npm run release:progress-refresh-smoke
npm run release:progress-freshness-smoke
npm run release:current-blocker
npm run release:current-blocker-smoke
npm run release:private-value-leak-audit-smoke
npm run release:private-value-leak-audit
```

Release harness coverage catalog includes the base structure, documentation rules, first-run React renderer surface, first-session workflow smoke, runtime first-run starter project smoke, live production Electron visual launch smoke, native desktop project IO smoke, packaged desktop app smoke, packaged project IO smoke, installed project IO smoke, local ad-hoc signing smoke, hardened runtime readiness smoke, Gatekeeper readiness smoke, Developer ID readiness smoke, Developer ID signing smoke, notarization smoke, release-channel unblock smoke, release current blocker smoke, release-channel unblock summary, release-channel unblock rows, and the private edit proof commands `npm run release:private-edit-quick-proof`, `npm run release:private-edit-quick-proof-smoke`, and `npm run release:private-edit-strict-proof-success-smoke`. `npm run release:channel-unblock-smoke` writes an isolated synthetic env fixture under ignored build output, `npm run release:current-blocker` refreshes release doctor, proof bundle, external gate dry-run, update-feed checkpoint, and release progress evidence, `npm run release:current-blocker-smoke` reads the existing release doctor, external proof bundle, external distribution gate, and release progress artifacts, `npm run release:update-metadata-publish-packet-smoke` writes a value-free operator handoff for update metadata files with selected source artifact posture, signed artifact blockers, proof order, and hard-gate separation, and `npm run release:external-completion-run-packet-smoke` refreshes the completion summary, update feed edit packet, update metadata publish packet, and Developer ID operator packet.

`npm run release:completion-summary-refresh-smoke` now also runs the real `npm run release:channel-apply-private-env-preflight` after the external resume packet and before the conditional 10-plan checkpoint, accepting only success exit `0` or expected blocked exit `1`. Its after-work receipt keeps the synthetic resume-packet blocked-smoke fields separate from the real operator preflight readout, which reports value-free local-env loaded posture, private input file presence, loaded-key count, missing/placeholder/invalid input counts, next write command, proof command, and no-modification/no-claim posture.


`npm run release:operator-completion-brief-smoke` mirrors the current-blocker Current Operator Command Sequence into the compact operator brief, validates the first brief command against the current operator first command, includes `npm run release:prepare-env` only for missing ignored-env setup, and preserves preflight-before-apply plus apply-before-strict-proof order without recording private values.

`npm run release:developer-id-operator-packet-smoke` writes a value-free operator handoff for Developer ID signing, notarization, stapled Gatekeeper evidence, manual QA, distribution-channel blockers, proof order, and hard-gate separation. `npm run release:developer-id-operator-packet-smoke` refreshes Developer ID readiness, conditional isolated signing/notarization/Gatekeeper evidence, manual QA, and distribution-channel QA, then reports Developer ID, notary, stapling, Gatekeeper, signed update artifact, manual QA, distribution-channel readiness rows without values. It conditionally refreshes isolated signing/notarization/Gatekeeper evidence only when local prerequisites exist, stays outside `npm run verify`, and keeps Developer ID identity labels, credentials, tokens, release/support/feed URLs, channel values, private beats, and real user audio out of JSON, Markdown, and console output.

`npm run desktop:package-smoke` signs the freshly copied local `GrooveForge.app` with a launch-only ad-hoc runtime signature before its hidden-window launch. The smoke runs `codesign --force --deep --options runtime --entitlements harness/fixtures/macos-hardened-runtime-entitlements.plist --sign -`, verifies `codesign --verify --deep --strict --verbose=2`, rejects Developer ID authority claims, then verifies Squirrel, ReactiveObjC, and Mantle are present, strictly code-signed, signature-compatible with the app bundle, and dyld-loadable through Electron Framework rpaths. This keeps stale, unsigned, or signature-incompatible nested framework failures, including `@rpath/Squirrel.framework/Squirrel` dyld aborts, inside structured package-smoke failures before a user opens the bundle.

`npm run desktop:packaged-project-io-smoke`, `npm run desktop:adhoc-sign-smoke`, and `npm run desktop:installed-project-io-smoke` reuse the same value-free Electron runtime dependency guard immediately before launching their packaged, runtime-signed, or simulated-installed app copies. This preserves the Squirrel/ReactiveObjC/Mantle rpath, presence, strict code-signature, and dyld-loadable checks across every post-package launch path, so a stale `.app` from an older worktree reports a structured rebuild/package-smoke action instead of surfacing a user-facing macOS `Namespace DYLD` Crash Reporter log.

`npm run release:external-completion-run-packet-smoke` writes a value-free operator run receipt for the remaining external completion pass. It refreshes the completion summary, update feed edit packet, update metadata publish packet, and Developer ID operator packet in order, then reports source rows, blocker action/focus rows, the mirrored Current Operator Command Sequence, the separate `npm run release:channel-apply-private-env-proof` runner, current completion percentage, 10-plan progress, current blocker, current env edit target, current private input placeholder location count/summary/rows, the real current private input receipt mode/presence/loaded/missing/placeholder/invalid counts and next operator command, and 12 ordered run rows from release-channel metadata through the hard gate without recording private values, probing networks, signing, submitting to Apple, publishing feeds, uploading releases, running the hard gate, or claiming auto-update/external distribution. Its first release-channel run row must match the current operator first command, so the run receipt starts with the non-writing private env preflight before the apply helper whenever placeholder metadata is the current blocker. `npm run release:external-completion-resume-packet-smoke` refreshes the external completion run packet, then writes a value-free resume receipt with the first blocked run row and next resume command, next proof command, the mirrored Current Operator Command Sequence, the same current private input placeholder location count/summary/rows, the same real current private input receipt state, the same proof runner handoff, already-ready rows, remaining resume rows, blocker action/focus rows, and hard-gate posture without running the hard gate or claiming external distribution. Its next resume command must match the current operator first command whenever release-channel metadata is the first blocked phase.

`npm run release:private-value-leak-audit` treats generated YAML update metadata (`.yml`/`.yaml`) as scannable text evidence alongside Markdown, JSON, logs, scaffolds, and text files.

`npm run release:private-edit-strict-proof` blocked receipts include value-free blocked handoff rows with the current blocker, ignored edit target, placeholder count, `npm run release:channel-apply-private-env-preflight` before `npm run release:channel-apply-private-env`, return command, and strict first proof command before the command exits non-zero.

`npm run release:private-edit-strict-proof-blocked-smoke` uses a synthetic ignored env fixture to prove the blocked handoff receipt path without reading or modifying the real ignored env.

Completion summary and current-blocker receipts surface `npm run release:channel-apply-private-env-preflight` before `npm run release:channel-apply-private-env`, then `npm run release:private-edit-strict-proof`, so after-work reports show the private-input readiness check, write step, and strict proof chain without recording release URLs, support URLs, channel values, credentials, or tokens. They also mirror the separate `npm run release:channel-apply-private-env-proof` runner as a value-free convenience handoff after preflight readiness without replacing the preflight-first operator sequence. Current-blocker and external next-actions Current Action Handoff packages also surface the separate `npm run release:channel-private-input-template` helper, `.env.release-channel.local`, `GROOVEFORGE_RELEASE_CHANNEL_INPUT_FILE`, and template-before-preflight posture as value-free scaffold guidance without making that helper the current first operator command.

`npm run release:channel-setup-brief` writes a focused value-free operator brief for the active release-channel metadata blocker. It reads process-env presence, the ignored `.env.release-channel.local` private input file selected by `GROOVEFORGE_RELEASE_CHANNEL_INPUT_FILE`, and the ignored distribution env target selected by `GROOVEFORGE_DISTRIBUTION_ENV_FILE`, then records only key names, source labels, file names, line numbers, booleans, row counts, and command names. The brief reports effective input readiness, private-input placeholder rows, current local-env placeholder rows, `npm run release:channel-private-input-template` as the scaffold helper, `npm run release:channel-apply-private-env-preflight` as the current operator first command after inputs, `npm run release:channel-apply-private-env` as the write command, `npm run release:channel-apply-private-env-proof` as the convenience runner, `npm run release:private-edit-strict-proof` as the recommended proof chain, and `npm run release:channel-setup-wizard` as a guided fallback without recording release/support URL values or claiming external distribution. `npm run release:channel-setup-brief-smoke` proves the same rows against synthetic placeholder fixtures under ignored build output without reading or modifying real ignored env files.

`npm run release:channel-placeholder-input-receipt` runs the non-writing release-channel apply preflight and writes a focused value-free receipt for the private input file mode: missing, incomplete, placeholder, invalid-shape, or ready. It records the ignored private input file path, loaded-key count, placeholder/missing/invalid key counts, file/line rows, private-input missing/placeholder/invalid location summaries, next operator command, preflight/apply/proof command rows, and no-modification/no-claim posture without recording release URLs, support URLs, channel values, credentials, tokens, or local env values. `npm run release:channel-placeholder-input-receipt-smoke` proves the placeholder-private-input-file branch with synthetic ignored fixtures under `build/desktop/`, loads four placeholder private input keys, expects the preflight to block, and is included in `npm run verify` after the setup brief smoke; the real receipt runs immediately after the smoke so current-blocker evidence can report the actual ignored private input file mode.

External next-actions builds a separate value-free Current Operator Command Sequence for the active release-channel metadata blocker. The sequence preserves `currentNextCommand` for broader proof refresh semantics, while the operator-facing rows show `npm run release:prepare-env` when the ignored env scaffold is missing, then `npm run release:channel-apply-private-env-preflight`, `npm run release:channel-apply-private-env`, `npm run release:private-edit-strict-proof`, `npm run release:current-blocker`, and `npm run release:next-actions`. The release-channel edit packet exposes the same rows as the source sequence while keeping `npm run release:channel-setup-wizard` as a guided setup helper, and proof bundle, release progress, current-blocker, progress-refresh, completion-summary, completion-summary-refresh, 10-plan checkpoint, and completion-report-packet smokes mirror the same sequence and validate source first-command alignment, guided setup exclusion, preflight before apply, and apply before strict proof without recording private values.

`npm run release:channel-apply-private-env-preflight` runs the same process-env and ignored-env checks as the apply helper without writing the ignored local env file. It writes ignored release-channel-apply-private-env-preflight Markdown/JSON artifacts with a Process Env Input Checklist for the four `process.env` key names, expected input-shape labels, input readiness booleans, preflight/write/proof commands, would-apply rows, readiness booleans, preflight remediation rows, file names, line numbers, `npm run release:channel-apply-private-env` as the next write command, and `npm run release:channel-setup-wizard` as a separate guided fallback only. `npm run release:operator-completion-brief-smoke` mirrors the blocked preflight smoke's preserved checklist into the operator brief as value-free process-env rows before the write helper, so remediation/targeted synthetic smokes cannot replace the operator brief's source. `npm run release:channel-apply-private-env-preflight-smoke` proves that non-writing preflight path against a synthetic ignored env fixture under `build/desktop/`, verifies would-apply `4/4`, applied `0/4`, no synthetic fixture modification, no real local-env read or modification, and no URL/channel/private values in output. `npm run release:channel-apply-private-env-preflight-blocked-smoke` runs the real preflight command with the four private process env inputs intentionally unset, accepts the expected blocked exit, verifies the source report and Process Env Input Checklist remain preflight-only/value-free/non-claiming, and proves the ignored local env is not modified. `npm run release:channel-apply-private-env-remediation-smoke` covers the blocked remediation branches for missing ignored env scaffold, missing process env inputs, placeholder process env inputs, and shape-invalid process env inputs with synthetic fixtures only.

`npm run release:channel-apply-private-env` applies operator-owned release-channel metadata from the current process environment or ignored private input file into the ignored local env file (`.env.distribution.local` by default, or `GROOVEFORGE_DISTRIBUTION_ENV_FILE` when configured), validates the allowed channel token plus safe HTTPS URL shapes, and writes ignored release-channel-apply-private-env Markdown/JSON artifacts with key names, readiness booleans, source rows, row counts, file names, and line numbers only. `npm run release:channel-apply-private-env-input-file-smoke` proves the same private-env apply path can use ignored `.env.release-channel.local` source rows when process env inputs are absent, then passes strict live-check against synthetic ignored fixtures under `build/desktop/`. `npm run release:channel-apply-private-env-success-smoke` proves the process-env apply path against a synthetic ignored env fixture under `build/desktop/`, runs `harness/scripts/run_release_channel_apply_private_env.mjs --success-smoke` against a synthetic ignored env root, verifies 4/4 current-ready rows, and records no synthetic values, URL values, real local-env read or modification, network probe, signing, Apple notary submission, upload, or external-distribution claim.

`npm run release:channel-apply-private-env-proof` is the operator-facing one-command private metadata proof runner: it runs preflight before apply, skips apply while preflight is blocked, runs strict proof only after apply readiness, and reads the compact completion summary only after strict proof readiness. It writes value-free release-channel-apply-private-env-proof Markdown/JSON artifacts with command/source rows, first-command alignment, current completion posture, and no value/external-claim fields. `npm run release:channel-apply-private-env-proof-smoke` runs the same control flow with synthetic preflight/apply/strict-proof success coverage plus a synthetic completion readout so this path can be validated without bootstrapping the full ignored release evidence set.

`npm run release:channel-setup-wizard` is the guided local-only path around the same apply helper. It reads the four release-channel metadata values from `process.env` first, then the ignored `.env.release-channel.local` file selected by `GROOVEFORGE_RELEASE_CHANNEL_INPUT_FILE`, then prompts when needed, creates the ignored local env scaffold when missing, runs `npm run release:channel-apply-private-env-preflight` before delegating the write to `npm run release:channel-apply-private-env`, runs `npm run release:channel-live-check-strict`, and writes value-free release-channel-setup-wizard Markdown/JSON artifacts with key names, input-source rows, private input-file metadata, readiness booleans, command names, and exit codes only. `npm run release:channel-setup-wizard-success-smoke` proves the process-env synthetic path, and `npm run release:channel-setup-wizard-input-file-success-smoke` proves the ignored private input-file path, both against synthetic ignored env roots for `npm run verify` without reading or modifying the real ignored env or recording synthetic URL/channel values.

`npm run release:final-handoff` refreshes the post-edit proof bundle, strict success smoke, and real strict live-check receipt, then writes ignored release-final-handoff Markdown/JSON artifacts that tie source artifact readiness, current private edit rows, strict proof rows, post-edit proof sequence rows, release-channel post-edit operator receipt rows, hard-gate boundary, user-facing completion percentage, and 10-plan cadence into one value-free operator receipt without recording URL/channel values or claiming external distribution. `npm run release:final-handoff-success-redaction-smoke` uses synthetic value-free source artifacts with a separate final-handoff artifact stem to prove the strict-ready final handoff path stays redacted without reading or modifying the real local env. `npm run release:final-handoff-refresh-smoke` runs proof-bundle refresh, progress refresh, real final handoff, and success-redaction final handoff in order, then writes an ignored final-handoff-refresh receipt proving final-handoff and post-edit-proof labels match the latest 10-plan checkpoint while values and external distribution claims remain absent. `npm run release:audience-completion-handoff-smoke` refreshes persona readiness, sample-free local delivery, package reopen, and release doctor evidence, then writes an ignored audience-completion-handoff receipt proving beginner/professional producer readiness, direct composition, all-genre coverage, local package durability, current 10-plan progress, and the remaining external/private blocker without values or release claims.

`npm run release:channel-clearance-transition-smoke` refreshes the final-handoff success-redaction smoke and release doctor, then writes ignored release-channel-clearance-transition-smoke Markdown/JSON artifacts that connect synthetic clearance, the current release transition mode, the auto-update-feed next priority action, the hard-gate boundary, and the current 10-plan progress without recording URL/channel/private values or claiming external distribution. It validates that the synthetic strict-ready proof shows four ready release-channel rows and zero placeholders, the current mode is either missing ignored env setup, placeholder replacement, or post-release-channel-clearance with zero release-channel placeholders and a downstream current action, `auto-update-feed` remains the next operator focus with `npm run desktop:auto-update-readiness-smoke`, and `npm run release:external-check` remains the hard gate. `npm run release:channel-clearance-transition-post-clearance-smoke` runs the same report builder with synthetic post-clearance source fixtures so the downstream mode remains covered before real private values are applied.

`npm run release:channel-apply-private-env-targeted-smoke` runs the release-channel preflight, apply, and strict live-check helpers against a synthetic full distribution env scaffold, proving the apply helper changes only the four release-channel metadata rows, preserves unrelated update-feed, Developer ID, notary, and manual-QA placeholders, and records no URL/channel/feed/credential/token/private values or external-distribution claim.

`npm run release:channel-edit-packet-smoke` refreshes release doctor and release-channel live-check evidence, then writes ignored release-channel-edit-packet-smoke Markdown/JSON artifacts with the current edit packet mode, ignored env edit target, four release-channel metadata rows, key shape guidance, placeholder locations when present, `npm run release:channel-setup-wizard` as the guided setup command, `npm run release:private-edit-strict-proof` as the recommended operator proof chain, lower-level live-check and strict live-check proof commands, operator command order with `npm run release:channel-apply-private-env-preflight` before `npm run release:channel-apply-private-env`, a separate source Current Operator Command Sequence that excludes the guided setup wizard, hard-gate boundary, current 10-plan progress, completion percentage, and non-claim posture without URL/channel/private values. It is included in `npm run verify` after the release-channel setup wizard success smokes so completion evidence reads a fresh release-channel edit handoff.

`npm run release:completion-report-packet-smoke` refreshes audience completion handoff evidence, release-channel edit packet evidence, private-edit strict-proof blocked-smoke evidence, final handoff success-redaction evidence, release-channel clearance transition evidence, release auto-update transition evidence, and update-feed checkpoint evidence in one sequence, then writes ignored release-completion-report-packet-smoke Markdown/JSON artifacts with matched latest 10-plan labels, beginner/professional producer readiness, direct composition, all-genre coverage, local package durability, current release-channel edit packet mode, the edit packet's guided setup command, preflight command before apply, recommended `npm run release:private-edit-strict-proof` proof chain, source Current Operator Command Sequence and first-command match, private-edit blocked smoke evidence rows/readiness, blocked handoff row count, strict failure row count, real-env read/modify boundaries, final handoff success-redaction evidence rows/readiness, strict-ready metadata posture, synthetic final handoff real-env boundary, post-clearance next-action readiness, auto-update transition readiness, real auto-update blocker rows, update-feed checkpoint evidence rows/readiness, real and synthetic selected-key posture, hard-gate would-fail posture, completion percentage, remaining percentage, 10-plan report due flag, current 10-plan report boundary, next scheduled 10-plan progress report after delivery, current 10-plan completed rows, 10-plan progress report receipt rows/readiness including the private-edit blocked smoke, final handoff success-redaction, auto-update proof, and update-feed checkpoint proof rows, 10-plan cadence rollover rows/readiness, private-edit proof command order with preflight before apply, and external/private blocker posture without recording values or claiming external distribution.

`npm run release:auto-update-transition-smoke` refreshes the release-channel clearance transition, update feed config smoke, and real auto-update readiness smoke, then writes ignored release-auto-update-transition-smoke Markdown/JSON artifacts that connect `auto-update-feed`, the synthetic feed/channel config ready branch, real auto-update blocker rows, signed-update artifact readiness, the hard-gate boundary, and current 10-plan progress without recording feed/channel/private values or claiming auto-update or external distribution. It validates that the synthetic feed/channel config is ready and redacted, the real auto-update readiness remains blocked, `npm run desktop:auto-update-readiness-smoke` remains the proof command, and `npm run release:external-check` remains the hard gate.

`npm run release:external-check` is the hard external distribution gate. It runs `npm run release:check` first, then reruns the external distribution gate without `--dry-run`; when the external proof bundle exists, the hard gate report mirrors the same current next command, first blocker, edit guidance rows, proof checklist rows, and command verification rows, and fails until the current redacted artifacts prove private inputs, distribution-channel QA, auto-update, Developer ID signing, notarization/stapling, and notarized Gatekeeper readiness.

`npm run desktop:local-delivery-package-smoke` runs after the runtime smoke. It writes an ignored real local 8-bar delivery package under `build/desktop/` with project JSON, full mix WAV, four stem WAVs, arrangement MIDI, Handoff Sheet, and a checksum manifest, then verifies headers, byte sizes, checksums, project roundtrip, Handoff sections, and no optional-sampling language without writing private beats or user audio.

`npm run desktop:local-package-reopen-smoke` runs after the local delivery package smoke. It reads the ignored package back from disk, verifies manifest paths, byte sizes, SHA-256 checksums, WAV headers, MIDI header, Handoff sections, project roundtrip, and regenerated mix/stem/MIDI/Handoff output from the reopened project without recording private values or claiming external distribution completion.

`npm run desktop:local-delivery-zip-smoke` runs after the local package reopen smoke. It reads the ignored package and reopen evidence, writes a single ignored ZIP archive containing the project JSON, full mix WAV, four stem WAVs, arrangement MIDI, Handoff Sheet, and package manifest files, then verifies archive-safe paths, stored ZIP entries, local headers, central directory records, EOCD metadata, byte sizes, CRC-32 checksums, and source SHA-256 matches without recording private values or claiming external distribution completion.

`npm run delivery:bundle-zip-smoke` exercises the dependency-free in-app delivery bundle ZIP generator used by the toolbar, Handoff Pack, and Quick Actions Bundle export. It creates one sample-free local ZIP with project JSON, full mix WAV, four stem WAVs, arrangement MIDI, Handoff Sheet, manifest JSON, and manifest Markdown, then verifies project roundtrip, WAV/MIDI/Handoff headers, central directory entries, local headers, stored ZIP method, CRC-32 values, safe entry names, and value-free/local-first posture without recording private values or claiming external distribution completion.

`npm run desktop:external-remediation-smoke` runs after the dry-run external distribution gate. It writes ignored Markdown/JSON remediation artifacts that translate remaining hard-gate blockers into ordered operator action groups, required key names, evidence paths, prerequisite commands, and rerun commands without recording private values or claiming external distribution completion.

`npm run desktop:completion-status-smoke` runs after external remediation. It writes ignored Markdown/JSON status artifacts that consolidate local MVP readiness, desktop project IO readiness, local desktop package readiness, redacted distribution evidence, hard-gate readiness, pending external remediation groups, and next commands without recording private values or claiming external distribution completion.

`npm run desktop:external-operator-runbook-smoke` runs after completion status. `npm run verify` runs it again after `release:next-actions-smoke` so the final runbook can mirror current-action evidence. It writes ignored Markdown/JSON operator runbook artifacts with the final external distribution command sequence, desktop project IO readiness from completion status and the external gate, evidence checklist, current manual QA digest evidence, hard-gate command, current value-free edit guidance rows, current proof checklist rows, and current command verification rows without recording private values or claiming external distribution completion.

`npm run desktop:external-readiness-ledger-smoke` runs after the operator runbook. It writes ignored Markdown/JSON ledger artifacts with desktop project IO readiness from completion status, the external gate, and the operator runbook, hard-gate requirement counts, remediation counts, first blockers, manual QA digest posture, local release stage, evidence rows, and the refreshed runbook's current value-free edit guidance rows, proof checklist rows, and command verification rows without recording private values or claiming external distribution completion.

`npm run desktop:completion-progress-smoke` runs after the external readiness ledger. It writes ignored Markdown/JSON progress artifacts with source evidence readiness, missing source artifact rows, `npm run release:check` prerequisite guidance, local release readiness percent, desktop project IO readiness, external hard-gate readiness, external gate requirement counts, remediation group counts, current first blockers, the refreshed ledger's current value-free edit guidance rows, proof checklist rows, command verification rows, and not-recorded/not-claimed posture without recording private values or claiming external distribution completion.

`npm run release:progress` runs the full local release gate through `npm run release:check`, reads the generated completion progress JSON, external proof bundle JSON, and external distribution gate JSON, derives completed-plan progress from `docs/exec_plans/completed`, and writes ignored Markdown/JSON release progress report artifacts with user-facing overall completion percent, remaining completion percent, current 10-plan window progress, report cadence, current 10-plan report boundary, next scheduled 10-plan progress report after delivery, 10-plan progress report receipt rows/readiness, 10-plan cadence rollover rows/readiness, release-channel post-edit receipt rows/readiness/current-ready count, release-channel post-edit operator receipt rows/readiness, post-edit proof sequence receipt rows/readiness mirrored from the proof bundle, recommended `npm run release:private-edit-strict-proof` proof chain fields, next proof target, next blocker/command, operator action, local release readiness, desktop project IO readiness, external hard-gate readiness, gate requirement counts, remediation counts, first blockers, proof bundle readiness, proof artifact present/missing counts, proof gate ready/blocked counts, external gate source readiness, external gate current proof source readiness, external gate/proof current-action consistency checks, current proof target, current next command, current first blocker, current operator action, current required/placeholder key summaries, current private input placeholder location summary/rows, current env edit target, current placeholder remediation summary, current proof/action checklist summaries, current proof checklist rows, current action checklist rows, current rerun command, current command sequence summary, current command verification row summary, current command verification rows, external gate current proof rows including action checklist rows, and not-recorded/not-claimed posture without recording private values or claiming external distribution completion. It also writes `10-Plan Cadence Rollover`, `Release-Channel Post-Edit Operator Receipt`, and `Post-Edit Proof Sequence Receipt` sections with value-free current boundary, next scheduled report after delivery, `npm run release:channel-apply-private-env-preflight`, `npm run release:channel-apply-private-env`, recommended strict proof chain, doctor, current-blocker, next-actions, proof-bundle, progress-smoke, and hard-gate command order. `npm run release:progress-smoke` runs the same report writer with `--from-existing`, skips the full gate, requires existing completion progress, external proof bundle, and external distribution gate JSON, and records report mode plus release-check-run posture in the output.

`npm run release:current-blocker` refreshes the current external release blocker evidence without running the full local release gate when source release evidence already exists. It runs release doctor, release proof bundle, external distribution gate dry-run, update-feed checkpoint smoke, release progress smoke, and the real release-channel placeholder input receipt, then writes ignored Markdown/JSON current-blocker artifacts with source mode, refresh command sequence, current target, next command, first blocker, required keys, placeholder keys, edit locations, edit rows, proof checklist rows, action checklist rows, command verification rows, hard-gate ready/would-fail posture, hard-gate requirement ladder rows, blocked hard-gate requirement rows, blocked hard-gate action matrix rows, external next-actions priority action ladder rows, pending priority action evidence matrix rows, current/next action transition preview rows, next action preview ready criteria rows, checklist rows, blocker rows, verification rows, prerequisite command rows, operator action rows, and env edit rows from the second ordered priority action, external completion checklist rows, current action acceptance rows, current action acceptance blocker rows, current action acceptance remediation rows, current release-channel key remediation matrix rows, current command acceptance ladder rows, current command source artifact matrix rows, current action post-edit verification rows, current action handoff package rows, private edit safety checklist rows, current input shape checklist rows for release-channel metadata, local env loader diagnostic rows for file presence, placeholder scope, unknown keys, malformed lines, skipped existing env keys, loaded-key redaction, and value-recording posture, release-channel focus receipt rows/readiness/current-ready count mirrored from release doctor, placeholder private-input receipt mode, private input file presence, loaded/missing/placeholder/invalid row counts, private input missing/placeholder/invalid location summaries, placeholder-input command rows, guided setup fallback command with explicit primary-sequence exclusion, current priority action alignment, current completion checklist alignment, current action acceptance alignment, current action acceptance blocker alignment, current action acceptance remediation readiness, current release-channel key remediation readiness, blocked hard-gate action matrix readiness, priority action evidence matrix readiness, current command acceptance ladder readiness, current command source artifact matrix readiness, completion percent, 10-plan progress, 10-plan report due/cadence/current-boundary/next-scheduled posture mirrored from release progress, 10-plan progress report receipt rows/readiness mirrored from release progress, 10-plan cadence rollover rows/readiness mirrored from release progress, release-channel post-edit receipt rows/readiness/current-ready count mirrored from release progress, release-channel post-edit operator receipt rows/readiness plus recommended `npm run release:private-edit-strict-proof` proof chain mirrored from release progress, post-edit proof sequence receipt rows/readiness plus recommended strict proof chain mirrored from release progress, audience readiness rows, and audience acceptance matrix rows while recording no private values. If source release evidence is missing, it stops with `npm run release:check` guidance before the external gate dry-run. `npm run release:current-blocker-smoke` uses the same writer with `--from-existing` for `npm run verify` and expects the real placeholder input receipt artifact to exist from the earlier verify step.

`npm run release:doctor` runs targeted redacted distribution input/readiness smokes for the distribution env template, release prepare-env, update feed config, Developer ID readiness, distribution manual QA checklist, distribution-channel QA, and private-input checklist. It writes ignored Markdown/JSON release doctor artifacts with local env readiness, prepare-env scaffold readiness, private input group counts, local env placeholder key names/counts, manual QA digest posture, release-channel focus receipt rows/readiness/current-ready count mirrored from private-input evidence, channel metadata blockers, Developer ID readiness, notarization credential-signal readiness, source artifact presence, current next proof command, current operator start command, current rerun command sequence, and first blockers without recording private values, running the full release gate, probing remote channels, uploading releases, signing artifacts, submitting to Apple, or claiming external distribution completion. When release-channel placeholders remain, it keeps `npm run release:doctor` as the current next proof command, surfaces `npm run release:channel-apply-private-env-preflight` as the concrete operator start command, and includes `npm run release:current-blocker` as the value-free refresh rerun after ignored env edits.

`npm run release:external-preflight` runs after local release evidence exists. It reruns `npm run release:doctor`, completion audit, external distribution gate dry-run, external remediation, completion status, external operator runbook, external readiness ledger, and completion progress, then writes ignored Markdown/JSON external preflight artifacts with local release readiness, desktop project IO readiness, PKG payload project IO readiness, hard-gate would-fail status, requirement counts, remediation counts, private-input group counts, local env loader status, local env placeholder key names/counts, source evidence paths, first blockers, and hard-gate command guidance without recording private values, running the full release gate, probing remote channels, uploading releases, signing artifacts, submitting to Apple, or claiming external distribution completion.

`npm run release:next-actions` runs after external preflight evidence exists, and it can bootstrap a clean checkout when ignored source evidence is missing. It reruns `npm run release:external-preflight` by default when source evidence exists, then writes ignored Markdown/JSON external next-actions artifacts with current focus, current next command, current first blocker, current required key count/names, current placeholder key count/names, current placeholder edit location count/summary, current env key guidance count/summary, current env edit template count/summary, current env edit rows count/summary, current placeholder remediation row count/summary, current evidence rows count/summary, current evidence labels count/summary, current ready criteria count/summary, current proof checklist row count/summary, current action checklist count/summary, current action acceptance row/blocker summaries, current action post-edit verification row/current-ready summaries, current action handoff package rows, current private edit safety row summaries, current input shape checklist row summaries, current local env diagnostic row summaries, release-channel post-edit receipt row/current-ready summaries, release-channel post-edit operator receipt row summaries, post-edit proof sequence receipt row/command summaries, next priority action summary, next-action preview row summaries, current prerequisite command count/summary, current rerun command count/summary, current command sequence count/summary, current command verification row count/summary, current env edit target, current operator action, current rerun command, current action metadata, prioritized pending remediation groups, stable value-free evidence rows, key guidance, env edit templates, and env edit rows for every required private env key, value-free ready criteria, value-free edit locations, and value-free checklists in priority action details, next commands, first blockers, local release readiness, hard-gate posture, and source evidence paths. If source evidence is missing, it writes a bootstrap report with `bootstrapMode`, `sourceEvidenceReady: false`, missing source artifact rows, and first action `npm run release:check`. If release channel metadata is blocked and no local env file is loaded, it makes `npm run release:prepare-env` the first release-channel command before private-input validation, makes the missing ignored local env file the first blocker, and surfaces value-free guidance plus assignment templates for the current channel and URL keys. If a local env file is loaded but placeholders remain, it reports local env placeholder key names/counts, makes placeholder cleanup the first release-channel blocker, surfaces value-free file/line edit locations for the current release-channel placeholder keys, points the current operator action at the current env edit target for those current keys first, keeps the full local placeholder list as global evidence, surfaces the same current key guidance, current env edit template, current env edit rows, current placeholder remediation rows, current evidence rows, current evidence labels, current ready criteria, current proof checklist rows, current action acceptance rows/blockers, current action post-edit verification rows, current action handoff package rows, current private edit safety rows, current input shape checklist rows, current local env diagnostic rows, release-channel post-edit receipt rows, release-channel post-edit operator receipt rows, post-edit proof sequence receipt rows, current command verification rows, current action checklist, current prerequisite command summary, current rerun command summary, current command sequence summary, and next-action preview rows for the second priority action, keeps `npm run release:doctor` as the next proof command, and makes `npm run release:current-blocker` the first rerun command after applying private env metadata. Current env edit rows combine the edit target, file/line or scaffold placeholder, assignment shape, guidance, placeholder status, and `valueRecorded: false`; Current placeholder remediation rows combine key, location, assignment, guidance, release doctor source, next command, rerun command, and `valueRecorded: false`; current evidence rows cite only stable artifact labels, paths, presence, and `valueRecorded: false`; current evidence labels list those artifact names for console-first operation; current proof checklist rows connect each ready criterion to evidence labels, paths, proof command, rerun command, hard gate command, and `valueRecorded: false`; current action acceptance rows connect each ready criterion to current evidence, proof command, rerun command, and hard gate without values; current action post-edit verification rows name expected ready signals after private edits; current action handoff package rows summarize source artifacts, edit target, acceptance blockers, rerun order, and hard gate; current private edit safety rows cover ignored env target, value-free output, rerun order, hard-gate separation, and no remote side effects; current input shape checklist rows cover the four release-channel metadata keys with allowed channel-token and safe HTTPS URL-shape guidance; current local env diagnostic rows cover checked files, current edit target presence, current placeholder scope, unknown keys, malformed lines, skipped existing environment overrides, loaded-key redaction, and value-recording posture; release-channel post-edit receipt rows cover current key coverage, shape rehearsal coverage, placeholder cleanup acceptance, proof/rerun sequence, acceptance evidence coverage, and hard-gate separation without values; release-channel post-edit operator receipt rows cover ignored edit target, recommended strict proof chain, release doctor proof, current-blocker refresh, next-actions refresh, hard-gate boundary, and value redaction without values; post-edit proof sequence receipt rows cover `npm run release:channel-apply-private-env`, recommended strict proof chain, release doctor proof, current-blocker refresh, next-actions refresh, proof-bundle refresh, progress-smoke refresh, and hard-gate boundary without values; next-action preview rows mirror `priorityActions[1]` with ready criteria, checklist, evidence, blockers, verification signals, prerequisite commands, operator actions, and env edit rows without values; current command verification rows classify each sequence command by role, expected evidence, proof target, rerun posture, hard gate command, and `valueRecorded: false`; current command summaries list the prerequisite and rerun command sequence without values; current command sequence combines prerequisite, next, and rerun commands in order without values. Both paths avoid recording private values, running the full release gate inside the report command, probing remote channels, uploading releases, signing artifacts, submitting to Apple, or claiming external distribution completion. `npm run release:next-actions-smoke` uses the existing preflight evidence for the release gate and remains the strict existing-evidence path.

When available, selected ignored private input file placeholder rows are surfaced as value-free file/line/key guidance and mirrored through the current action summary without private values.

Current action handoff package rows also include a value-free private input template helper for `npm run release:channel-private-input-template`, `.env.release-channel.local`, `GROOVEFORGE_RELEASE_CHANNEL_INPUT_FILE`, and template-before-preflight posture while preserving the current first operator command: `npm run release:prepare-env` when the ignored env scaffold is missing, otherwise the private-env preflight.

Release next-actions also mirrors the release doctor release-channel focus receipt rows/readiness/current-ready count/placeholder count so the prioritized action list, release doctor, and current-blocker receipt all show the same four current metadata keys without recording URL or channel values.

`npm run release:proof-bundle` runs after next-actions evidence exists, and it reruns `npm run release:next-actions` by default before writing ignored Markdown/JSON external proof bundle artifacts. The bundle indexes value-free proof artifact paths and presence counts for release doctor, external preflight, external next-actions, operator runbook, readiness ledger, completion status/progress, remediation, hard gate, private inputs, distribution-channel QA, manual QA, auto-update, Developer ID signing, notarization, notarized Gatekeeper, release manifest, release notes, support, handoff, and bundle manifest evidence. It also mirrors gate requirement rows, current first blockers, current next command, current required/placeholder key summaries, current env edit target, current placeholder remediation summary, current proof checklist rows, current action checklist rows, current rerun command, current command sequence summary, current command verification rows, release-channel post-edit operator receipt rows, post-edit proof sequence receipt rows, local env loader posture, and `npm run release:external-check` without recording private values or claiming external distribution completion. `npm run release:proof-bundle-smoke` uses existing evidence for the release gate and remains value-free.

Release readiness evidence is maintained in `docs/release/readiness.md`. It maps the current professional-producer, beginner, direct-composition, all-style, local export, privacy, and desktop-readiness requirements to source/docs evidence and automated gates.

For release next-actions, proof bundle, and progress reporting, current evidence labels list those artifact names for console-first operation; current command summaries list the prerequisite and rerun command sequence without values. Current proof checklist rows, current action acceptance rows, post-edit verification rows, handoff package rows, private edit safety rows, input shape checklist rows, local env diagnostic rows, release-channel post-edit receipt rows, release-channel post-edit operator receipt rows, and command verification rows are top-level current-action evidence and are not private values. Next-action preview rows are forward-looking operator evidence from the second priority action and are also value-free.

`npm run renderer:smoke` uses Vite SSR to server-render the actual first-run React `App` without binding localhost. It verifies the default starter transport, beginner guide path, producer scan path, visible Audience Starter Build Starter Project controls, searchable Audience Starter Quick Actions, direct compose/sound/arrange/mix/master/export panels, Handoff surface, and sampling-free first-run copy while avoiding browser automation, Electron windows, network calls, imported audio, and sampler scope.

`npm run workflow:smoke` starts from `starterProject` and creates two concrete local first-session projects through `createAudienceStarterProject`: a beginner guided first beat and a producer fast-pass beat. It verifies setup changes, event density across drums/808/melody/chords, arrangement, delivery target, mix/master posture, `.grooveforge.json` save/load, export and stem analysis, MIDI bytes, Handoff Sheet sections, and sampling-free output without writing media artifacts.

`npm run persona:smoke` writes ignored Markdown/JSON persona readiness artifacts under `build/desktop/`. It verifies first-time composer readiness, professional producer readiness, direct composition readiness, 14/14 all-genre style readiness, local export readiness, shared Audience Starter domain creation, and sampling-secondary posture through the first-run renderer plus local workflow/domain/export evidence without recording private values or claiming external distribution.

`npm run desktop:smoke` runs after build artifacts exist. It checks that `dist-electron/main.js` and `dist-electron/preload.cjs` were built, the production Electron main entry loads `dist/index.html` through `loadFile`, the compiled CommonJS preload exposes the bounded `grooveforge` desktop bridge in Electron's sandboxed preload environment, native menu commands stay validated, the renderer `NativeMenuCommand` declaration and `handleNativeMenuCommand` switch cover every allowlisted command through existing workstation handlers, core BrowserWindow security settings remain enabled, the restricted macOS GUI launch guard blocks Codex `CODEX_SANDBOX` Electron launches before AppKit registration while allowing normal GUI sessions, and the shared AppKit `SIGABRT` / exit-code-6 plus Electron runtime framework DYLD classifiers are wired into launch-bearing desktop smokes.

`npm run desktop:launch-smoke` runs after build artifacts exist. It starts the production Electron app with `GROOVEFORGE_DESKTOP_LAUNCH_SMOKE=1`, refuses restricted macOS `CODEX_SANDBOX` launches before spawning Electron so the AppKit `SIGABRT` / exit-code-6 Crash Reporter path is not triggered in Codex sandbox runs, classifies any child-process AppKit `SIGABRT`, exit code 6, or attached Crash Reporter shape as a restricted/non-GUI launch diagnostic with the GUI/AppKit rerun action, classifies Squirrel/ReactiveObjC/Mantle `Namespace DYLD` library-missing reports as Electron runtime framework dependency failures with a fresh build/package-smoke action, keeps the BrowserWindow hidden, waits for the built renderer to mount under `#root`, verifies the live `window.grooveforge` preload bridge, checks the production `file:` renderer URL, confirms the first-run desktop DOM contains Audience Session Readout, Enter Guided, Enter Studio, the beginner guided path, producer studio path, direct compose/sound/arrange/mix/master/export controls, Handoff Pack, and no sampling-first language, then captures the rendered page without saving media artifacts and verifies screenshot dimensions, PNG byte size, opacity, contrast, sampled color diversity, and non-background UI pixel coverage.

`npm run desktop:project-io-smoke` runs after desktop launch smoke. It starts the same production Electron app with `GROOVEFORGE_DESKTOP_PROJECT_IO_SMOKE=1`, calls `window.grooveforge.saveProject` and `window.grooveforge.openProject` through the real context-isolated preload bridge, uses smoke-only ignored file paths instead of visible dialogs, writes and reopens a sample-free `.grooveforge.json` project under `build/desktop/`, verifies exact bytes plus `parseProjectFile` roundtrip, and keeps private values, user audio, network probes, and external-distribution claims out of the artifact.

`npm run desktop:package-smoke` runs after build artifacts exist. On macOS it assembles an ignored `build/desktop/GrooveForge-darwin-*/GrooveForge.app` portable app from the installed Electron runtime, copies the built renderer plus Electron main/preload output into `Contents/Resources/app`, writes a minimal production `package.json`, generates `GrooveForge.icns` from `assets/brand/grooveforge-icon.svg`, replaces the Electron default icon reference, removes the Electron default icon file, validates root/helper bundle naming plus `Info.plist` privacy posture, verifies Electron Framework `LC_RPATH` resolution for `Squirrel.framework`, `ReactiveObjC.framework`, and `Mantle.framework` with strict framework/binary `codesign` checks, then launches that packaged app with `GROOVEFORGE_DESKTOP_LAUNCH_SMOKE=1` to reuse the live beginner, producer, direct workstation, preload bridge, sampling-free copy, and screenshot pixel evidence checks. Hidden-window launch smoke waits for `ready-to-show` and retries visual evidence until the smoke deadline before failing. This smoke proves a local portable app bundle, not installer creation, Developer ID signing, notarization, auto-update, or distribution-channel QA.

`npm run desktop:packaged-project-io-smoke` runs after `npm run desktop:package-smoke` and before the dedicated hardened-runtime ad-hoc signing smoke. On macOS it requires the assembled `GrooveForge.app`, launches the app executable with `GROOVEFORGE_DESKTOP_PROJECT_IO_SMOKE=1`, uses the bundled main process plus context-isolated preload bridge and IPC handlers, writes and reopens smoke-only ignored `.grooveforge.json` paths under `build/desktop/`, verifies exact byte and `parseProjectFile` roundtrip for an 8-bar sample-free `beat_store` project, checks the derived project file name, and records no private values, user audio, network probes, release uploads, or external-distribution claims.

`npm run desktop:adhoc-sign-smoke` runs after `npm run desktop:packaged-project-io-smoke`. On macOS it applies a local ad-hoc signature to the packaged `GrooveForge.app` with `codesign --force --deep --options runtime --entitlements harness/fixtures/macos-hardened-runtime-entitlements.plist --sign -`, verifies it with `codesign --verify --deep --strict`, confirms `Signature=adhoc`, the `app.grooveforge.desktop` identifier, hardened runtime flags on both the app bundle and main executable, and the Electron runtime entitlements `com.apple.security.cs.allow-jit`, `com.apple.security.cs.allow-unsigned-executable-memory`, and `com.apple.security.cs.disable-library-validation`, rejects Developer ID authority claims, then launches the signed app with `GROOVEFORGE_DESKTOP_LAUNCH_SMOKE=1` to reuse the live beginner, producer, direct workstation, preload bridge, sampling-free copy, and screenshot pixel evidence checks. This smoke proves only local ad-hoc signing with hardened runtime option and entitlement compatibility plus signed-app launch integrity, not Developer ID signing, notarization, Gatekeeper approval, auto-update, app-store submission, or external distribution-channel QA.

`npm run desktop:hardened-runtime-readiness-smoke` runs after `npm run desktop:adhoc-sign-smoke`. On macOS it inspects the packaged app bundle and main executable with local `codesign --display --verbose=4` plus strict verification, writes an ignored `build/desktop/GrooveForge-*-hardened-runtime-readiness.json` summary with signature identifiers, runtime-flag evidence, ad-hoc/Developer ID authority posture, readiness blockers, `networkSubmissionAttempted: false`, `releaseGateClaimedHardenedRuntime: false`, `releaseGateClaimedNotarization: false`, `releaseGateClaimedGatekeeperApproval: false`, and `releaseGateClaimedExternalDistribution: false`. The local ad-hoc artifact should now expose runtime flags, while ad-hoc signing and missing Developer ID authority remain readiness blockers because local artifacts should not be reported as notarization-ready.

`npm run desktop:dmg-smoke` runs after `npm run desktop:hardened-runtime-readiness-smoke`. On macOS it creates an ignored local UDZO DMG from the validated ad-hoc signed `GrooveForge.app`, mounts it read-only, verifies that the image contains only `GrooveForge.app` plus an Applications shortcut, checks the mounted app payload for the GrooveForge bundle id, executable, `GrooveForge.icns`, packaged renderer/main assets, and absence of `electron.icns`, then detaches the image. This smoke proves a local disk-image artifact, not Developer ID signing, notarization, auto-update, app-store submission, or external distribution-channel QA.

`npm run desktop:pkg-smoke` runs after `npm run desktop:dmg-smoke`. On macOS it creates an ignored local unsigned `GrooveForge-*-darwin-*.pkg` from the ad-hoc signed `GrooveForge.app`, records `/Applications` install-location metadata, verifies expanded `PackageInfo`, payload file listing, required payload file listing for Info.plist, executable, GrooveForge icon, renderer HTML, Electron main, and preload, absence of `electron.icns`, PKG bytes, and SHA-256 evidence, then writes ignored Markdown/JSON PKG smoke artifacts. This smoke proves a local unsigned installer artifact, not real `/Applications` installation, Developer ID Installer signing, notarization, Gatekeeper approval, auto-update, upload, app-store submission, or external distribution completion.

`npm run desktop:pkg-payload-smoke` runs after `npm run desktop:pkg-smoke`. On macOS it expands the local unsigned PKG, extracts the Payload archive under ignored `build/desktop/.../pkg-payload-smoke/`, verifies the extracted `GrooveForge.app` bundle, retained ad-hoc signature, GrooveForge icon, packaged renderer/main/preload assets, and absence of `electron.icns`, then launches the extracted app with `GROOVEFORGE_DESKTOP_LAUNCH_SMOKE=1` to reuse the live beginner, producer, direct workstation, preload bridge, sampling-free copy, and screenshot pixel evidence checks. This smoke proves the local PKG payload contains a launchable app without running the macOS Installer, touching the real `/Applications` directory, claiming Developer ID Installer signing, claiming Developer ID Application signing, notarization, Gatekeeper approval, upload, app-store submission, or external distribution completion.

`npm run desktop:pkg-payload-project-io-smoke` runs after `npm run desktop:pkg-payload-smoke`. On macOS it launches the extracted PKG payload `GrooveForge.app` with `GROOVEFORGE_DESKTOP_PROJECT_IO_SMOKE=1`, writes and reopens an ignored sample-free `.grooveforge.json` project under `build/desktop/`, verifies exact bytes, parser roundtrip, bundled file assets, real preload bridge, save/open IPC handlers, and no sampling-first text, then writes value-free Markdown/JSON evidence. This smoke proves native project IO works from the app contained in the PKG payload without running the macOS Installer, touching the real `/Applications` directory, claiming Developer ID Installer signing, claiming Developer ID Application signing, notarization, Gatekeeper approval, upload, app-store submission, or external distribution completion.

`npm run desktop:install-smoke` runs after `npm run desktop:pkg-payload-project-io-smoke`. On macOS it mounts the local DMG read-only, copies `GrooveForge.app` into an ignored simulated Applications directory under `build/desktop/.../install-smoke/Applications/`, verifies the copied app keeps the GrooveForge bundle id, branded icon, packaged renderer/main/preload assets, no `electron.icns`, and `Signature=adhoc`, then launches the installed copy with `GROOVEFORGE_DESKTOP_LAUNCH_SMOKE=1` to reuse the live beginner, producer, direct workstation, preload bridge, sampling-free copy, and screenshot pixel evidence checks. This smoke proves a local DMG copy-and-launch path without touching the real `/Applications` directory and without claiming Developer ID signing, notarization, Gatekeeper approval, auto-update, app-store submission, or external distribution-channel QA.

`npm run desktop:installed-project-io-smoke` runs after `npm run desktop:install-smoke` and before Gatekeeper readiness. On macOS it requires the simulated installed `build/desktop/.../install-smoke/Applications/GrooveForge.app`, launches that app executable with `GROOVEFORGE_DESKTOP_PROJECT_IO_SMOKE=1`, uses the installed app's bundled main process plus context-isolated preload bridge and IPC handlers, writes and reopens smoke-only ignored `.grooveforge.json` paths under `build/desktop/`, verifies exact byte and `parseProjectFile` roundtrip for an 8-bar sample-free `beat_store` project, checks the derived project file name, and records no private values, user audio, network probes, release uploads, real `/Applications` install claims, or external-distribution claims.

`npm run desktop:gatekeeper-readiness-smoke` runs after `npm run desktop:installed-project-io-smoke`. On macOS it uses local `spctl` assessment for the simulated installed app and generated DMG, writes an ignored `build/desktop/GrooveForge-*-gatekeeper-readiness.json` summary with acceptance booleans, `spctl` output, readiness blockers, `networkSubmissionAttempted: false`, `releaseGateClaimedGatekeeperApproval: false`, and `releaseGateClaimedExternalDistribution: false`. Rejection is recorded as a readiness blocker rather than hidden, because local ad-hoc/not-notarized artifacts should not be reported as Gatekeeper-approved.

`npm run desktop:release-manifest-smoke` runs after `npm run desktop:gatekeeper-readiness-smoke`. On macOS it writes an ignored `build/desktop/.../GrooveForge-*-release-manifest.json` with app, DMG, and PKG paths, byte sizes, SHA-256 checksums, GrooveForge bundle metadata, key app payload checksums, local ad-hoc signed distribution scope, ad-hoc signature evidence, and explicit false flags for Developer ID signing, notarization, auto-update, and external distribution-channel QA. This smoke makes local release artifacts traceable without claiming external certificate signing or distribution completion.

`npm run desktop:release-notes-smoke` runs after `npm run desktop:release-manifest-smoke`. On macOS it writes ignored `build/desktop/.../GrooveForge-*-release-notes.md` and `GrooveForge-*-release-notes.json` artifacts from the local release manifest, names the all-genre direct-composition product scope, audience posture, app/DMG/checksum evidence, local privacy posture, and current external-distribution blockers, and records no release URLs, support URLs, feed values, credentials, tokens, or channel values.

`npm run desktop:support-artifact-smoke` runs after `npm run desktop:release-notes-smoke`. On macOS it writes ignored `build/desktop/.../GrooveForge-*-support.md` and `GrooveForge-*-support.json` artifacts from the local release manifest and release notes artifact, names install/launch scope, first-session help, export help, update support posture, local-first privacy posture, and current external-distribution blockers, and records no support URLs, release URLs, feed values, credentials, tokens, or channel values.

`npm run desktop:update-feed-config-smoke` runs after `npm run desktop:support-artifact-smoke`. It loads the ignored distribution local env file through the shared redacted loader, imports the shared Electron update feed policy, validates feed URL/channel env keys, verifies missing, valid, fallback, non-HTTPS, credential-bearing, fragment-bearing, and invalid-channel configurations without contacting a feed, writes an ignored `build/desktop/GrooveForge-*-update-feed-config.json` with only local env loader metadata, redacted env-key names, validation booleans, and blockers, and records `localEnvValueRecorded: false`, `networkProbeAttempted: false`, `releaseGateClaimedAutoUpdate: false`, and `releaseGateClaimedExternalDistribution: false`.

`npm run desktop:update-metadata-policy-smoke` runs after `npm run desktop:update-feed-config-smoke`. On macOS it loads the ignored distribution local env file through the shared redacted loader, then writes an ignored `build/desktop/.../GrooveForge-*-update-metadata-policy.json` with the local release manifest input, required `latest-mac.yml`, `app-update.yml`, and DMG blockmap policy, feed/channel environment-key names without values, and Developer ID signing, notarization, Gatekeeper acceptance, and distribution-channel prerequisites. It records `localEnvValueRecorded: false`, `networkProbeAttempted: false`, `releaseGateClaimedAutoUpdate: false`, `releaseGateClaimedDeveloperIdSigning: false`, `releaseGateClaimedNotarization: false`, `releaseGateClaimedGatekeeperApproval: false`, and `releaseGateClaimedExternalDistribution: false`.

`npm run desktop:update-metadata-artifacts-smoke` runs after `npm run desktop:update-metadata-policy-smoke`. On macOS it writes ignored local draft `latest-mac.yml`, `app-update.yml`, `GrooveForge-*.dmg.blockmap`, and `GrooveForge-*-update-metadata-artifacts.json` files from selected DMG evidence, preferring a notarized isolated DMG when Developer ID signing, notarization/stapling, and notarized Gatekeeper summaries are ready and otherwise falling back to the release manifest DMG. The draft files record DMG size/checksum metadata, selected-source state, signed update artifact readiness, and redacted provider placeholders only, do not publish an update feed, and record `localEnvValueRecorded: false`, `feedValueRecorded: false`, `channelValueRecorded: false`, `updateFeedPublishAttempted: false`, `releaseGateClaimedAutoUpdate: false`, and `releaseGateClaimedExternalDistribution: false`.

`npm run desktop:auto-update-readiness-smoke` runs after `npm run desktop:update-metadata-artifacts-smoke`. On macOS it loads the ignored distribution local env file through the shared redacted loader, then writes an ignored `build/desktop/GrooveForge-*-auto-update-readiness.json` summary, inspects the Electron built-in `autoUpdater` Help > Check for Updates integration, release manifest metadata, update feed config policy, update metadata policy, local update metadata artifact drafts, selected signed/notarized update artifact evidence, and bounded update feed/channel environment-key presence without recording feed, channel, or local env values or probing a remote feed. It records `localEnvValueRecorded: false`, `networkProbeAttempted: false`, `releaseGateClaimedAutoUpdate: false`, and `releaseGateClaimedExternalDistribution: false`, then reports missing or unsafe provider/feed/channel metadata separately from unsigned, not-notarized, or not-Gatekeeper-accepted update artifacts instead of claiming automatic updates.

`npm run desktop:developer-id-readiness-smoke` writes an ignored `build/desktop/GrooveForge-*-developer-id-readiness.json` summary. On macOS it loads the ignored distribution local env file through the shared redacted loader, then checks `codesign`, `security find-identity`, valid Developer ID Application identity count, `xcrun notarytool`, `xcrun stapler`, and bounded notary credential signal presence through environment key names without printing credential values or identity labels. It records local env loader metadata, `localEnvValueRecorded: false`, `developerIdIdentityValueRecorded: false`, `credentialValueRecorded: false`, `networkSubmissionAttempted: false`, `releaseGateClaimedExternalDistribution: false`, readiness booleans, and blockers so the local release gate can pass while still making missing external-distribution prerequisites explicit. This smoke does not submit to Apple notary services and does not claim Developer ID signing, notarization, Gatekeeper approval, auto-update, app-store submission, or external distribution-channel QA.

`npm run desktop:developer-id-signing-smoke` writes an ignored `build/desktop/GrooveForge-*-developer-id-signing.json` summary. On macOS it loads the ignored distribution local env file, requires explicit `GROOVEFORGE_DEVELOPER_ID_IDENTITY`, inspects keychain Developer ID Application identities, and signs only an isolated copied app under `build/desktop/.../developer-id-signing-smoke/` when a matching identity exists. It verifies Developer ID authority, `app.grooveforge.desktop`, hardened runtime flags, and the required Electron runtime entitlements, while recording only local env loader metadata, identity counts, matching booleans, and `developerIdIdentityValueRecorded: false`; identity labels and fingerprints are not written, while keeping `networkSubmissionAttempted: false`, `releaseGateClaimedDeveloperIdSigning: false`, `releaseGateClaimedNotarization: false`, `releaseGateClaimedGatekeeperApproval: false`, and `releaseGateClaimedExternalDistribution: false`. Without a matching configured identity it records blockers and does not attempt signing.

`npm run desktop:notarization-smoke` writes an ignored `build/desktop/GrooveForge-*-notarization.json` summary. On macOS it loads the ignored distribution local env file, requires explicit `GROOVEFORGE_NOTARY_SUBMIT=1` before Apple notary submission, checks the Developer ID signing summary and isolated signed app copy, checks `xcrun notarytool submit`, `xcrun stapler`, `hdiutil`, and bounded credential signals, then prepares an isolated notarization DMG under `build/desktop/.../notarization-smoke/` only when a Developer ID signed app copy exists. If explicit submission and credentials are present, it submits with `notarytool --wait --output-format json`, staples accepted tickets, and validates the staple. Default QA does not submit to Apple services; missing identity, credentials, signed copy, or submit flag are recorded as blockers while local env values, credential values, identity labels, `releaseGateClaimedDeveloperIdSigning: false`, `releaseGateClaimedNotarization: false`, `releaseGateClaimedGatekeeperApproval: false`, and `releaseGateClaimedExternalDistribution: false` remain explicit.

`npm run desktop:notarized-gatekeeper-smoke` writes an ignored `build/desktop/GrooveForge-*-notarized-gatekeeper.json` summary. On macOS it reads the notarization summary, requires `notarizationReady`, `notarizationAccepted`, `stapled`, `stapleValidationPassed`, and the isolated notarization DMG path before assessment, then uses local `spctl` checks for the stapled DMG plus the mounted app and detaches the read-only mount deterministically. Default QA does not submit to Apple services and records missing notarized/stapled artifacts as blockers while `releaseGateClaimedDeveloperIdSigning: false`, `releaseGateClaimedNotarization: false`, `releaseGateClaimedGatekeeperApproval: false`, and `releaseGateClaimedExternalDistribution: false` remain explicit.

`npm run desktop:distribution-manual-qa-smoke` runs after `npm run desktop:notarized-gatekeeper-smoke`. On macOS it writes ignored `build/desktop/.../GrooveForge-*-distribution-manual-qa.md` and `GrooveForge-*-distribution-manual-qa.json` artifacts from local release, support, update, signing, notarization, and Gatekeeper summaries. It lists the manual operator checks required before `GROOVEFORGE_DISTRIBUTION_QA_APPROVED=1` for selected signed artifact, release notes, support content, update metadata/feed, user-facing update behavior, Developer ID signing, notarization/stapling, and notarized Gatekeeper, computes deterministic `manualQaChecklistSha256` evidence, and instructs operators to set `GROOVEFORGE_DISTRIBUTION_QA_CHECKLIST_SHA256` to the current checklist SHA-256 before approval while recording no private values and making no approval or distribution claim.

`npm run desktop:distribution-channel-qa-smoke` writes an ignored `build/desktop/GrooveForge-*-distribution-channel-qa.json` summary after the manual QA checklist. On macOS it reads the local release manifest, release notes artifact, support artifact, update feed config, update metadata policy, auto-update readiness, Developer ID signing, notarization, notarized Gatekeeper, and distribution manual QA summaries; validates redacted distribution channel metadata environment-key presence and URL safety; requires `GROOVEFORGE_DISTRIBUTION_QA_CHECKLIST_SHA256` to match the current manual checklist digest before accepting `GROOVEFORGE_DISTRIBUTION_QA_APPROVED=1`; records `networkProbeAttempted: false`, `releaseUploadAttempted: false`, and false release claims; and reports blockers when channel metadata, release notes, support artifact, Developer ID signing, notarization, Gatekeeper acceptance, auto-update, update metadata, manual QA checklist, checklist digest match, or manual channel QA approval is missing. It never records release URLs, support URLs, credentials, tokens, or private feed values.

The digest check extends the existing blocker contract that reports blockers when channel metadata, release notes, support artifact, Developer ID signing, notarization, Gatekeeper acceptance, auto-update, update metadata, manual QA checklist, or manual channel QA approval is missing.

`npm run desktop:distribution-handoff-smoke` runs after `npm run desktop:distribution-channel-qa-smoke`. On macOS it writes ignored `build/desktop/.../GrooveForge-*-distribution-handoff.md` and `GrooveForge-*-distribution-handoff.json` artifacts that consolidate the local release manifest, release notes, support artifact, update feed config, update metadata policy, auto-update readiness, Developer ID readiness/signing, notarization, notarized Gatekeeper, manual QA checklist, and distribution-channel QA summaries into one redacted handoff. It records only environment-key names and readiness booleans, reports current external-distribution blockers, and records no release URLs, support URLs, feed values, credentials, tokens, or channel values.

`npm run desktop:distribution-bundle-manifest-smoke` runs after `npm run desktop:distribution-handoff-smoke`. On macOS it writes ignored `build/desktop/.../GrooveForge-*-distribution-bundle-manifest.md` and `GrooveForge-*-distribution-bundle-manifest.json` artifacts that reference the local release manifest, release notes, support artifact, distribution handoff, update metadata policy, auto-update readiness, Developer ID signing, notarization, notarized Gatekeeper, and distribution-channel QA summaries. It records local ignored paths, bytes, SHA-256 evidence, required private input key names, bundle manifest blockers, and external-distribution blockers only; it does not copy the DMG or app bundle and records no release URLs, support URLs, feed values, credentials, tokens, identity labels, or channel values.

`npm run desktop:distribution-env-template-smoke` runs after `npm run desktop:distribution-bundle-manifest-smoke`. It validates `harness/templates/distribution-private-inputs.env.example`, the ignored `.env.distribution.local` posture, optional `GROOVEFORGE_DISTRIBUTION_ENV_FILE`, required private key coverage including `GROOVEFORGE_DISTRIBUTION_QA_CHECKLIST_SHA256`, placeholder rejection, and the shared local-env loader, then writes ignored `build/desktop/.../GrooveForge-*-distribution-env-template.md` and `GrooveForge-*-distribution-env-template.json` artifacts with key names, local env readiness, and loader blockers only. It records no release URLs, support URLs, feed values, credentials, tokens, identity labels, channel values, private beats, or real user audio.

`npm run release:prepare-env-smoke` runs after `npm run desktop:distribution-env-template-smoke` in the release gate. It parses `harness/templates/distribution-private-inputs.env.example`, writes ignored `build/desktop/.../GrooveForge-*-release-prepare-env.md`, `GrooveForge-*-release-prepare-env.json`, and a guided build scaffold without touching root `.env.distribution.local`. The scaffold groups distribution metadata, manual QA approval, update feed/channel, Developer ID identity, and notarization credential keys with local-only guidance and rerun commands, and records no release URLs, support URLs, feed values, credentials, tokens, identity labels, channel values, private beats, or real user audio. `npm run release:prepare-env-write-smoke` runs immediately after it, exercises the local-env write branch against an ignored build-scoped `.env.distribution.local`, reports the write target, placeholder counts, and real-root modification status, and proves the real root `.env.distribution.local` was not modified. `npm run release:prepare-env` is the explicit operator command that creates ignored `.env.distribution.local` and refuses to overwrite it unless rerun with `--force`.

`npm run desktop:distribution-private-inputs-smoke` runs after `npm run desktop:distribution-env-template-smoke`, `npm run release:prepare-env-smoke`, and `npm run release:prepare-env-write-smoke`. On macOS it writes ignored `build/desktop/.../GrooveForge-*-distribution-private-inputs.md` and `GrooveForge-*-distribution-private-inputs.json` artifacts, groups required release/support URL, update feed/channel, Developer ID identity, notary credential, channel metadata, `GROOVEFORGE_DISTRIBUTION_QA_APPROVED`, and `GROOVEFORGE_DISTRIBUTION_QA_CHECKLIST_SHA256` manual approval keys, records key names, local env placeholder key names/counts, presence/validation booleans, value-free placeholder blockers, local env loader status, and blockers only, reads bundle/handoff/channel/update/signing/notarization/Gatekeeper summaries, and records no URL/feed/credential/token/identity/channel values.

`npm run release:channel-unblock-smoke` runs after `npm run desktop:distribution-private-inputs-smoke` in the release gate. It writes an isolated synthetic `.env.distribution.local` fixture only under ignored `build/desktop/`, temporarily ignores any configured `GROOVEFORGE_DISTRIBUTION_ENV_FILE` override while loading that fixture through the shared local-env loader, verifies the four release-channel metadata keys are present, non-placeholder, and shape-valid, proves the current placeholder blocker can clear once an operator supplies real ignored values, writes ignored Markdown/JSON unblock artifacts, and records no release URL, support URL, feed URL, credential, token, identity, channel value, private beat, real user audio, remote probe, signing, notary submission, upload, or external-distribution claim.

`npm run release:channel-live-check` is the operator-facing companion for real ignored local env edits. It reads the actual `.env.distribution.local` or configured local env file through the shared loader, inspects only `GROOVEFORGE_DISTRIBUTION_CHANNEL`, `GROOVEFORGE_RELEASE_DOWNLOAD_URL`, `GROOVEFORGE_RELEASE_NOTES_URL`, and `GROOVEFORGE_SUPPORT_URL`, writes ignored `build/desktop/.../GrooveForge-*-release-channel-live-check.md` and `GrooveForge-*-release-channel-live-check.json` artifacts, and records key names, file/line edit locations, placeholder booleans, shape readiness, current-ready counts, and follow-up commands only. It passes truthfully with readiness false while placeholders remain, records no URL/channel/feed/credential/token/identity values, and makes no remote probe, signing, notary submission, upload, or external-distribution claim. `npm run release:channel-live-check-strict` uses the same parser and redaction path with `--strict`, writes separate `release-channel-live-check-strict` Markdown/JSON artifacts, and exits non-zero until all four current metadata rows are present, non-placeholder, and shape-ready. `npm run release:channel-live-check-strict-success-smoke` runs that same strict path against a synthetic ignored env root and writes separate `release-channel-live-check-strict-success-smoke` Markdown/JSON artifacts so the strict pass branch is covered without overwriting the real placeholder receipt.

`npm run release:update-feed-live-check` is the operator-facing companion for the `auto-update-feed` step after release-channel metadata clears. It reads the actual ignored `.env.distribution.local` or configured local env file through the shared loader, inspects only `GROOVEFORGE_UPDATE_FEED_URL`, `ELECTRON_UPDATE_FEED_URL`, `UPDATE_FEED_URL`, `GROOVEFORGE_UPDATE_CHANNEL`, `ELECTRON_UPDATE_CHANNEL`, and `UPDATE_CHANNEL`, writes ignored `release-update-feed-live-check` Markdown/JSON artifacts, and records key names, selected-key booleans, placeholder booleans, shape readiness, file/line edit locations, current 10-plan progress, and non-claiming safety fields only. It passes truthfully with readiness false while feed/channel metadata is missing or placeholder, records no feed URL or channel values, probes no update feed, publishes no feed, signs nothing, and claims neither auto-update nor external distribution. `npm run release:update-feed-live-check-strict` uses the same parser and redaction path with `--strict`, writes separate strict artifacts, and exits non-zero until one update feed URL key and one update channel key are selected, non-placeholder, and shape-ready. `npm run release:update-feed-live-check-strict-success-smoke` runs that strict path against a synthetic ignored env root and proves the ready branch with two selected-ready keys, zero placeholders, no real local env read/modify, no feed/channel values in output, and no auto-update or external distribution claim.

`npm run release:update-feed-post-edit-proof` is the operator-facing wrapper after ignored update feed/channel edits. It runs the real update feed live check first and the real auto-update readiness smoke second, writes ignored `release-update-feed-post-edit-proof` Markdown/JSON artifacts, and records command rows, source artifact rows, live-check posture, strict failure rows, placeholder edit locations, real auto-update blocker rows, signed-update artifact readiness, hard-gate boundary, current 10-plan progress, completion percentage, and not-recorded/not-claimed posture. It stays outside `npm run verify` while private placeholder values remain expected.

`npm run release:update-feed-post-edit-proof-success-smoke` runs the same wrapper with a separate success-smoke artifact stem, using the synthetic strict-ready update feed live-check smoke before the real auto-update readiness smoke. It proves the live-ready branch with `2/2` selected-ready feed/channel keys and zero placeholders while preserving downstream auto-update blockers, signed-update artifact boundary, hard-gate boundary, value redaction, and false auto-update/external distribution claims.

`npm run release:update-feed-checkpoint-smoke` refreshes the real update feed post-edit proof first and the synthetic success-path proof second, then writes separate ignored checkpoint Markdown/JSON artifacts. The checkpoint compares real ignored-env placeholder posture with the synthetic `2/2` selected-ready branch, preserves downstream auto-update blockers and signed-update artifact boundaries, keeps `npm run release:external-check` as the would-fail hard gate, records the current 10-plan progress, and keeps feed/channel/private values plus auto-update/external distribution claims false.

`npm run release:progress-refresh-smoke` refreshes the proof bundle and external distribution gate first, then runs the update-feed checkpoint, existing-evidence progress refresh, real placeholder input receipt, current-blocker refresh, completion report packet refresh, freshness check, and operator completion brief refresh in one value-free sequence. The refresh receipt proves current proof rows are aligned before progress reads them, proves the release-channel placeholder input receipt exists before current-blocker reads existing evidence, proves release-progress-report, release-current-blocker, release-completion-report-packet-smoke, release-channel-clearance-transition-smoke, release-auto-update-transition-smoke, release-progress-freshness, and release-operator-completion-brief labels match the latest checkpoint, records final fresh/stale/missing counts including completion report packet, clearance transition, and auto-update transition freshness, proves the operator brief is ready with source privacy boundaries and blocked-or-cleared release-channel metadata posture, exposes a compact `completionSummary` plus stable top-level aliases for post-work completion reporting, including the completion blocker action receipt with the current edit target, required keys, placeholder keys, placeholder locations, current private input placeholder locations, private input missing/placeholder/invalid location summaries, guided setup fallback command outside the primary current-operator sequence, first proof command, recommended strict proof chain, and focus rows, and keeps private values plus auto-update/external distribution claims false.

The current-blocker receipt has its own value-free freshness guard for existing evidence: it derives the latest completed-plan 10-plan window from `docs/exec_plans/completed`, compares it with the release-progress source label and rows, and surfaces stale evidence with `npm run release:completion-summary-refresh-smoke` and `npm run release:progress-refresh-smoke` guidance instead of silently presenting old completion rows as current.

`npm run release:completion-summary-smoke` reads the existing release-progress-refresh-smoke JSON, validates the compact `completionSummary`, then writes separate ignored Markdown/JSON readout artifacts for latest completed plan, 10-plan progress, user-facing completion and remaining percentage, freshness counts, release-channel blocker posture, operator proof command, strict proof handoff readiness, private-edit blocked-smoke coverage, final handoff success-redaction readiness, current first blocker, and the value-free completion blocker action/focus rows from the source refresh. When the source is missing, stale, or incomplete, it points after-work reporting to `npm run release:completion-summary-refresh-smoke` and source-only refreshes to `npm run release:progress-refresh-smoke`.

`npm run release:completion-summary-refresh-smoke` runs progress refresh, completion-summary smoke, and the conditional 10-plan checkpoint before writing a value-free after-work receipt. It validates latest plan, 10-plan progress, checkpoint requirement/run status, checkpoint proof/gate refresh readiness, checkpoint user-report receipt readiness, completion percentage, explicit user-facing completion/remaining aliases, freshness, strict proof handoff, private-edit blocked smoke, final handoff success-redaction, current blocker, completion blocker action/focus rows, guided setup fallback command, real preflight fallback alignment, command order, source artifact alignment, git branch, full and short HEAD SHA, worktree basename, dirty file count, no recorded git status paths, and non-claiming release posture before the user-facing completion report.

`npm run release:10-plan-checkpoint-smoke` reads the existing release-progress-refresh-smoke JSON, independently derives the current 10-plan window from `docs/exec_plans/completed`, and writes ignored checkpoint Markdown/JSON artifacts only when the refreshed source and local completed-plan files agree that the window is at `10/10`. It requires the source refresh command sequence to start with `npm run release:proof-bundle` and `npm run desktop:external-distribution-gate-smoke`, mirrors those proof/gate rows as value-free checkpoint evidence, and preserves the compact completion summary, current blocker, operator proof command, hard-gate would-fail posture, and value-free/non-claiming release boundary while keeping the legacy current-boundary field separate from the post-delivery next 10-plan report target.

After each completed work, run `npm run release:completion-summary-refresh-smoke` before reporting the user-facing completion percentage. Read `userFacingCompletionPercent`, `userFacingCompletionLabel`, `userFacingRemainingPercent`, and `userFacingRemainingLabel` from the receipt when automation needs the same values shown to the user. Read the completion blocker action receipt from the same receipt when the report needs the current unblock action. When the completed plan closes a 10-plan window, that refresh runs `npm run release:10-plan-checkpoint-smoke` automatically to capture the 10/10 checkpoint and mirrors the checkpoint User Report Receipt into the refresh receipt, covering boundary plan, 10-plan progress, completion, remaining work, current blocker, operator start command, guided fallback, strict proof command, edit target, and external-distribution non-claim posture. Use the checkpoint command directly only for a manual rerun from already refreshed source evidence. Use the receipt's git context to distinguish ignored build evidence generated from an old checkout or removed worktree from evidence generated by the current branch and HEAD. Use `npm run release:progress-refresh-smoke` when only the source bundle needs refreshing, and use `npm run release:progress-smoke` only as the narrower existing-evidence release-progress rewrite after the source bundle is already refreshed.

`npm run release:progress-freshness-smoke` refreshes that checkpoint, then writes separate ignored release-progress-freshness-smoke Markdown/JSON artifacts. The freshness receipt compares the latest checkpoint progress label with existing release-progress-report, release-current-blocker, release-completion-report-packet-smoke, release-channel-clearance-transition-smoke, and release-auto-update-transition-smoke labels, marks each source as fresh, stale, or missing, and emits value-free rerun commands including `npm run release:completion-report-packet-smoke`, `npm run release:channel-clearance-transition-smoke`, and `npm run release:auto-update-transition-smoke` while leaving stale optional artifacts as evidence freshness gaps rather than completion claims.

Update feed live-check, post-edit proof, update-feed checkpoint, and release 10-plan checkpoint receipts derive the current 10-plan window from `docs/exec_plans/completed` only, then count completed plan files inside that window. This keeps the operator-facing progress receipt aligned with user-facing completion progress while an active plan is still in flight, without another hardcoded script update.

`npm run release:progress` and `npm run release:progress-smoke` refresh `npm run release:channel-live-check` before writing the user-facing release progress report, then mirror live-check source readiness, command, first proof after private edits, recommended `npm run release:private-edit-strict-proof` operator proof chain, current-ready rows, placeholder keys, placeholder edit locations, follow-up proof commands, and value-redaction posture into JSON, Markdown, and console output. `npm run release:current-blocker` and `npm run release:current-blocker-smoke` mirror those same live-check receipt fields from release progress so the compact current-blocker report shows both the synthetic unblock rehearsal and the real ignored-env live-check posture without recording URL or channel values.

`npm run release:post-edit-proof` is the operator-facing wrapper after ignored local env edits. It runs `npm run release:channel-live-check` first, then `npm run release:current-blocker`, and writes ignored `release-post-edit-proof` Markdown/JSON artifacts with command order, live-check readiness, placeholder counts/locations, current blocker posture, completion percentage, current 10-plan progress, and non-claiming safety fields without recording URL or channel values.

`npm run release:post-edit-proof-success-smoke` is the value-free success-path rehearsal for that wrapper. It writes ignored `release-post-edit-proof-success-smoke` Markdown/JSON artifacts from synthetic ready evidence, proves four current-ready release-channel metadata rows, zero placeholder keys, the same live-check/current-blocker command order, no real local env read or modification, and no URL/channel values or external distribution claim.

`npm run release:post-edit-proof-bundle` runs the success-path rehearsal first and the real post-edit proof second, then writes ignored `release-post-edit-proof-bundle` Markdown/JSON artifacts that keep success-branch coverage, current real release-channel blocker posture, current 10-plan progress, source artifact rows, readiness rows, and non-claiming safety fields in one value-free receipt without recording URL/channel values.

Current-blocker reporting keeps release-channel unblock readiness, release-channel live-check source/readiness/current-ready/placeholder-key posture, `npm run release:channel-live-check` as the first proof after private edits, `npm run release:private-edit-strict-proof` as the recommended operator proof chain, placeholder-blocker-cleared rehearsal posture, and release-channel live-check rows/placeholder locations mirrored from release progress in one value-free operator receipt.

The same receipt preserves release-channel unblock readiness, release-channel live-check source/readiness/current-ready/placeholder-key posture, placeholder-blocker-cleared rehearsal posture, and the live-check first-proof command without recording private values.

This keeps the private-input grouping that groups required release/support URL, update feed/channel, Developer ID identity, notary credential, channel metadata, and manual approval keys.

`npm run desktop:completion-audit-smoke` runs after `npm run desktop:distribution-private-inputs-smoke`. On macOS it writes ignored `build/desktop/.../GrooveForge-*-completion-audit.md` and `GrooveForge-*-completion-audit.json` artifacts that consolidate release readiness docs, native/packaged/PKG payload/installed project IO artifacts, release/package artifacts, support/update/signing/notarization/Gatekeeper summaries, distribution handoff, bundle manifest, env-template evidence, and private-input evidence. It reports local MVP evidence readiness, local desktop package evidence readiness, desktop project IO evidence readiness, explicit PKG payload project IO readiness, redacted distribution evidence readiness, external distribution readiness, requirement audit rows, local env loader status, local completion blockers, and external distribution blockers while recording no URL/feed/credential/token/identity/channel values and making no Developer ID, notarization, Gatekeeper, auto-update, app-store, upload, or external distribution claims.

`npm run desktop:external-distribution-gate-smoke` runs after `npm run desktop:completion-audit-smoke`. It runs the external distribution gate in `--dry-run` mode, writes ignored `build/desktop/.../GrooveForge-*-external-distribution-gate.md` and `GrooveForge-*-external-distribution-gate.json` artifacts, and reports whether hard mode would fail. It reads completion audit, including the desktop project IO readiness requirement and explicit PKG payload project IO readiness, env-template, private-input, distribution-channel QA, auto-update, Developer ID signing, notarization, notarized Gatekeeper summaries, and the external proof bundle when present; when proof-bundle evidence exists, it mirrors the current next command, first blocker, current env edit rows, current proof checklist rows, current action checklist rows, and current command verification rows with `valueRecorded: false`; it records no URL/feed/credential/token/identity/channel values and makes no external distribution completion claim in dry-run mode.

Production build validation depends on Vite 8 / Rolldown output code splitting, not warning suppression. `vite.config.ts` keeps `base: "./"` so Electron `file:` loading resolves built assets relative to `dist/index.html`, keeps `outDir: "dist"` and `sourcemap: true`, then uses `build.rolldownOptions.output.codeSplitting.groups` for `react-vendor`, `icons-vendor`, remaining `vendor`, `audio-engine`, `workstation-core`, `workstation-ui-model`, `workstation-editor-audition`, `workstation-selected-actions`, `workstation-pattern-tools`, `workstation-mix-panels`, `workstation-compose-panels`, `workstation-guidance-panels`, `workstation-shell-panels`, `workstation-snapshot-compare`, `workstation-analysis`, `workstation-app-helpers`, `workstation-app-quick-action-route-labels`, `workstation-app-quick-action-palette`, `workstation-app-quick-actions`, and `workstation-app-derivations` paths so eligible modules split when present and stable dependencies plus audio engine, workstation UI helper code, quick-action route-label helpers, quick-action command-palette helpers, quick-action result/metric code, editor audition code, selected-event action code, render-only mix/master panels, render-only compose/editor panels, render-only Guided/Studio workflow panels, render-only shell panels, Snapshot Compare derivation code, app derivation code, and shared analysis helpers do not stay in one large client chunk.
