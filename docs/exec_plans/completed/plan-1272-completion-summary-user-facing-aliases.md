# plan-1272-completion-summary-user-facing-aliases

## Goal

Make the after-work completion summary refresh receipt expose explicit user-facing completion aliases in JSON so operator scripts and user reports can read the same completion values without guessing internal field names.

## Scope

- Add explicit value-free `userFacingCompletion...` and `userFacingRemaining...` fields to `npm run release:completion-summary-refresh-smoke`.
- Validate those aliases mirror the existing completion and remaining percentage fields.
- Update README, release readiness, harness architecture, quality rules, and QA text expectations.
- Run QA and completion summary refresh before/after moving the plan to completed.

## Out of Scope

- Changing completion percentage math.
- Filling private release-channel metadata.
- Developer ID signing, notarization, Gatekeeper approval, update feed publishing, release upload, or remote channel probing.

## Validation

- Passed: `python3 -c "import py_compile; py_compile.compile('harness/scripts/run_qa.py', cfile='/private/tmp/grooveforge-plan-1272-run_qa.pyc', doraise=True)"`.
- Passed: `node --check harness/scripts/run_release_completion_summary_refresh_smoke.mjs`.
- Passed: `npm run qa`.
- Expected pre-refresh blocker observed before source evidence was generated: `GROOVEFORGE_DISTRIBUTION_ENV_FILE=/private/tmp/grooveforge-plan-1262-placeholder.env npm run release:completion-summary-refresh-smoke` failed with guidance that completion-progress source evidence must be generated by `npm run release:check` or `npm run verify`.
- Passed: `GROOVEFORGE_DISTRIBUTION_ENV_FILE=/private/tmp/grooveforge-plan-1262-placeholder.env npm run verify` with approved macOS GUI/AppKit access.
- Passed: `GROOVEFORGE_DISTRIBUTION_ENV_FILE=/private/tmp/grooveforge-plan-1262-placeholder.env npm run release:completion-summary-refresh-smoke` after verify.
- Confirmed active-plan JSON aliases: latest plan `plan-1271`, 10-plan progress `1271-1280: 1/10`, `completionPercent` and `userFacingCompletionLabel` both `99.999999%`, `remainingPercent` and `userFacingRemainingLabel` both `0.000001%`, numeric aliases `99.999999` and `0.000001`, 5 value-free user-facing alias rows, git status paths not recorded.
- Passed: `git diff --check`.

## Decision Log

- 2026-07-02: Started after main completion summary refresh showed the completion values through console and `completionPercent`/`remainingPercent`, while direct JSON consumers did not have explicit `userFacing...` field names.
